import { type FormEvent, useEffect, useMemo, useRef, useState } from 'react';
import { Link, NavLink, useLocation, useNavigate, useParams } from 'react-router-dom';
import { ThemeToggle } from '../design-system/ThemeToggle';
import { categoryCatalog, featuredCategories } from '../data/category-catalog';
import { GameBoard, type BoardCell } from '../features/board/game-board';
import { generateBoard } from '../features/game/domain/board';
import { connectionLabel, stateLabel } from '../features/ui/game-state';
import type { IntentType, ProjectionEnvelope, SafeProjection } from '../features/game/runtime/contracts';
import { gameRuntime } from '../features/game/runtime';

type StoredCapability = { token: string; role: 'host' | 'player' | 'audience' };
type ViewProjection = SafeProjection & { question?: { headerAr?: string; promptAr?: string; primaryAnswer?: string; acceptedAnswers?: string[]; revealedAnswer?: string; sources?: Array<{ title?: string; url?: string }> }; audit?: Array<{ revision: number; type: string; payload?: unknown }> };
const capabilityKey = (id: string) => `huroof:${id}`;
const capabilityFor = (id: string) => { try { const value = sessionStorage.getItem(capabilityKey(id)); return value ? JSON.parse(value) as StoredCapability : undefined; } catch { return undefined; } };
const saveCapability = (id: string, value: StoredCapability) => sessionStorage.setItem(capabilityKey(id), JSON.stringify(value));
const readableError = (error: unknown) => error instanceof Error ? error.message.replace(/^Error:\s*/, '') : 'تعذر إتمام العملية.';
const previewLetters = ['أ', 'ب', 'ت', 'ث', 'ج', 'ح', 'خ', 'د', 'ذ', 'ر', 'ز', 'س', 'ش', 'ص', 'ض', 'ط'];
/** Stable, rule-generated board while a room projection is still loading. */
export const previewBoardCells: BoardCell[] = generateBoard(0x4855524f, previewLetters).cells;
function useDocumentTitle(title: string) { useEffect(() => { document.title = `${title} | استوديو الحروف`; }, [title]); }
const teamName = (name?: string) => name?.trim().startsWith('فريق') ? name.trim() : `فريق ${name?.trim() || 'غير محدد'}`;

export function AppHeader() { return <><a className="skip-link" href="#main-content">تجاوز إلى المحتوى</a><header className="app-header"><Link className="wordmark" to="/">استوديو الحروف</Link><nav aria-label="التنقل الرئيسي"><NavLink to="/how-to-play">كيف تلعب؟</NavLink><NavLink to="/questions">الأسئلة</NavLink></nav><ThemeToggle /></header></>; }
export function DemoBanner({ demo }: { demo?: true }) { return demo ? <p className="demo-banner" role="status">وضع تجريبي بمسودات غير مراجعة. يمكن للمضيف بدء مباراة بلا لاعبين والتحكم بالفريقين، أو انتظار فريقين جاهزين.</p> : null; }
const AxisMark = ({ axis }: { axis: 'horizontal' | 'vertical' }) => <span className={`axis-mark axis-mark--${axis}`}>{axis === 'horizontal' ? '↔ أفقي' : '↕ عمودي'}</span>;

export function HostNewRoute() {
  useDocumentTitle('إنشاء مباراة');
  const navigate = useNavigate();
  const [form, setForm] = useState({ mode: 'classic' as 'classic' | 'fast' | 'custom', horizontal: 'فريق ↔', vertical: 'فريق ↕', questionSeconds: 20, opponentSeconds: 10, demo: true, categories: [] as string[] });
  const [error, setError] = useState(''); const [busy, setBusy] = useState(false);
  const update = <K extends keyof typeof form>(key: K, value: typeof form[K]) => setForm((current) => ({ ...current, [key]: value }));
  const toggleCategory = (id: string) => update('categories', form.categories.includes(id) ? form.categories.filter((item) => item !== id) : [...form.categories, id]);
  async function create(event: FormEvent) { event.preventDefault(); setBusy(true); setError(''); try { const request = { displayName: 'المضيف', demo: form.demo, mode: form.mode, questionSeconds: form.questionSeconds, opponentSeconds: form.opponentSeconds, teams: { horizontal: form.horizontal, vertical: form.vertical } }; const body = gameRuntime.kind === 'fixture' ? await fetch('/api/rooms', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify(request) }).then(async (response) => { if (!response.ok) throw new Error((await response.json() as { error?: string }).error ?? 'تعذر إنشاء الغرفة.'); return response.json() as Promise<{ roomId: string; roomCode: string; token: string }>; }) : await gameRuntime.createRoom(request); if (!body.roomId || !body.roomCode) throw new Error('تعذر إنشاء الغرفة.'); saveCapability(body.roomId, { token: body.token ?? '', role: 'host' }); sessionStorage.setItem(`huroof:code:${body.roomCode}`, body.roomId); navigate(`/room/${body.roomCode}/lobby`); } catch (reason) { setError(readableError(reason)); } finally { setBusy(false); } }
  return <main className="app-page" id="main-content"><AppHeader /><section className="setup-page" aria-labelledby="setup-title"><div><p className="eyebrow">إعداد المباراة</p><h1 id="setup-title">أنشئ مباراة</h1></div><form className="setup-form" id="match-setup-form" onSubmit={create}><section><h2>المباراة</h2><div className="segmented" role="radiogroup" aria-label="نمط المباراة">{(['classic', 'fast', 'custom'] as const).map((mode) => <button aria-checked={form.mode === mode} className={form.mode === mode ? 'is-selected' : ''} key={mode} onClick={() => update('mode', mode)} role="radio" type="button">{({ classic: 'كلاسيكية', fast: 'سريعة', custom: 'مخصصة' })[mode]}</button>)}</div><p className="field-note">تنتهي المباراة بفوز فريق بجولتين متتاليتين أو بثلاث جولات إجمالاً</p></section><section className="team-fields"><h2>الفريقان</h2><label>اسم الفريق الأفقي <AxisMark axis="horizontal" /><input value={form.horizontal} onChange={(event) => update('horizontal', event.target.value)} /></label><label>اسم الفريق العمودي <AxisMark axis="vertical" /><input value={form.vertical} onChange={(event) => update('vertical', event.target.value)} /></label></section><section><h2>الفئات</h2><p className="field-note">اختيار الفئات محفوظ في ملخص الإعداد فقط؛ لا يغيّر ترشيح الأسئلة على الخادم في هذا الإصدار.</p><div className="category-grid">{categoryCatalog.map((category) => <button aria-pressed={form.categories.includes(category.id)} className={`category-choice ${form.categories.includes(category.id) ? 'is-selected' : ''}`} key={category.id} onClick={() => toggleCategory(category.id)} type="button"><img alt="" src={`/${category.cover.web320}`} /><span>{category.displayNameAr}</span><small>{category.questionReadiness === 'ready' ? 'متاح' : 'مخزون قيد التجهيز'}</small></button>)}</div><p className="rights-note">أغلفة الفئات للاستعراض المحلي فقط؛ حقوق النشر غير مؤكدة ولا يجوز نشرها.</p></section><section className="timing-fields"><h2>التوقيت</h2><label>وقت السؤال<input min="10" max="60" type="number" value={form.questionSeconds} onChange={(event) => update('questionSeconds', Number(event.target.value))} /></label><label>فرصة الخصم<input min="10" max="60" type="number" value={form.opponentSeconds} onChange={(event) => update('opponentSeconds', Number(event.target.value))} /></label></section><label className="demo-control"><input checked={form.demo} onChange={(event) => update('demo', event.target.checked)} type="checkbox" /> استخدم مسودات تجريبية صريحة؛ يمكن للمضيف تشغيل الفريقين دون لاعبين.</label></form><aside className="setup-summary"><p className="eyebrow">ملخص مباشر</p><h2>{form.horizontal} <span>×</span> {form.vertical}</h2><p><AxisMark axis="horizontal" /> مقابل <AxisMark axis="vertical" /></p><dl><div><dt>قاعدة الفوز</dt><dd>تنتهي المباراة بفوز فريق بجولتين متتاليتين أو بثلاث جولات إجمالاً</dd></div><div><dt>الفئات المختارة</dt><dd>{form.categories.length}</dd></div><div><dt>المخزون المعتمد</dt><dd>0</dd></div><div><dt>التوقيت</dt><dd>{form.questionSeconds}ث / {form.opponentSeconds}ث</dd></div></dl><p className="field-note">ابدأ بلا لاعبين للتحكم بالفريقين، أو انضم بلاعبين من الفريقين واجعلهم جميعاً جاهزين.</p><button className="button button--primary" data-testid="create-room" disabled={busy || !form.demo} form="match-setup-form" title={!form.demo ? 'لا يوجد مخزون معتمد كافٍ' : undefined} type="submit">{busy ? 'جارٍ الإنشاء…' : 'أنشئ الغرفة التجريبية'}</button><p aria-live="polite" className="form-message">{error || (!form.demo ? 'لا يوجد مخزون معتمد كافٍ لإنشاء مباراة عادية.' : '')}</p></aside></section></main>;
}

function useRoom(code?: string, autoAudience = false, projectionRole?: 'host' | 'player' | 'audience') {
  const [envelope, setEnvelope] = useState<ProjectionEnvelope<ViewProjection> | null>(null); const [connection, setConnection] = useState<'connecting' | 'connected' | 'reconnecting' | 'offline' | 'stale'>('connecting'); const [error, setError] = useState('');
  const retry = useRef<number | undefined>(undefined); const socketRef = useRef<WebSocket | undefined>(undefined); const connecting = useRef(false);
  useEffect(() => {
    let alive = true; const online = () => navigator.onLine !== false;
    const clearRetry = () => { if (retry.current !== undefined) { window.clearTimeout(retry.current); retry.current = undefined; } };
    const closeSocket = () => { const socket = socketRef.current; socketRef.current = undefined; connecting.current = false; if (socket && socket.readyState < WebSocket.CLOSING) socket.close(); };
    const schedule = (connect: () => void, delay = 1_000) => { if (alive && online() && retry.current === undefined && !socketRef.current && !connecting.current) retry.current = window.setTimeout(() => { retry.current = undefined; connect(); }, delay); };
    const connect = async () => {
      if (!alive) return; if (!online()) { setConnection('offline'); return; }
      if (connecting.current || socketRef.current) return; connecting.current = true;
      try {
        if (!code) throw new Error('ROOM_CODE_REQUIRED'); let roomId = sessionStorage.getItem(`huroof:code:${code}`); let cap = roomId ? capabilityFor(roomId) : undefined;
        if (!cap && autoAudience) { if (gameRuntime.kind === 'firebase' && gameRuntime.joinAudience) { const audience = await gameRuntime.joinAudience(code); roomId = audience.roomId; saveCapability(roomId, { token: '', role: 'audience' }); } else { const claim = await fetch(`/api/rooms/${code}/audience`, { method: 'POST' }); if (!claim.ok) throw new Error('تعذر فتح شاشة الجمهور.'); const audience = await claim.json() as { roomId: string; token: string }; roomId = audience.roomId; saveCapability(roomId, { token: audience.token, role: 'audience' }); } sessionStorage.setItem(`huroof:code:${code}`, roomId); cap = roomId ? capabilityFor(roomId) : undefined; }
        if (!roomId || !cap) throw new Error('افتح الرابط بعد الانضمام أو من رابط المضيف.'); const role = projectionRole ?? cap.role; if (gameRuntime.kind === 'firebase') { const unsubscribe = gameRuntime.subscribeProjection(roomId, role, '', (value) => { if (!alive) return; setEnvelope(value as ProjectionEnvelope<ViewProjection>); setConnection('connected'); setError(''); }, (reason) => { if (!alive) return; connecting.current = false; setConnection('offline'); setError(readableError(reason)); }); socketRef.current = { readyState: WebSocket.OPEN, close: unsubscribe } as unknown as WebSocket; connecting.current = false; return; } const response = await fetch(`/api/rooms/${roomId}`, { headers: { authorization: `Bearer ${cap.token}` } }); if (!response.ok) throw new Error('تعذر الاتصال بالغرفة.'); const snapshot = await response.json() as ProjectionEnvelope<ViewProjection>;
        if (!alive || !online()) return; setEnvelope(snapshot); setConnection('connected'); setError(''); const protocol = location.protocol === 'https:' ? 'wss:' : 'ws:'; const socket = new WebSocket(`${protocol}//${location.host}/ws?roomId=${encodeURIComponent(roomId)}&token=${encodeURIComponent(cap.token)}`); socketRef.current = socket;
        socket.onopen = () => { connecting.current = false; };
        socket.onmessage = (event) => { const value = JSON.parse(String(event.data)) as ProjectionEnvelope<ViewProjection> & { error?: string }; if (value.error) { setError(value.error); return; } setEnvelope(value); setConnection('connected'); };
        socket.onclose = () => { if (socketRef.current === socket) socketRef.current = undefined; connecting.current = false; if (alive && online()) { setConnection('reconnecting'); schedule(() => void connect()); } };
        socket.onerror = () => socket.close();
      } catch (reason) { connecting.current = false; if (!alive) return; setError(readableError(reason)); if (online()) { setConnection('reconnecting'); schedule(() => void connect()); } else setConnection('offline'); }
    };
    const offline = () => { clearRetry(); closeSocket(); setConnection('offline'); };
    const reconnect = () => { if (!alive) return; setConnection('reconnecting'); schedule(() => void connect(), 250); };
    window.addEventListener('offline', offline); window.addEventListener('online', reconnect); if (online()) void connect(); else offline();
    return () => { alive = false; window.removeEventListener('offline', offline); window.removeEventListener('online', reconnect); clearRetry(); closeSocket(); };
  }, [autoAudience, code, projectionRole]);
  const send = async (type: IntentType, payload: Record<string, unknown> = {}) => { if (connection !== 'connected') throw new Error('CONNECTION_UNAVAILABLE'); if (!envelope) return; if (gameRuntime.kind === 'firebase') { await gameRuntime.submitGameIntent(envelope.roomId, { type, payload, expectedRevision: envelope.revision, intentId: crypto.randomUUID() }); return; } const cap = capabilityFor(envelope.roomId); if (!cap) return; const response = await fetch(`/api/rooms/${envelope.roomId}/intents`, { method: 'POST', headers: { 'content-type': 'application/json', authorization: `Bearer ${cap.token}` }, body: JSON.stringify({ type, payload, expectedRevision: envelope.revision, intentId: crypto.randomUUID() }) }); const result = await response.json() as { projection?: ProjectionEnvelope<ViewProjection>; error?: string }; if (!response.ok) throw new Error(result.error ?? 'تعذر تنفيذ العملية.'); if (result.projection) setEnvelope(result.projection); };
  return { envelope, connection, error, send, reportError: setError };
}

function Countdown({ deadlineAt, serverTime, onDeadline }: { deadlineAt?: string; serverTime: string; onDeadline?: () => void }) {
  const [clientNow, setClientNow] = useState(() => Date.now()); const offset = useMemo(() => Date.parse(serverTime) - Date.now(), [serverTime]); const fired = useRef<string | undefined>(undefined);
  useEffect(() => { setClientNow(Date.now()); if (!deadlineAt) return; const timer = window.setInterval(() => setClientNow(Date.now()), 250); return () => window.clearInterval(timer); }, [deadlineAt, offset]);
  useEffect(() => { if (deadlineAt && Date.parse(deadlineAt) <= clientNow + offset && fired.current !== deadlineAt) { fired.current = deadlineAt; onDeadline?.(); } }, [clientNow, deadlineAt, offset, onDeadline]);
  if (!deadlineAt) return null; const seconds = Math.max(0, Math.ceil((Date.parse(deadlineAt) - (clientNow + offset)) / 1000)); const text = `${String(Math.floor(seconds / 60)).padStart(2, '0')}:${String(seconds % 60).padStart(2, '0')}`;
  return <p className="timer" data-testid="countdown" style={{ fontVariantNumeric: 'tabular-nums' }}>{text}</p>;
}

function matchWinner(projection: SafeProjection): string | undefined { return projection.matchWinner ? teamName(projection.room.teams?.[projection.matchWinner]) : undefined; }
function matchReason(projection: SafeProjection): string | undefined { return projection.matchWinReason === 'two_consecutive_round_wins' ? 'بجولتين متتاليتين' : projection.matchWinReason === 'three_total_round_wins' ? 'بثلاث جولات إجمالاً' : undefined; }

function ConnectionStatus({ value }: { value: string }) { return <p className={`connection connection--${value}`} role="status">{connectionLabel(value)}</p>; }
function BuzzWinnerBanner({ winner }: { winner: NonNullable<SafeProjection['buzzWinner']> }) { return <p className={`buzz-winner buzz-winner--${winner.team}`} role="status">{winner.method === 'host' ? 'اختيار المضيف:' : 'الأسرع:'} {winner.displayName} <AxisMark axis={winner.team} /></p>; }
function HostActions({ state, action, playerCount, teams, entitledTeam }: { state: string; action: (type: IntentType, payload?: Record<string, unknown>) => void; playerCount: number; teams: NonNullable<SafeProjection['room']['teams']>; entitledTeam?: 'horizontal' | 'vertical' }) {
  const manualSelection = state === 'QUESTION_READING' || state === 'OPPONENT_CHANCE'; const hostOnly = playerCount === 0; const primary: Partial<Record<string, [IntentType, string]>> = { ROUND_SETUP: ['ROUND_READY', 'جهّز الجولة'], LETTER_REVEAL: ['LETTER_REVEALED', 'اكشف الحرف'], ...(hostOnly ? {} : { QUESTION_READING: ['OPEN_QUESTION', 'افتح البازر'] }), CELL_AWARDED: ['AWARD_CELL', 'ثبّت الخلية (+1 نقطة)'], PATH_CHECK: ['CHECK_PATH', 'تحقق من المسار'], ROUND_COMPLETE: ['START_NEXT_ROUND', 'جولة جديدة'] };
  return <div className="control-grid">{state === 'FIRST_ANSWER' && <><button className="button button--primary" onClick={() => action('JUDGE_CORRECT')}>إجابة صحيحة</button><button className="button" onClick={() => action('JUDGE_INCORRECT')}>إجابة خاطئة</button><button className="button" onClick={() => action('JUDGE_CORRECT')}>قبول بديل</button></>}{state === 'QUESTION_FAILED' && <><button className="button button--primary" onClick={() => action('RETRY_CELL')}>أعد المحاولة</button><button className="button" onClick={() => action('RETURN_CELL')}>أعد الخلية</button></>}{primary[state] && <button className="button button--primary" onClick={() => action(primary[state]![0])}>{primary[state]![1]}</button>}{manualSelection && <section className="host-team-selection" aria-label="اختيار فريق الإجابة"><p>{hostOnly ? 'تحكم المضيف: اختر الفريق المجيب مباشرة.' : 'تجاوز يدوي للمضيف: اختر الفريق المجيب.'}</p><div>{(['horizontal', 'vertical'] as const).map((team) => <button aria-label={`اختيار ${teams[team]} للإجابة`} className={`button host-team-selection__button host-team-selection__button--${team}`} data-testid={`host-select-${team}`} disabled={state === 'OPPONENT_CHANCE' && entitledTeam !== team} key={team} onClick={() => action('HOST_SELECT_TEAM', { team })}><span>{teamName(teams[team])}</span><AxisMark axis={team} /></button>)}</div></section>}{state !== 'CORRECTION' && state !== 'MATCH_COMPLETE' && <button className="button button--quiet" onClick={() => action(state === 'PAUSED' ? 'RESUME' : 'PAUSE')}>{state === 'PAUSED' ? 'استئناف' : 'إيقاف مؤقت'}</button>}</div>;
}

export function RoomRoute({ surface }: { surface: 'lobby' | 'host' | 'play' | 'display' | 'results' }) {
  useDocumentTitle(surface === 'host' ? 'لوحة المضيف' : surface === 'play' ? 'بازر اللاعب' : surface === 'display' ? 'شاشة الجمهور' : surface === 'results' ? 'نتيجة المباراة' : 'ردهة المباراة');
  const { roomCode } = useParams(); const { envelope, connection, error, send, reportError } = useRoom(roomCode, surface === 'display', surface === 'display' ? 'audience' : undefined); const navigate = useNavigate(); const projection = envelope?.projection;
  const action = (type: IntentType, payload?: Record<string, unknown>) => void send(type, payload).catch((reason) => reportError(readableError(reason)));
  const reconcileDeadline = () => { if (gameRuntime.kind === 'firebase' && envelope) void gameRuntime.syncDeadline?.(envelope.roomId).catch((reason) => reportError(readableError(reason))); };
  const rematch = async () => { const settings = projection?.room.matchSettings; if (!settings) return; try { const body = await gameRuntime.createRoom({ displayName: 'المضيف', ...settings }); if (!body.roomId || !body.roomCode) throw new Error('تعذر إنشاء الإعادة.'); saveCapability(body.roomId, { token: body.token ?? '', role: 'host' }); sessionStorage.setItem(`huroof:code:${body.roomCode}`, body.roomId); navigate(`/room/${body.roomCode}/lobby`); } catch (reason) { reportError(readableError(reason)); } };
  if (!projection || !envelope) return <main className="app-page" id="main-content"><AppHeader /><section className="status-panel"><h1>{connection === 'offline' ? 'تعذر الاتصال بالغرفة' : 'جارٍ الاتصال بالغرفة…'}</h1><p>{error || 'نحافظ على مساحة الشاشة كي لا يقفز المحتوى.'}</p><Link className="button" to="/">العودة إلى الدخول</Link></section></main>;
  const role = envelope.role; const state = projection.room.state; const host = role === 'host'; const team = projection.self?.team; const cells = projection.board ?? previewBoardCells; const winner = matchWinner(projection); const victoryReason = matchReason(projection);
  const buzzText = connection === 'offline' ? 'انقطع الاتصال' : connection === 'reconnecting' ? 'جارٍ إعادة الاتصال' : projection.self?.canBuzz ? 'اضغط الآن' : state === 'FIRST_ANSWER' && projection.answeringTeam === team ? 'أنت الأسرع — أجب' : state === 'OPPONENT_CHANCE' ? 'الفرصة للفريق الآخر' : 'استعد';
  if (surface === 'lobby') return <main className="app-page" id="main-content"><AppHeader /><DemoBanner demo={projection.demo} /><section className="lobby-page"><div className="lobby-title"><p className="eyebrow">غرفة مباشرة · <bdi dir="ltr">{projection.room.roomCode}</bdi></p><h1>ردهة المباراة</h1><button className="copy-button" onClick={() => void navigator.clipboard?.writeText(projection.room.roomCode)}>انسخ الرمز</button></div><div className="lobby-rosters">{(['horizontal', 'vertical'] as const).map((axis) => <section className={`roster roster--${axis}`} key={axis}><h2><AxisMark axis={axis} /> {projection.room.teams?.[axis]}</h2>{projection.room.members?.filter((member) => member.team === axis).map((member) => <p className="member-row" key={`${axis}-${member.displayName}`}><span>{member.displayName}</span><span>{member.ready ? 'جاهز · تم اختبار البازر' : 'بانتظار الجاهزية'}</span></p>) || <p>لاعب بانتظار الانضمام</p>}</section>)}<section className="lobby-ready"><GameBoard cells={cells} className="motif-board" /><ConnectionStatus value={connection} /><strong>{projection.room.readyCount}/{projection.room.memberCount} جاهزون</strong></section></div>{role === 'player' && <button className="button button--primary" data-testid="ready" onClick={() => action('LOBBY_SET_READY', { ready: !projection.self?.ready })}>{projection.self?.ready ? 'إلغاء الجاهزية' : 'اختبر البازر وأنا جاهز'}</button>}{host && <div className="lobby-actions"><button className="button button--primary" data-testid="start-match" disabled={!projection.room.canStart} onClick={() => action('START_MATCH')}>ابدأ المباراة</button><p data-testid="start-blocked">{projection.room.startBlockedReason ? 'عند وجود لاعبين، يلزم لاعبان في فريقين وجاهزية الجميع قبل البدء.' : (projection.room.members?.some((member) => member.role === 'player') ? 'المباراة جاهزة بعد اكتمال الفريقين.' : 'وضع المضيف فقط جاهز: ستتحكم بالفريقين يدوياً.')}</p><Link className="button" to={`/room/${roomCode}/host`}>لوحة المضيف</Link><button className="button" onClick={() => navigate(`/room/${roomCode}/display`)}>شاشة الجمهور</button></div>}<p aria-live="polite" className="form-message">{error}</p></section></main>;
  if (surface === 'play') return <main className="player-page" id="main-content"><a className="skip-link" href="#main-content">تجاوز إلى المحتوى</a><DemoBanner demo={projection.demo} /><header><p>{teamName(projection.room.teams?.[team ?? 'horizontal'])} <AxisMark axis={team ?? 'horizontal'} /></p><ConnectionStatus value={connection} /></header><section><p className="eyebrow">{stateLabel(state)}</p><h1>{connection !== 'connected' ? buzzText : state === 'MATCH_COMPLETE' ? winner ? `اكتملت المباراة — فاز ${winner}` : 'اكتملت المباراة' : buzzText}</h1><button aria-label={buzzText} className="buzzer" data-state={connection === 'offline' ? 'offline' : connection === 'reconnecting' ? 'reconnecting' : projection.self?.canBuzz ? 'open' : projection.self?.isBuzzWinner ? 'first' : 'locked'} disabled={!projection.self?.canBuzz || connection !== 'connected'} onClick={() => action('BUZZ')}><span>{projection.self?.canBuzz ? <>اضغط<br />الآن</> : projection.self?.isBuzzWinner ? <>أنت<br />الأسرع</> : <>البازر<br />مغلق</>}</span></button><p aria-live={connection === 'offline' ? 'assertive' : 'polite'}>{projection.self?.isBuzzWinner ? 'أنت الأسرع — أجب الآن. قُفل البازر للجميع.' : projection.messageAr || (state === 'MATCH_COMPLETE' ? winner ? `المسار والمباراة اكتملَا لصالح ${winner}.` : 'اكتملت المباراة.' : projection.self?.canBuzz ? 'البازر مفتوح لفريقك.' : 'سيظهر هنا وضعك التالي.')}</p></section><Link to={`/room/${roomCode}/lobby`}>العودة إلى الردهة</Link></main>;
  if (surface === 'display') return <main className="stage" data-state={state} id="main-content"><a className="skip-link" href="#main-content">تجاوز إلى المحتوى</a><DemoBanner demo={projection.demo} />{state === 'FIRST_ANSWER' && projection.buzzWinner ? <BuzzWinnerBanner winner={projection.buzzWinner} /> : null}<section className="stage-game" aria-label="لوحة المباراة والنتيجة"><p className="stage-score__team stage-score__team--vertical"><AxisMark axis="vertical" /><span>{teamName(projection.room.teams?.vertical)}</span><strong>{projection.roundWins?.vertical ?? 0}</strong><small>جولات</small><span className="stage-score__points">نقاط الإجابات: {projection.questionScores?.vertical ?? 0}</span></p><div className="stage-board-column"><p className="stage-round">{stateLabel(state)}<small>الجولة {projection.currentRound ?? 0}</small></p><GameBoard activeCellId={projection.activeCellId} cells={cells} className="stage-board" winningPath={projection.winningPath} /></div><p className="stage-score__team stage-score__team--horizontal"><AxisMark axis="horizontal" /><span>{teamName(projection.room.teams?.horizontal)}</span><strong>{projection.roundWins?.horizontal ?? 0}</strong><small>جولات</small><span className="stage-score__points">نقاط الإجابات: {projection.questionScores?.horizontal ?? 0}</span></p></section><section className="question-band" aria-live="polite">{state === 'MATCH_COMPLETE' ? <><p>اكتمل المسار والمباراة</p><h1>{winner ? `فاز ${winner}` : 'انتهت المباراة'}</h1>{victoryReason && <p>{victoryReason}</p>}</> : state === 'CELL_SELECTION' ? <p>بانتظار اختيار الخلية</p> : <><p>{projection.question?.headerAr || 'سؤال الجولة'}</p><h1>{projection.question?.promptAr || projection.messageAr || 'بانتظار السؤال'}</h1><Countdown deadlineAt={projection.deadlineAt} onDeadline={reconcileDeadline} serverTime={envelope.serverTime} />{projection.question?.revealedAnswer && <p>الإجابة: {projection.question.revealedAnswer}</p>}</>}</section></main>;
  if (surface === 'results') return <main className="app-page" id="main-content"><AppHeader /><DemoBanner demo={projection.demo} /><section className="results-page"><div><p className="eyebrow">النتيجة النهائية</p><h1>{winner ? `الفائز: ${winner}` : projection.winningPath?.length ? 'اكتمل المسار الفائز' : 'نتيجة المباراة'}</h1><p>الجولات: {teamName(projection.room.teams?.horizontal)} {projection.roundWins?.horizontal ?? 0} — {projection.roundWins?.vertical ?? 0} {teamName(projection.room.teams?.vertical)}</p>{victoryReason && <p>سبب الفوز: {victoryReason}</p>}</div><GameBoard cells={cells} className="result-board" winningPath={projection.winningPath} /><div className="result-meta"><p>نقاط الإجابات: ↔ {projection.questionScores?.horizontal ?? 0} · ↕ {projection.questionScores?.vertical ?? 0}</p>{projection.roundResults?.length ? <p>تسلسل الجولات الفعّال: {projection.roundResults.map((result) => `${result.round}: ${result.winner === 'horizontal' ? '↔' : '↕'}`).join(' · ')}</p> : null}<ol><li>اختيار الخلية</li><li>إجابة صحيحة وتثبيت ملكيتها (+1 نقطة)</li><li>اكتمال المسار (+1 جولة)</li></ol><Link className="button button--primary" to="/host/new">مباراة جديدة</Link>{host && <button className="button" data-testid="rematch-same-settings" onClick={() => void rematch()}>إعادة بنفس الإعدادات</button>}</div><p aria-live="polite" className="form-message">{error}</p></section></main>;
  return <main className="app-page host-page" data-state={state} id="main-content"><AppHeader /><DemoBanner demo={projection.demo} /><h1 className="sr-only">لوحة المضيف</h1>{state === 'FIRST_ANSWER' && projection.buzzWinner ? <p className={`buzz-winner buzz-winner--${projection.buzzWinner.team}`} role="status">الأسرع: {projection.buzzWinner.displayName} <AxisMark axis={projection.buzzWinner.team} /></p> : null}<section className="host-layout"><div className="host-stage" aria-label="لوحة المباراة والنتيجة"><section className="host-score host-score--vertical" data-testid="host-score-vertical"><AxisMark axis="vertical" /><span className="host-score__team">{teamName(projection.room.teams?.vertical)}</span><strong data-testid="host-round-wins-vertical">{projection.roundWins?.vertical ?? 0}</strong><small>جولات</small><span className="host-score__points">نقاط الإجابات: {projection.questionScores?.vertical ?? 0}</span></section><div className="host-board"><p className="host-board__state">{stateLabel(state)} · <AxisMark axis={projection.entitledTeam ?? 'horizontal'} /></p><GameBoard activeCellId={projection.activeCellId} cells={cells} className="host-game-board" onSelect={(cellId) => action('SELECT_CELL', { cellId })} selectable={host && state === 'CELL_SELECTION'} winningPath={projection.winningPath} /></div><section className="host-score host-score--horizontal" data-testid="host-score-horizontal"><AxisMark axis="horizontal" /><span className="host-score__team">{teamName(projection.room.teams?.horizontal)}</span><strong data-testid="host-round-wins-horizontal">{projection.roundWins?.horizontal ?? 0}</strong><small>جولات</small><span className="host-score__points">نقاط الإجابات: {projection.questionScores?.horizontal ?? 0}</span></section></div><aside className="host-controls"><HostActions action={action} entitledTeam={projection.entitledTeam} playerCount={projection.room.members?.filter((member) => member.role === 'player').length ?? 0} state={state} teams={projection.room.teams ?? { horizontal: 'فريق ↔', vertical: 'فريق ↕' }} /><div className="host-controls__topline"><ConnectionStatus value={connection} /><p className="phase-label">{stateLabel(state)}</p><Countdown deadlineAt={projection.deadlineAt} onDeadline={reconcileDeadline} serverTime={envelope.serverTime} /></div>{projection.question && <section className="private-question"><p>{projection.question.headerAr}</p><h2>{projection.question.promptAr}</h2><div className="private-answer"><b>الإجابة الخاصة</b><p>{projection.question.primaryAnswer || 'تظهر للمضيف فقط عند وصول السؤال.'}</p><p>البدائل: {projection.question.acceptedAnswers?.join('، ') || '—'}</p></div><p className="source-note">المصدر: {projection.question.sources?.map((source) => source.title).join(' · ') || 'غير متاح'}</p></section>}{state === 'CORRECTION' ? <CorrectionReview correction={projection.correction} onCancel={() => action('CANCEL_CORRECTION')} onConfirm={() => action('CONFIRM_CORRECTION')} /> : <details className="correction"><summary>تصحيح وسجل التدقيق</summary><CorrectionForm cells={cells} onCorrect={(payload) => action('BEGIN_CORRECTION', payload)} />{projection.audit?.map((entry) => <p key={entry.revision}>{entry.revision} · {stateLabel(entry.type)}</p>)}</details>}<p aria-live="polite" className="form-message">{error}</p></aside></section></main>;
}

function CorrectionForm({ cells, onCorrect }: { cells: BoardCell[]; onCorrect: (value: Record<string, unknown>) => void }) { const [cellId, setCellId] = useState(''); const [owner, setOwner] = useState(''); const [reason, setReason] = useState(''); return <form className="correction-form" onSubmit={(event) => { event.preventDefault(); if (cellId && reason.trim()) onCorrect({ cellId, owner: owner || undefined, reason: reason.trim() }); }}><label>الخلية<select required value={cellId} onChange={(event) => setCellId(event.target.value)}><option value="">اختر خلية</option>{cells.map((cell) => <option key={cell.id} value={cell.id}>{cell.visibleValue} ({cell.id})</option>)}</select></label><label>المالك<select value={owner} onChange={(event) => setOwner(event.target.value)}><option value="">غير مملوكة</option><option value="horizontal">الفريق الأفقي</option><option value="vertical">الفريق العمودي</option></select></label><label>سبب التصحيح<textarea required value={reason} onChange={(event) => setReason(event.target.value)} /></label><button className="button" type="submit">معاينة التصحيح</button></form>; }
function CorrectionReview({ correction, onConfirm, onCancel }: { correction?: SafeProjection['correction']; onConfirm: () => void; onCancel: () => void }) { if (!correction) return <section className="correction"><h2>التصحيح قيد التحضير</h2><p>تُراجع التأثيرات التابعة قبل التأكيد.</p></section>; const owner = correction.owner === 'horizontal' ? 'الفريق الأفقي' : correction.owner === 'vertical' ? 'الفريق العمودي' : 'غير مملوكة'; const prior = correction.priorOwner === 'horizontal' ? 'الفريق الأفقي' : correction.priorOwner === 'vertical' ? 'الفريق العمودي' : 'غير مملوكة'; return <section className="correction" data-testid="correction-review"><h2>مراجعة التصحيح</h2><p>الخلية: {correction.cellId} · من {prior} إلى {owner}</p><p>السبب المطلوب: {correction.reason}</p><p>الأثر التابع: سيُعاد احتساب المسار الفائز ونتيجة الجولة قبل حفظ التصحيح.</p><div className="control-grid"><button className="button button--primary" onClick={onConfirm}>تأكيد التصحيح</button><button className="button" onClick={onCancel}>إلغاء التصحيح</button></div></section>; }

export function HowToPlayRoute() { useDocumentTitle('كيف تلعب'); return <main className="app-page" id="main-content"><AppHeader /><section className="rules-page"><div><p className="eyebrow">قواعد مختصرة</p><h1>كيف تلعب؟</h1><ol><li><b>اختر</b> خلية من اللوح.</li><li><b>اسأل</b> وافتح البازر.</li><li><b>التقط</b> الخلية بإجابة صحيحة.</li><li><b>صِل</b> بين طرفي محرك.</li></ol></div><div><GameBoard activeCellId="cell-2-2" cells={previewBoardCells.map((cell) => cell.id === 'cell-2-2' ? { ...cell, owner: 'horizontal' as const } : cell)} /><p><AxisMark axis="horizontal" /> يصل اليسار باليمين · <AxisMark axis="vertical" /> يصل الأعلى بالأسفل</p></div></section></main>; }

type Item = { id: string; targetLetter: string; headerAr: string; promptAr?: string; canonicalAnswer?: string; acceptedAnswers?: string[]; difficulty?: string; status: string; categoryId?: string; sourceUrl?: string; useCount?: number; objectionCount?: number; reviewError?: string; readOnly?: boolean };
export function QuestionsRoute() { useDocumentTitle('إدارة الأسئلة'); const { questionId } = useParams(); const location = useLocation(); const editorId = questionId ?? (location.pathname.endsWith('/new') ? 'new' : undefined); const [items, setItems] = useState<Item[]>([]); const [letterFilter, setLetterFilter] = useState(''); const [categoryFilter, setCategoryFilter] = useState('all'); const [difficulty, setDifficulty] = useState('all'); const [status, setStatus] = useState('all'); const [sourceState, setSourceState] = useState('all'); const [usage, setUsage] = useState('all'); const [objections, setObjections] = useState('all'); const [loadError, setLoadError] = useState(''); useEffect(() => { void fetch('/api/admin/questions').then((response) => response.ok ? response.json() as Promise<Item[]> : Promise.reject(new Error())).then(setItems).catch(() => setLoadError('تعذر تحميل المخزون المحلي. تحقق من خدمة الإدارة.')); }, []); const visible = useMemo(() => items.filter((item) => (!letterFilter || item.targetLetter === letterFilter) && (categoryFilter === 'all' || item.categoryId === categoryFilter) && (difficulty === 'all' || item.difficulty === difficulty) && (status === 'all' || item.status === status) && (sourceState === 'all' || (sourceState === 'complete' ? Boolean(item.sourceUrl) : !item.sourceUrl)) && (usage === 'all' || (usage === 'used' ? (item.useCount ?? 0) > 0 : (item.useCount ?? 0) === 0)) && (objections === 'all' || (objections === 'has' ? (item.objectionCount ?? 0) > 0 : (item.objectionCount ?? 0) === 0))), [categoryFilter, difficulty, items, letterFilter, objections, sourceState, status, usage]); return <main className="app-page" id="main-content"><AppHeader /><section className="admin-page"><header><p className="admin-note">إدارة محلية فقط؛ مصادقة الإنتاج خارج نطاق هذه الواجهة.</p><h1>{editorId ? 'محرر السؤال' : 'مخزون الأسئلة'}</h1></header>{editorId ? <QuestionEditor id={editorId} /> : <div className="inventory-layout"><aside className="filter-rail"><Link className="button button--primary" to="/questions/new">مسودة جديدة</Link><label>الحرف<input aria-label="الحرف" maxLength={1} placeholder="مثال: أ" value={letterFilter} onChange={(event) => setLetterFilter(event.target.value)} /></label><label>الفئة<select aria-label="الفئة" value={categoryFilter} onChange={(event) => setCategoryFilter(event.target.value)}><option value="all">كل الفئات</option>{categoryCatalog.map((category) => <option key={category.id} value={category.id}>{category.displayNameAr}</option>)}</select></label><label>الحالة<select aria-label="حالة المراجعة" value={status} onChange={(event) => setStatus(event.target.value)}><option value="all">كل الحالات</option><option value="draft">مسودة</option><option value="approved">معتمد</option><option value="rejected">مرفوض</option></select></label><label>الصعوبة<select aria-label="الصعوبة" value={difficulty} onChange={(event) => setDifficulty(event.target.value)}><option value="all">كل الصعوبات</option><option value="easy">سهل</option><option value="medium">متوسط</option><option value="hard">صعب</option><option value="سهل">سهل (قديم)</option><option value="متوسط">متوسط (قديم)</option><option value="صعب">صعب (قديم)</option></select></label><label>حالة المصدر<select aria-label="حالة المصدر" value={sourceState} onChange={(event) => setSourceState(event.target.value)}><option value="all">كل المصادر</option><option value="complete">مصدر مكتمل</option><option value="missing">مصدر ناقص</option></select></label><label>عدد الاستخدام<select aria-label="عدد الاستخدام" value={usage} onChange={(event) => setUsage(event.target.value)}><option value="all">كل الاستخدامات</option><option value="used">مستخدم</option><option value="unused">غير مستخدم</option></select></label><label>الاعتراضات<select aria-label="الاعتراضات" value={objections} onChange={(event) => setObjections(event.target.value)}><option value="all">كل الاعتراضات</option><option value="has">لديه اعتراضات</option><option value="none">بلا اعتراضات</option></select></label><p>{categoryCatalog.length} فئة مستوردة</p></aside><div className="inventory-table">{loadError ? <section className="empty-state" data-testid="admin-review-error"><h2>تعذر تحميل الأسئلة</h2><p>{loadError}</p></section> : visible.length === 0 ? <section className="empty-state"><h2>لا توجد أسئلة مطابقة</h2><p>غيّر عوامل التصفية أو أنشئ مسودة محلية للمراجعة.</p></section> : <><div className="table-head"><span>الحرف</span><span>العنوان</span><span>الحالة</span><span>الاستخدام / الاعتراضات</span></div>{visible.slice(0, 100).map((item) => <Link className="table-row" key={item.id} to={`/questions/${item.id}`}><b>{item.targetLetter}</b><span>{item.headerAr}</span><span>{item.status}</span><span>{item.useCount ?? 0} / {item.objectionCount ?? 0}</span></Link>)}</>}</div></div>}</section></main>; }
function QuestionEditor({ id }: { id: string }) { const [item, setItem] = useState<Item | undefined>(id === 'new' ? { id: 'new', targetLetter: '', headerAr: '', status: 'draft', difficulty: 'متوسط' } : undefined); const [note, setNote] = useState(''); const [saved, setSaved] = useState(''); const [loadError, setLoadError] = useState(''); useEffect(() => { if (id === 'new') return; void fetch(`/api/admin/questions/${encodeURIComponent(id)}`).then((response) => response.ok ? response.json() as Promise<Item> : Promise.reject(new Error())).then(setItem).catch(() => setLoadError('تعذر تحميل هذا السؤال للمراجعة.')); }, [id]); const readOnly = Boolean(item?.readOnly || item?.status === 'approved'); async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (readOnly) return; const values = new FormData(event.currentTarget); try { const response = await fetch('/api/admin/questions', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ id: id === 'new' ? undefined : id, promptAr: values.get('prompt'), canonicalAnswer: values.get('answer'), acceptedAnswers: String(values.get('alternatives') ?? '').split('،').filter(Boolean), targetLetter: values.get('letter'), sourceUrl: values.get('source'), versionNotes: note }) }); setSaved(response.ok ? 'حُفظت كمسودة محلية؛ لا تُنشر تلقائياً.' : 'تعذر حفظ المسودة.'); } catch { setSaved('تعذر حفظ المسودة.'); } } if (loadError) return <section className="empty-state" data-testid="admin-review-error"><h2>تعذر تحميل السؤال</h2><p>{loadError}</p></section>; if (!item) return <section className="empty-state"><p>جارٍ تحميل السؤال…</p></section>; return <form className="editor-layout" onSubmit={save}><section><p>السؤال: <bdi dir="ltr">{id === 'new' ? 'NEW' : id}</bdi></p>{item.reviewError && <p className="form-message" data-testid="review-error">خطأ مراجعة: {item.reviewError}</p>}<label>السؤال<input defaultValue={item.promptAr ?? item.headerAr} disabled={readOnly} name="prompt" required /></label><label>الإجابة الأساسية<input defaultValue={item.canonicalAnswer} disabled={readOnly} name="answer" required /></label><label>بدائل الإجابة<input defaultValue={item.acceptedAnswers?.join('، ')} disabled={readOnly} name="alternatives" /></label><label>الحرف المستهدف<input defaultValue={item.targetLetter} disabled={readOnly} maxLength={1} name="letter" required /></label><label>الفئة<select defaultValue={item.categoryId} disabled={readOnly} name="category">{featuredCategories.map((category) => <option key={category.id} value={category.id}>{category.displayNameAr}</option>)}</select></label><label>الصعوبة<select defaultValue={item.difficulty ?? 'متوسط'} disabled={readOnly} name="difficulty"><option>متوسط</option><option>سهل</option><option>صعب</option></select></label><label>المصدر / الرابط<input defaultValue={item.sourceUrl} disabled={readOnly} name="source" required type="url" /></label></section><aside><h2>المراجعة والدليل</h2>{readOnly ? <p data-testid="read-only-question">هذا السؤال معتمد للقراءة فقط؛ أنشئ مسودة محلية لتغييره.</p> : <><p>لا تنشر الأسئلة المولدة أو المسودات بلا مراجعة بشرية ومصدر مكتمل.</p><label>ملاحظات الإصدار<textarea value={note} onChange={(event) => setNote(event.target.value)} /></label><button className="button button--primary" type="submit">احفظ كمسودة محلية</button></>}<p aria-live="polite">{saved || 'المسودة غير منشورة.'}</p></aside></form>; }
export function NotFoundRoute() { useDocumentTitle('الصفحة غير موجودة'); return <main className="app-page" id="main-content"><AppHeader /><section className="status-panel"><p className="eyebrow">404</p><h1>الصفحة غير موجودة</h1><p>ربما تغيّر الرابط أو انتهت الجلسة.</p><Link className="button button--primary" to="/">العودة إلى الدخول</Link></section></main>; }
