import { useEffect, useState } from 'react';

type Item = { incomingId:string; categoryId:string; outcome:string; reason:string; storageState:string };
type Page = { runKind:string; after:Record<string,number>; outcomes:Record<string,number>; total:number; offset:number; limit:number; categories:string[]; items:Item[] };
type Detail = { incomingId:string; mode:string; question:string|null; instruction:string|null; answer:string; acceptedAnswers:string[]; letterModeEligible:boolean|null; stagingState?:string; storageState:string; currentStored?:{id:string;prompt?:string;answer?:string}; media?:{assetSha256:string;altAr:string} };
export const LocalImportReviewRoute=()=>{
 const [page,setPage]=useState<Page|null>(null),[state,setState]=useState<'loading'|'ready'|'error'>('loading'),[category,setCategory]=useState(''),[outcome,setOutcome]=useState(''),[query,setQuery]=useState(''),[offset,setOffset]=useState(0),[selected,setSelected]=useState<Detail|null>(null),[detailState,setDetailState]=useState<'idle'|'loading'|'error'>('idle');
 const load=()=>{setState('loading');const p=new URLSearchParams({limit:'40',offset:String(offset)});if(category)p.set('category',category);if(outcome)p.set('outcome',outcome);if(query)p.set('q',query);void fetch(`/api/local-import-review?${p}`,{cache:'no-store'}).then(async r=>{if(!r.ok)throw new Error('IMPORT_REVIEW_UNAVAILABLE');return r.json() as Promise<Page>;}).then(next=>{setPage(next);setState('ready');setSelected(null);}).catch(()=>setState('error'));};
 useEffect(()=>{load();},[category,outcome,query,offset]);
 const filters=(setter:(v:string)=>void)=>(value:string)=>{setOffset(0);setter(value);};
 const reveal=(id:string)=>{setDetailState('loading');void fetch(`/api/local-import-review/entries/${encodeURIComponent(id)}`,{cache:'no-store'}).then(async r=>{if(!r.ok)throw new Error('IMPORT_REVIEW_DETAIL_UNAVAILABLE');return r.json() as Promise<Detail>;}).then(next=>{setSelected(next);setDetailState('idle');}).catch(()=>setDetailState('error'));};
 if(state==='loading'&&!page)return <main className="local-import-review" dir="rtl"><p role="status">جارٍ تحميل سجل الاستيراد…</p></main>;
 if(state==='error')return <main className="local-import-review" dir="rtl"><section className="local-import-review__notice" role="alert"><p>لا يتوفر سجل الاستيراد المحلي أو لا يطابق هذا الطلب حماية السطح المحلي.</p><button className="button button--secondary" onClick={load} type="button">أعد المحاولة</button></section></main>;
 if(!page)return null;
 return <main className="local-import-review" dir="rtl"><p className="eyebrow">محلي فقط · مراجعة قراءة فقط</p><h1>مراجعة استيراد البنك المحفوظ</h1><p>يعرض هذا السطح سجل المصدر وقاعدة SQLite الحالية. لا يمنح موافقة ولا يضيف أسئلة إلى اللعب.</p><dl className="local-import-review__counts"><div><dt>المخزن الحالي</dt><dd>{page.after.stored??0}</dd></div><div><dt>المسودات</dt><dd>{page.after.drafts??0}</dd></div><div><dt>المعتمد</dt><dd>{page.after.approved??0}</dd></div><div><dt>المخزون المعتمد للعب</dt><dd>{page.after.normalApprovedGameplayPool??0}</dd></div></dl><p className="local-import-review__notice">نتائج المصدر: {Object.entries(page.outcomes).map(([n,c])=>`${n}: ${c}`).join(' · ')}. السجلات المرحّلة تحتاج مراجعة ولا تُعد قابلة للعب.</p><section className="local-import-review__filters" aria-label="تصفية سجل الاستيراد"><label>الفئة<select value={category} onChange={e=>filters(setCategory)(e.target.value)}><option value="">كل الفئات</option>{page.categories.map(id=><option key={id} value={id}>{id}</option>)}</select></label><label>النتيجة<select value={outcome} onChange={e=>filters(setOutcome)(e.target.value)}><option value="">كل النتائج</option>{Object.keys(page.outcomes).map(n=><option key={n} value={n}>{n}</option>)}</select></label><label>بحث<input aria-label="بحث في سجل الاستيراد" value={query} onChange={e=>filters(setQuery)(e.target.value)}/></label></section><p aria-live="polite">{page.total} سجل مصدر · نوع التشغيل: {page.runKind}</p>{page.items.length?<div className="local-import-review__table-wrap"><div className="local-import-review__table" role="table" aria-label="سجل استيراد محلي"><div role="row" className="local-import-review__row local-import-review__row--head"><b role="columnheader">المعرف</b><b role="columnheader">الفئة</b><b role="columnheader">النتيجة</b><b role="columnheader">الحفظ الحالي</b><b role="columnheader">تفاصيل</b></div>{page.items.map(i=><div role="row" className="local-import-review__row" key={i.incomingId}><span role="cell"><bdi dir="ltr">{i.incomingId}</bdi></span><span role="cell"><bdi dir="ltr">{i.categoryId}</bdi></span><span role="cell">{i.outcome}</span><span role="cell">{i.storageState}</span><span role="cell"><button className="button button--secondary" type="button" onClick={()=>reveal(i.incomingId)}>كشف المراجعة</button></span></div>)}</div></div>:<p className="local-import-review__notice">لا توجد سجلات ضمن هذه التصفية.</p>}<nav className="local-import-review__pager" aria-label="صفحات سجل الاستيراد"><button className="button button--secondary" disabled={page.offset===0} onClick={()=>setOffset(Math.max(0,page.offset-page.limit))} type="button">السابق</button><span>{page.offset+1}–{Math.min(page.offset+page.items.length,page.total)} من {page.total}</span><button className="button button--secondary" disabled={page.offset+page.limit>=page.total} onClick={()=>setOffset(page.offset+page.limit)} type="button">التالي</button></nav>{detailState==='loading'?<p role="status">جارٍ تحميل الإجابة المحفوظة…</p>:detailState==='error'?<p role="alert">تعذر تحميل تفاصيل السجل.</p>:selected?<section className="local-import-review__detail" aria-live="polite"><h2>تفاصيل مراجعة قراءة فقط</h2><p>{selected.stagingState?`سبب الترحيل: ${selected.stagingState}`:'سجل مصدر محفوظ للمراجعة.'}</p><p><b>الحفظ الحالي:</b> {selected.storageState}</p><p><b>النمط:</b> {selected.mode}</p>{selected.question?<p><b>السؤال الأصلي:</b> {selected.question}</p>:null}{selected.instruction?<p><b>تعليمات المؤدي الأصلية:</b> {selected.instruction}</p>:null}<p><b>إجابة المصدر المكشوفة:</b> {selected.answer}</p>{selected.currentStored?<p><b>السجل المحفوظ الحالي:</b> {selected.currentStored.prompt??'لا يوجد نص'} · {selected.currentStored.answer??'لا توجد إجابة'}</p>:null}<p><b>الإجابات المقبولة:</b> {selected.acceptedAnswers.join('، ')}</p><p><b>أهلية الحرف:</b> {selected.letterModeEligible===null?'غير معروفة ومحفوظة كما وردت':selected.letterModeEligible?'مذكورة في المصدر':'غير مؤهلة في المصدر'}</p>{selected.media?<img alt={selected.media.altAr} className="local-import-review__media" src={`/api/local-import-review/entries/${encodeURIComponent(selected.incomingId)}/media?sha=${selected.media.assetSha256}`}/>:null}</section>:null}</main>;
};