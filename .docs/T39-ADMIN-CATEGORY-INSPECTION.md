# T39 — Admin category inspection and correction (2026-09-24)

## Authority and verified baseline

The user requested category view/edit and a way to inspect every question in a category, including question/answer text and media, difficulty, and related information on `/admin/categories`. This is authorized implementation work, following the existing admin design and immutable-release architecture. The current list links only to a short category detail; published questions are in a separate filtered, paginated server query. Published category/question records are immutable. `adminCategoryMetadata` is a separate authoring collection, and production admin mutations are staged. No published question in the captured T38 release has a `difficulty` property; the UI must say that honestly. Existing media preview is verified and capped at 1 MB.

**Decision T39.D1:** Edits are correction drafts for review and a successor immutable release, never in-place writes to an active release. The user was asked whether edits should be immediate or reviewed; no answer arrived during the independent inspection milestone, so the reviewed model is the stated working assumption and can be adjusted if the user replies. The database review is complete. Do not label an `adminCategoryMetadata` save as a player-visible change.

**Visual contract:** Reuse the existing admin surfaces: calm Arabic cobalt/navy/ice, IBM Plex Arabic, density 5/10 desktop and 4/10 mobile. The category title and question count lead, followed by actions and a compact paginated list; detail opens a readable question/answer/media surface. Maintain 8/16/24/32 spacing, semantic status and focus states, existing `admin-*` components, and restrained motion. Avoid giant cards, raw release JSON, invented difficulty, and exposing private storage paths.

## Ordered work

### T39.1 — Published category → questions inspection

**Owner:** Forge implementation and checks; root acceptance. **State:** Done — root accepted 2026-09-24. **Dependencies:** existing admin routes and current published release read callables. **Scope:** `src/features/admin` routes/types/service/CSS, allowlisted published-question DTO if needed, focused admin route and callable tests. Add distinct “view category” and “view questions” actions from category rows; category detail links to its server-filtered question list and states whether it has zero questions. The list accepts a category deep link, resets pagination correctly, and preserves release/cursor authorization and stale-response guards. Question detail shows all *available, approved public-admin fields* through an explicit DTO: prompt, answers/aliases, media and answer media, modality, points or source metadata only if safe and useful, and difficulty as “not specified” when absent. Preserve the verified <=1 MB media preview boundary, and explain an unavailable large file precisely. Do not add arbitrary canonical fields or raw paths to the client.

**Acceptance:** A scoped admin can enter a category and page only its questions; zero-question categories have an honest empty state; every question opens its own read-only detail; answer media and video controls appear when the verified media is available; missing difficulty is explicit. Out-of-scope category/question/cursor requests remain denied. URL changes cannot show stale category data. Existing drafts and game routes regressions pass. **Checks:** focused UI and Functions tests, app/Functions typecheck and build, mobile/desktop rendered inspection where an authenticated fixture is available. **Rollback:** revert UI and allowlist extension; active release, assets and pointers are unchanged.

**Implementation evidence (2026-09-24):** Category rows now expose separate Arabic links for category detail and its release-bound question list. The URL-backed `categoryId`/modality filters reset correctly and ignore stale responses; category and question details retain the release ID. The read DTO permits prompt, answers, validated media metadata, finite points, and optional difficulty only; it does not project object paths, signed URLs, or other release internals. Category detail shows an existing browse-only derived topic label and clearly states the zero-question case. The question detail labels absent difficulty as not stored, offers question/answer media previews through the pre-existing verified 1 MB callable, and gives a specific oversize error. Verified: `npm run test:ui -- --run tests/app/admin-routes.test.tsx` (17 passed); `npm --prefix functions test` (63 passed, 3 documented fixture skips); `npm run typecheck:app`; `npm --prefix functions run build`; `npm run build`. No authenticated fixture was available for live desktop/mobile rendering; focused UI tests cover the view links, deep link, reset, stale response, zero state, and existing draft-route behavior.

### T39.2 — Category correction entry

**Resolved scope:** An administrator with `categories.write` can edit the proposed Arabic category name and an internal correction note as a **draft**, explicitly showing the published name beside it. The draft binds category ID, current release ID/root hash, baseline label, author, revision, operation ID, and status. Each save retains an immutable revision snapshot and audit record; stale revision, changed active release, wrong role/scope, and replay mismatch fail closed. This milestone implements draft saves only. Review and publication controls remain unavailable until a separate policy and frozen-revision workflow are defined; no direct published catalog write or game-read override is allowed. Production write enablement uses a dedicated correction-draft gate and session capability indicator; it never turns on global admin mutations. The shared draft is one record per category and base release, protected by optimistic revision checks. Only super_admin or a category-scoped content_admin may read its internal note or edit it. Publication to players remains the existing successor-release workflow and requires a fresh-base verified plan and separate activation action; the editing page must say this plainly.

**Database impact and rollback:** Additive `adminCategoryCorrections` records/revisions only; no migration, Firestore Rules change, or active release pointer change. If the draft feature fails, disable its specific callable gate and retain audit/history. The database review also identified a catalog-prefix audit gap in the general release adapter; a future category publication must fix and exercise that path before activation. No category change is published as part of this feature unless a separately reviewed successor release is actually prepared and verified.

**Owner:** Forge implementation and checks; root acceptance. **State:** Done — root accepted 2026-09-24 after Security and independent Gate review. **Scope:** Provide a clearly labelled correction form for proposed Arabic category title and internal correction note only, with revision/conflict handling, audit and role checks. Save as a release-bound draft; do not add review or publication controls in this milestone. Do not pretend the current `adminUpdateCategory` changes the published catalog or lift the global production mutation gate as a side effect. **Acceptance:** edits persist as identified drafts and round-trip under scoped write-capable access; the UI shows draft status and the unchanged published label, and active gameplay never reads the draft. **Checks:** revision/replay, transaction-level reauthorization and pointer-change races, dedicated-gate isolation from globally staged mutations, stored-category scope and internal-note privacy, database and security review. **Rollback:** disable correction entry; keep immutable release intact and retain audit records.

**Implementation evidence (2026-09-24):** `adminSaveCategoryCorrection` writes the additive `adminCategoryCorrections/{categoryId}/releases/{activeReleaseId}` draft only after a single transaction re-reads the active pointer, immutable release root, published category baseline, live principal role/scope, correction revision, and operation receipt. It creates immutable revision snapshots plus note-free operation/audit receipts; the request’s release ID must still equal the transaction-derived active release. The dedicated `ADMIN_CATEGORY_CORRECTION_DRAFTS_ENABLED` gate is independent from `ADMIN_MUTATIONS_ENABLED`; the emulator is enabled for focused tests. The category detail renders the published name, status/revision, proposed Arabic label, and a two-field writer form with successor-release copy; viewers receive no internal note or save control. Both browser fields are required with Arabic guidance. Pending saves are identity-guarded by category/release, and a dirty correction requires an explicit discard before category/question navigation. Verified: focused UI test (23 passed), Functions test (66 passed, 3 documented skips), app typecheck/build, Functions build, and focused Firebase emulator callable test covering create, immutable snapshot/audit, no published-release/catalog write, identical and altered replay, stale revision, role and scope revocation after principal preflight, and active-release change (1 passed). The emulator test uses the rate-limit transaction completion as the observable boundary between callable preflight and the correction transaction; it proves the transaction’s live-principal read rejects both role and scope races. No authenticated production browser session was available for rendered desktop/mobile inspection.

### T39.3 — Integration and production

**Owner:** root, with independent Gate for release. **State:** Production published and read back 2026-09-24; authenticated local desktop/mobile and media checks complete, authenticated production browser check pending. **Scope:** inspect integrated diff, run one load-bearing check, verify signed-in admin on desktop/mobile, release via scoped GitHub PR and Vercel/Functions deployment as applicable, and read back deployed version. **Acceptance:** reviewable PR, passing production checks, live admin route verified by an authorized account, and honest handover of any withheld edit or media functionality. **Rollback:** restore previous Vercel deployment and disable new admin mutation entry without changing active content.

## Boundary reviews

Security read-only review (2026-09-24): keep category scope and release-bound cursors; use an explicit DTO; maintain media binding/hash/MIME/size checks; keep corrections distinct from published records. Database review (2026-09-24) found no schema change needed for browsing and recommends append-only, release-bound correction drafts with immutable revision snapshots; do not use mutable category metadata as a public override. No live admin account was available in the observed browser session, so production browser acceptance is not yet evidenced.

Security post-implementation review passed the transaction-bound authority, separate gate, correction-note privacy and release boundary. Independent Gate passed the integrated technical release candidate: admin UI 23/23, Functions 66 passed with 3 documented fixture skips, focused Firebase emulator transaction 1/1, production build, packaging/privacy inspection and `git diff --check`. The emulator host ran Node 24 while Functions target Node 22; production deployment and authenticated desktop/mobile use are T39.3 checks, not covered by this technical verdict.

**Production readback (2026-09-24):** [PR #4](https://github.com/sanadii/huroof-wa-oloof/pull/4) merged as `4a6597bd3d2e9f6a742e2b08d111a72128d09ec8` after a passing Vercel check. The production alias points to Ready deployment `dpl_3VRbwmGSwSjTVfaokUDu9xBMaDoH`; `/admin/categories` returns HTTP 200, and its served `AdminRoutes-ePtu3ElY.js` contains the new category-question and correction UI. Targeted Firebase deployment created `adminGetCategoryCorrection` and `adminSaveCategoryCorrection`, and updated `adminGetPublishedQuestion` and `adminGetSession`; all four read back ACTIVE, gcfv2 Node 22, `me-central1`, with `ADMIN_CATEGORY_CORRECTION_DRAFTS_ENABLED=true` and `ADMIN_MUTATIONS_ENABLED` unset. The Firebase CLI returned exit 1 only after successful function operations because it could not configure an Artifact Registry cleanup policy; that policy was left unchanged. No active content release or catalog pointer was changed. Authenticated desktop/mobile use and media access still need a signed-in admin session; the observed browser had guest access only.

**Inline media follow-up (2026-09-24):** [PR #6](https://github.com/sanadii/huroof-wa-oloof/pull/6) merged as `be742e5ee5df67c1f5d179d6095cdd7d1f696ac9`. The question detail now places its verified image/video inside the question section and answer media inside the answer section. Images load automatically; clicking a video opens native inline controls without a download step. Media metadata remains in a small disclosure. The existing release-bound 1 MB media callable and its authorization/hash/MIME checks are unchanged. Root verified a real published image and paired goal-video card in an authenticated local Firebase session, including actual video playback and a narrow layout; focused admin tests passed 25/25, scoped lint and app build passed, and independent Gate found no blockers. Vercel deployment `dpl_6vN2SvagwHNNBCrpxAtyW23auWc6` is Ready and lists `https://huroof-wa-oloof.vercel.app` as an alias. The observed production browser tab is a guest session, so an authenticated production page inspection remains open.

## T39.4 — Category question inspection workflow (authorized 2026-09-24)

**Request and basis:** The user wants the published category name and image at the top of each question, a way to move through that category, and a reviewed check that immediately advances. `AdminPublishedQuestionsRoute` currently loads each detail independently; `publishedQuestionPage` uses ascending document-ID order, and the immutable release already has scoped question/category read callables. The existing editorial review/approval workflow applies to drafts and must remain separate. The approved T39 admin visual contract and public category-cover assets remain the visual authority.

**Decisions:** T39.D2: “Reviewed” is a shared operational inspection marker for one question in one immutable active release, not editorial approval, factual certification, or a gameplay eligibility change. T39.D3: create it once outside the release tree at `adminPublishedQuestionInspections/{releaseId}/questions/{questionId}`; retain the release root hash and question-content digest, stored category and question IDs, first reviewer UID and server time, and immutable audit/operation receipts. A successor release starts unchecked; no uncheck, bulk mark, notes, or carry-forward is implied. T39.D4: a distinct `questions.inspect` capability permits `super_admin` and category-scoped `content_admin`/`reviewer`; `viewer` and `game_ops` cannot write. Existing `game_ops` published-question read policy remains unchanged. A dedicated `ADMIN_PUBLISHED_QUESTION_INSPECTIONS_ENABLED` gate is independent of general admin mutations and category corrections. T39.D5: next/previous follow all questions in the stored category by ascending immutable document ID, never wrap or silently skip reviewed rows. Marking persists first, then moves to the known next question; at the last question it stays and states the end of the category. T39.D6: the category name comes from the scoped published release DTO, while the image uses only a publishable existing public cover keyed by the stored category ID or an explicitly labelled fallback; no private asset path reaches the browser.

**Owner and state:** Forge owns one implementation milestone end-to-end and records checks; root owns decisions and acceptance. State: Done — root accepted 2026-09-24 after integrated review, independent Gate PASS, and signed-in local desktop/narrow-layout readback; Database and Security read-only boundary reviews complete. Dependencies: T39.1–3, existing active release and approved `DESIGN-DNA.md`. Scope: `functions/src/admin/admin.ts` and exports, admin service/types/routes/CSS, focused Functions/UI/emulator/rules tests. Exclusions: mutations to `releases/`, editorial review records, game question content or scoring, new category imagery, global admin mutation enablement, and any unreviewed queue.

**Implementation sequence:** (1) Extend the published detail response with a scoped category label, inspected boolean/time, and category-local neighbor IDs; preserve the explicit DTO and release/category authorization. (2) Add a narrow create-once inspection callable. Its transaction re-reads active pointer/root, stored question/category, live principal roles/scopes/version, marker and operation receipt; mismatched operation replay fails, duplicate/concurrent inspection preserves the original marker, and an audit event records the outcome. (3) Add the category identity, artwork, next/previous controls, reviewed state and mark-and-advance action. Bind async completions to the current question/release, retain the operation ID on uncertain failure, disable duplicate action during save, and show saved-vs-navigation errors honestly. (4) Verify emulator index behavior before adding an index; deploy backend compatibility with the gate off before enabling the UI action.

**Acceptance:** Direct question links show the server category name and real public image or honest fallback; navigation remains inside that category across existing 50-row page boundaries and stops at first/last. An authorized click records one shared, release-bound inspection, advances exactly once after server success, and survives refresh/another authorized session. Viewer/gate-disabled states are read-only. Denial, expiry/release change, retry, duplicate click, concurrent marks, late responses, and media loading do not mark the wrong question or navigate incorrectly. Published content, editorial reviews and active pointer are unchanged. Desktop/mobile RTL, keyboard focus and existing inline image/video behavior remain usable.

**Required checks and rollback:** Focused admin UI and Functions tests; emulator tests for transaction reauthorization, identical/altered replay, concurrency, scope, stale release and neighbor boundaries; direct Firestore Rules denial; app/Functions builds, scoped lint and `git diff --check`; authenticated desktop/mobile inspection. Database/Security review before source changes; root reproduces a load-bearing check at acceptance. Roll back by disabling the dedicated gate and restoring the prior frontend if needed, while preserving marker/audit evidence. No destructive migration or release-pointer rollback.

**Boundary review resolution (2026-09-24):** Database review confirms additive out-of-release markers need no migration and must never affect immutable release auditing. Security requires content identity binding, live transactional reauthorization, exact operation replay, and a final principal/release/scope check after any private media download used by this view. Root chooses shared release-scoped review state, with original reviewer/time retained, because the requested category workflow asks whether a question *was reviewed* rather than asking each administrator to complete a personal checklist. No cross-release carry-forward. Cover lookup must use the stored category ID and publishable public assets only; label matching is not authority. This additive collection can be disabled through the narrow gate and retained for audit. Read-only roles see status but cannot mark; `game_ops` retains its existing no-published-read policy.

**Implementation evidence (2026-09-24):** `adminGetPublishedQuestion` now returns only the stored release category label, release-bound marker status/time, and adjacent IDs from the stored category’s document-ID order. `adminMarkPublishedQuestionInspected` is gated by `ADMIN_PUBLISHED_QUESTION_INSPECTIONS_ENABLED`, rechecks the active pointer/root, stored question/category, live principal role/scope/version, valid marker and exact operation receipt inside one transaction, and writes only additive marker/operation/audit records. A post-download live principal/release/scope check closes the admin media preview authorization window. The UI uses an ID-only allowlist of publishable public covers with an image-load fallback, bounded no-wrap navigation, an operational-review explanation, and identity/unmount/pending guards before mark-and-advance. Verified: Functions tests 69 passed with 3 documented skips; focused UI 28/28; focused Firebase emulator callable/Rules test 1/1 (concurrent marks, replay mismatch, neighbor boundaries, scope revocation, active-release change, direct-rule denial); app typecheck, Functions build, app build, scoped lint, and `git diff --check` passed. Authenticated desktop/mobile rendered inspection remains root acceptance work.

## T39.5 — Integrated release and production readback

**Owner and state:** root, with independent Gate for the production mutation boundary. State: production deployed and technical readback complete 2026-09-24; a real production mark/reload/advance remains unperformed to avoid falsely recording an unreviewed content card. Entry: T39.4 implementation complete, Database/Security concerns resolved, required checks passing. Deploy compatible Functions first with the inspection gate disabled; confirm index readiness and read paths, publish the frontend, then enable only the inspection gate. Verify the targeted Functions are ACTIVE and Vercel is Ready, inspect one authorized mark/reload/advance in production when a signed-in admin session is available, and confirm the active release root/content is unchanged. If production browser authorization is unavailable, report that precise acceptance gap; do not claim a live mark. Record exact PR/deployment/readback evidence here and in `HANDOVER.md`. Rollback: disable the gate for new marks, retain existing records, and restore the prior frontend while keeping backend compatibility.

**Root acceptance and production readback (2026-09-24):** Root reran the 28-test admin UI suite and independently inspected the signed-in local UI against the newly deployed production Functions. The actual `goals-2026` release category showed its published Arabic name, copied public cover, first/second question neighbor links in both directions, and native inline question-video controls. Narrow-layout DOM measurements showed the category grid collapsed to one column without document overflow; the in-app-browser screenshot itself was clipped by its panel, so this is not a human mobile visual sign-off. The cover came from the user's pre-existing untracked workspace file and was copied byte-identically into PR #8 (SHA-256 `a74f97cc5e3ef5b257f6d68f6edec662249d7195a1b1b4ac5d30f9e3170948f3`); no new image was generated. Independent Gate repeated Functions 69 pass/3 fixture skips, admin UI 28/28, emulator/Rules 1/1 and diff check, then issued PASS.

[PR #8](https://github.com/sanadii/huroof-wa-oloof/pull/8) merged as `95b06078371a6a537362a849406e7522d04b27ee`. Backend deployment first created `adminMarkPublishedQuestionInspected` and updated `adminGetPublishedQuestion`, `adminGetPublishedQuestionMedia`, and `adminGetSession` with the inspection gate absent; the signed-in page showed read-only staged status while its real category-neighbor query succeeded. Vercel production deployment `dpl_5SvtoytMWQS1WojhVerdQAf7DczB` became Ready on `https://huroof-wa-oloof.vercel.app`; the production category-cover asset returned HTTP 200, `image/webp`, 49,680 bytes, and `/admin/categories` returned HTTP 200. The dedicated gate was then set to true in the ignored production Functions environment file and only `adminGetSession` and `adminMarkPublishedQuestionInspected` were updated again. The signed-in local page then exposed **تسجيل الفحص والانتقال** for the same active release `t38b2-calculations-6a73e1c8757035264bf256adad3b146f`. No real published card was marked merely for deployment testing. The Firebase CLI reported successful targeted function operations in both deployments but exited 1 afterward because it could not configure an Artifact Registry cleanup policy; no cleanup policy was changed. `ADMIN_MUTATIONS_ENABLED` remains unset, and no active release pointer or published question was intentionally written. A production-origin authenticated mark/reload/advance and human mobile visual approval remain open. Rollback is to remove the narrow gate flag and redeploy the session/mark callables; existing markers and audit stay intact.

## T39.6 — Persistent published-question list and inspection dialog (authorized 2026-09-25)

**Request and basis:** Opening a question currently replaces the paginated list, forcing its categories and rows to load again on return. The list has no reviewed-status projection; category DTOs already provide `approvedCount`. The user requests a dialog, status icons and question counts. This continues T39.4–5 without changing the meaning of reviewed or the immutable release.

**Decisions:** T39.D7: list-row activation opens a URL-addressable dialog within the still-mounted list; the existing `/admin/questions/:id` route remains a standalone direct-link view. Browser Back closes and Forward reopens the dialog. Within an open dialog, next/previous and mark-and-advance update its question ID while preserving list filters, loaded rows, cursor and scroll. T39.D8: neighbors continue to traverse the entire stored category, including questions outside any list modality filter, per T39.D5. T39.D9: show a loaded-result count and a separately labelled published category total from inventory; never label the category total as the number matching a modality filter. T39.D10: dismissal during a pending mark does not cancel a submitted operation; stale completion must not reopen or navigate the dialog. Only server-confirmed marks update the row indicator.

**Owner and state:** Root owns scope, decisions and acceptance. One implementation milestone covers T39.6a–d; status: **Done — root accepted 2026-09-25** after independent Gate PASS and signed-in desktop/mobile readback. Existing T39.4/5 evidence remains complete with its production-origin mark and human mobile review still open. Dependencies: T39.4 inspection marker and its exact binding validator; current admin read API and approved `DESIGN-DNA.md`. No schema migration or active release change is needed.

| Task | Outcome and affected modules | Testable acceptance |
|---|---|---|
| T39.6a — Bounded status projection | Extend `publishedQuestionPage` in `functions/src/admin/admin.ts` and the typed summary in `src/features/admin/types.ts`; batch-read only markers for authorized returned page documents and reuse release-root, question-digest and category validation. | Only reviewed boolean/time is exposed. Missing marker is unchecked; invalid binding or read failure is not silently treated as unchecked. Scope and cursor rules remain. No reviewer UID, receipt, digest, answer or private media path appears in summaries. |
| T39.6b — Persistent dialog | Reuse detail/media UI in `src/features/admin/AdminRoutes.tsx` and `admin.css`; keep list mounted for question selection, provide a labelled native dialog, Escape/close, focus return, direct-detail fallback and URL history. | Row activation, Back/Forward, previous/next, refresh, loading/error/retry, keyboard and focus work. Loaded rows, cursor, filters and scroll survive dialog navigation. Stale detail responses never display the wrong question. |
| T39.6c — Icons and counts | Add labelled status indicators and loaded/published totals to the list; update a row after confirmed inspection. | Reviewed, unchecked and unavailable states are distinguishable without color; category total is explicit even with a modality filter; no invented exact filtered total. Failed/uncertain marks do not display confirmation. |
| T39.6d — Integrated UX checks | Focused UI/Functions/emulator checks and signed-in desktop/mobile review. | Question/answer media remain inline in their own sections; compact RTL list/dialog has no horizontal overflow; background is inert while modal; previous/next retain T39.D5; ordinary admin routes regressions pass. Record evidence and any limits here. |

**Boundary, checks and rollback:** Security reviews the summary allowlist and marker validation before implementation; database review covers bounded batch reads, cost and release compatibility. Required checks: focused UI and Functions tests, scoped emulator/Rules coverage for marker validity and scope, app/Functions typecheck and build, scoped lint and diff check, then rendered desktop/mobile dialog and scroll readback. The list callable remains read-only and additive. Rollback restores the prior frontend and may retain the compatible backend summary field; preserve inspection markers and audit evidence. Excluded: new schema, rules, category artwork, bulk review, editorial approval, gameplay changes, a reviewed-total aggregate or production content mutation.

**Implementation evidence (2026-09-25):** `adminListPublishedQuestions` now binds each returned question summary to its marker using the existing immutable release root, stored category, and full detail digest. It calls `db.getAll` only for `result.docs.slice(0, input.limit)`, maps snapshots by question ID, and fails the page for a missing/bad batch result, invalid binding, or malformed non-`Timestamp`/non-null review time. The summary carries only `inspection.reviewed` and `inspection.reviewedAt`; it carries no actor, receipt, digest, answer, or private-media data. The list keeps its loaded rows, cursor and filters mounted while a native URL-addressable `questionId` dialog loads detail; direct `/admin/questions/:id` remains standalone. An in-app click creates one transient dialog history entry, dialog next/previous and mark-and-advance replace it, and close returns to the prior list; direct query links close by removing `questionId`. The native dialog has a stable accessible name during loading, failure, retry, and loaded detail. Dialog errors are visible with a retry for the same requested identity. Explicit close/Escape owns route dismissal; native cleanup only closes the element, so StrictMode effect replay cannot dismiss the URL state. Dialog close/Escape restores the trigger focus, detail/media unmount on close, and request token plus requested release/question identity guards prevent stale detail or media DOM from rendering. A pending mark cannot reopen or navigate a dismissed dialog; confirmation alone changes the row icon. The category view distinguishes loaded rows from the existing full published `approvedCount`, including when a modality filter is selected; reviewed, unchecked, and old-backend-unavailable states have Arabic text plus symbols. At the mobile breakpoint the dialog close control leaves its desktop sticky float and occupies its own row above the category hero.

**Security/database review findings:** Security conditions are implemented: marker timestamps validate fail-closed, list projections retain the allowlist, and media/detail unmount with the dialog. The direct marker-read denial case passed `npm run test:firestore-rules` at 07:11 UTC using Android Studio JBR after the default host Java lookup failed. Database review is GO for the implementation: one bounded `getAll` adds at most 50 marker reads for the UI page size, no marker read for the cursor sentinel, and no schema, Rules, index, or migration change.

**Checks:** Passed `npm run test:ui -- --run tests/app/admin-routes.test.tsx` (39, including actual-click next/close/Back/Forward, native Escape/cancel focus restoration, StrictMode actual-click persistence with a simulated asynchronous native close event, stale-detail identity, failed-load retry, and named loading/error dialog cases); `npm --prefix functions test` (70 passed, 3 pre-existing/documented skips); `npm run typecheck:app`; `npm run build`; scoped `npx eslint src/features/admin/AdminRoutes.tsx src/features/admin/types.ts tests/app/admin-routes.test.tsx tests/firestore-rules.test.ts`; `git diff --check`; and `npm run test:firestore-rules` (root-run PASS at 07:11 UTC using Android Studio JBR). Independent Gate re-review passed focused UI 39/39, app typecheck and diff check. Root signed into local Chrome with the user-specified admin account and verified that the native dialog stays open, next question retains 51 table rows and `scrollY=252`, close returns focus, Forward reopens, and Escape closes. At 390×844, the final screenshot shows the close control above the title; measured close bottom 65 px and title top 81 px, dialog frame `scrollWidth=clientWidth=342`, and document scroll width 375 px below the 390 px viewport. Mobile close retains all 51 rows. The still-deployed callable omits list inspection status, so local rows correctly show the explicit unavailable state until T39.7 deploys the compatible backend.

## T39.7 — Integrated release and production readback

**Owner and state:** Root; authorized after T39.6 acceptance and an independent release Gate. Status: Pending upstream T40.5 release dependency, backend/client deployment and production readback. Implementation `de90db9a` was pushed to [PR #12](https://github.com/sanadii/huroof-wa-oloof/pull/12), stacked on PR #11; no production deploy or database write was made. Standard Admin SDK ADC remains absent (no credential env path, standard ADC file or gcloud executable found at 2026-09-25 local preflight). The prior automatic approval review rejected adapting the Firebase CLI refresh token into an ADC file, so the reviewed T40.5 create-only sidecar write remains pending a standard authorized ADC and fresh pointer/root check. Deploy the compatible list callable before the client, verify targeted Functions ACTIVE and Vercel Ready, then read a signed-in production page without manufacturing a review mark. Preserve the older open production-origin mark and human mobile acceptance items unless genuinely completed. Record deployment identities, live count/status readback and rollback here and in `HANDOVER.md`. If the backend summary causes a problem, retain immutable markers and restore the prior client or callable projection; do not change the active release or inspection gate solely for this UI rollback.
