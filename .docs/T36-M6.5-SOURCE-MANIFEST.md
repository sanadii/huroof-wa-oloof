# T36 M6.5 source manifest

Base: `origin/codex/app-update-2026-09-07` at `e854df41`.

## Allowlist

The staged diff is the authority. It is limited to:

- `src/features/game/**`, required game UI/route/adapter/header/theme files,
  and the public category-topic import closure;
- `functions/src/**` authority, permit, map-commitment, and test sources;
- `server/**` challenge definition/runtime integration;
- `firestore.rules`, `firebase.json`, and synthetic tests;
- these two public release notes.

## Snapshot parity

All reviewed client source and public assets are included so the older GitHub
base cannot roll back current production behavior. The one source difference
is `src/features/game/runtime/challenge-question-selector.ts`: it carries the
accepted post-snapshot selection repair. Its compiled runtime hash is checked
against the accepted functions package during release verification.

The release keeps the full-checkout Vercel exclusion policy, rather than the
snapshot's reduced-package ignore file. This prevents source, tests, content,
and documentation from becoming Vercel upload inputs.

## Public-data boundary

Excluded paths include `content/`, `output/`, `public/assets/questions/`, the
source-derived M4/M5 fixtures, and all private definition or media inputs.
The bundle scan must find no canonical grading keys or source-pin payloads.
