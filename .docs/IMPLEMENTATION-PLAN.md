# Implementation plan — current state

## T-14.5 follow-up — approved production room setup (2026-09-11)

User authorizes live rooms/gameplay and all-question review. This executes existing T-14.4–6 without claiming missing review/billing prerequisites complete. Database review and fresh preflight identify a demo-only frontend gate and absent release-bound catalog discovery. Production remains the 88-category static preview until full release acceptance.

- Owner: Forge implementation; root integration/status; Gate security/release review. Scope: authenticated/App-Check read-only release readiness/catalog callable, matching Firebase adapter contract and non-demo HostNewRoute path. Preserve local/fixture and static-preview behavior. No public Firestore release reads, approval writes, schema/rules weakening or question/media discovery payload.
- Sequence: inspect release schema/runtime validation; implement minimal metadata projection from the active immutable approved release; wire loading/error/empty states and Firebase demo:false creation; cover absent release and stale discovery; keep final createRoom transaction authoritative. Reuse existing visual layout and status controls.
- Acceptance: Firebase host selects approved release categories and submits demo:false when ready; absent/invalid/demo release prevents creation with actionable status; discovery excludes answers/question bodies/sources/media paths; local draft runtime and 88-category preview remain intact. Catalog visibility is not approval.
- Checks: focused Functions/helper and adapter/UI tests, app/Functions typecheck/build, preview regression, Gate payload/auth review; emulator check where available. Production additionally requires billing, actual reviewed release, AppCheck configuration and real multi-client smoke before runtime switch.
- Rollback: revert only this delta or retain static preview; no schema/data migration or room/history deletion. Dependencies: T-14.4 authentic review/immutable release and T-14.5 infrastructure before activation. Status: source implementation Done — root accepted after final Gate PASS; live activation and all-question approval remain blocked on the documented external prerequisites. Evidence: output/live-game-release-20260911/IMPLEMENTATION-EVIDENCE.md and gate/FINAL-GATE-REPORT.md.


## Design integration follow-up — 2026-09-11

Scope: the approved C rollout (0006/0007), amended by the September 9 **تحدي الخلية** identity and category-only background, plus the user's September 11 homepage refinement and board-choice placement request. This supplements completed T-01–07; it does not reopen production or content-import milestones.

Owner: root for acceptance and records; Forge for the implemented home/setup changes. Dependencies: binding `DESIGN-DNA.md`, existing game setup behavior and real category catalogue. Preserve historical receipts and all unrelated working-tree edits.

- [x] Keep the Arabic text wordmark, approved shared artwork, semantic palette and actual content.
- [x] Refine home hierarchy around create/join, readable foreground panels and responsive artwork placement.
- [x] Place descriptive board-type cards at the start of game creation; home CTA routes to `/host/new`.
- [x] Preserve RTL keyboard selection and existing mode/category setup behavior.
- [x] Verify 1440/390/320 layouts, focused tests, typecheck, lint, visual scanner and build. Entry/setup tests re-run September 11: 29/29 pass; existing category-cover React act warning remains.
- [x] Human visual acceptance recorded: after the rendered review and explicit approval question, the user replied “OK continue, do it all” on September 11. This approves the presented integration and authorizes remaining design work.
- [x] Complete the bounded loading/chunk-error presentation follow-up using the existing approved spatial panels; preserve retry behavior and accessible status announcements. Forge implemented App.tsx and focused verification; root reviewed the diff, desktop loading and mobile error renders.

Evidence: [implementation and rendered review](../output/homepage-refinement-20260911.md), [mobile setup](../output/playwright/board-kind-placement-setup-390.png), [desktop home](../output/playwright/board-kind-placement-home-1440.png). Changed implementation files: `HomeSurface.tsx`, `GameRoutes.tsx`, homepage/setup CSS and two focused route test files. No standalone pictorial logo has been accepted; current identity is the Arabic text wordmark. The broad phrase “logo and all” is not evidence of acceptance of an additional mark.

Status: Done — root accepted September 11 after human approval and final bounded verification. Loading/error panels now reuse the spatial design; auth status uses plain Arabic and the home title reads الرئيسية. Route-status/auth/entry tests: 15/15; typecheck, touched-file lint, visual scanner and local build passed. Initial auth test failed because it asserted replaced copy; the assertion was updated and passed. Existing build chunk warning remains. Browser fault injection on the real GameRoutes chunk produced the error panel, and retry recovered to setup with the board-kind selector. No overflow at 1440/390/320. Evidence: [desktop loading](../output/playwright/route-fallback-loading-1440.png), [mobile actual chunk failure](../output/playwright/route-fallback-natural-error-390.png). Root also verified rebuilt homepage title and login copy live. Rollback, if needed, is limited to this follow-up's source hunks; never discard the mixed working tree. Production M-6 and later data/release tasks remain separate.

## T-09 — QR joining and host player moves (2026-09-08)

### Inline lobby QR follow-up — September 11

Completion: **Done — root accepted** after 20/20 host-lobby tests, app typecheck, lint, visual scanner and local build. The rendered public-origin QR decoded to the exact sample player-room URL at 1440/390/320 without overflow. Root reviewed source and mobile capture. Shared URL-keyed state prevents stale images; explicit errors replace loading on failure. [Final handover and screenshot](HANDOVER.md#inline-lobby-qr--september-11). The following intake text is historical; its in-progress status is superseded by this completion record. Public deployment was not performed.

User request: display the actual scannable QR image in the lobby instead of the text-only QR trigger. User clarified that players join through the deployed public website. Owner: Forge for GameRoutes, scoped CSS and focused tests; root for review. Reuse locally generated QR encoding of the validated player join URL. Share origin/payload state between inline image and enlarged dialog, retain loopback/configuration guidance, and preserve existing host controls and game state. Acceptance: visible decodable image for a valid public origin; room-only player URL without host credentials; working enlargement and origin synchronization; loading/error states; no overflow at 320/mobile/desktop. Verify focused tests, app typecheck, lint, build and actual QR decode. No production deployment or mutation of the user's current room is included. Status: implementation in progress.

**Milestone:** M-8. **Owner:** Forge; root acceptance with security-boundary review and Gate. **Status:** Done — root accepted 2026-09-08. **Authorization:** user requested QR joining and drag/drop into team boxes, explicitly including during a match.

**Scope and evidence:** Extend T-08's host-only assignment and existing join route. Current local and Firebase assignment guards accept only LOBBY. Reuse member IDs only from the host projection; never identify players by name. No schema or role-grant changes. Existing DESIGN-DNA and recent rounded outlined host controls remain binding.

**Decisions:** Permit team moves in LOBBY, ROUND_SETUP, CELL_SELECTION, QUESTION_FAILED, ROUND_COMPLETE. Active answer/buzzer states, PAUSED, CORRECTION and completed matches remain locked, with a visible explanation. Preserve earned points, board ownership, question state and historical winner snapshots; lobby cross-team moves clear readiness as before. Live moves do not rewrite historical results. Local and Firebase must enforce the same rules with existing revision and actor checks. QR encodes only a player join link and is generated locally; loopback must not be presented as phone-accessible. Provide configured/LAN origin support without exporting host credentials.

**Sequence/modules:** (1) QR modal and safe join URL in host UI; (2) accessible drag/drop plus existing explicit assignment fallback and connection/pending/error handling; (3) matching local/Firebase guards and target-member persistence; (4) focused local/Functions/UI tests and browser verification; (5) independent Gate and root acceptance. Affected modules: GameRoutes, shared host CSS, runtime contracts/helpers as needed, server/service, functions/game and focused tests; QR dependency only if no existing generator is available.

**Acceptance/checklist:**
- [x] QR button opens/closes accessible panel with scannable player URL, copy link, and truthful local-network guidance.
- [x] Host moves identified players between red/green boxes with drag/drop and accessible keyboard/touch alternative; pending/failure/stale responses stay authoritative.
- [x] Allowed live states succeed; answer-window, paused/correction, non-host, invalid target and stale-revision moves fail in local/Firebase checks.
- [x] Scores, board, timer and historical winner remain unchanged; only target team/readiness as specified changes.
- [x] UI tests, scoped local/Functions tests, app typecheck, build, root browser verification, and independent Gate boundary review pass.

**Risk/rollback:** wrong-team live eligibility or host credentials leaking in QR. Avoid moves during active questions and build URL from public join route only. Roll back this task's source edits without resetting rooms or existing work; already assigned teams remain valid. No production deploy or firewall changes authorized by this implementation.

**Root acceptance (2026-09-08):** Gate passed real non-loopback HTTP admin-path isolation, local/Firebase safe-state and history/readiness matrix, actual intercepted Firebase join transactions (capacity/closed/removed-member checks), and secure UUID fallback. Root verified generated LAN link prefilled the room, two independent browser players joined, readiness succeeded over plain LAN HTTP after UUID repair, host moved a player in lobby and live ROUND_SETUP, scores stayed unchanged, and QUESTION_READING disabled movement with explanation. QR modal rendered with RTL and loaded image. Final integrated app build passed (existing bundle-size advisory). UI now 8 passing, local29 and Functions26. [Browser evidence](../output/t09-gate/root-browser.md), [Gate artifacts](../output/t09-gate/), [usage](../output/t09-gate/usage.md). Native phone camera scanning and external-device firewall reachability were not physically tested; no firewall change or production deployment. Separate T-10 presentation work remains independently owned.

**Implementation evidence (2026-09-08):** Added a locally generated QR dialog that encodes only `/?room=<code>` for the existing player entry form. It rejects loopback, credentialed, path-bearing, and malformed origins; the host can enter a public/LAN origin and copy the same player-only link. Host player chips are draggable in lobby teams and in the existing live score cards, with a visible keyboard/touch assignment control and no optimistic projection changes. Local and Firebase now accept assignments and new player joins only in `LOBBY`, `ROUND_SETUP`, `CELL_SELECTION`, `QUESTION_FAILED`, and `ROUND_COMPLETE`; live moves preserve readiness, board, timer, scores, question state, and historical buzz/round outcomes, while lobby moves still clear changed-team readiness. The local authority is loopback-bound by default; opt-in LAN Vite uses a direct-socket, normalized `/api/admin` guard before proxying, with loopback-only backend enforcement. The app timer uses a browser `number` handle so Node ambient timer overloads do not conflict with `window.setTimeout`. Verified: `npx vitest run tests/app/host-lobby.test.tsx` (8 passing); `npm run test:game` (29 passing); `npm --prefix functions test` (26 passing); `npm run typecheck:app`; `npm run build` (passed; existing Rollup >500 kB advisory); and `git diff --check`. Added regression coverage for safe/locked local and Firebase reducer moves, QR origin selection, and normalized LAN admin paths (including repeated slash/dot segments and spoof-resistant socket-only policy). Root must perform browser/LAN verification and Gate review before marking Done.

**Inspection-driven scope clarification:** Firebase new-player joining currently accepts lobby only; extend to the same five safe states while preserving capacity, membership and closed-room checks. Before exposing Vite on LAN, close its proxy bypass of local-only admin routes: normalize admin path segments consistently at backend and pre-proxy middleware, enforce actual socket loopback rather than forwarded headers, and bind backend to loopback by default. Security inspection found repeated-slash backend prefix bypass as well. These bounded fixes are necessary for phone QR access; verify rejected LAN admin variants on isolated data and working room join/WebSocket traffic. No firewall or production change implied.

## T-08 — Host lobby management (2026-09-07)

**Milestone:** M-7. **Owner:** Forge; root owns acceptance. **Authorization:** user request to show player counts/names, assign teams, and start directly from host view. **Status:** Done — root accepted 2026-09-07.

Current evidence: the lobby route already exposes START_MATCH and player readiness. The host route shows empty scores and moderation controls in LOBBY. Local/Firebase members already store uid/team/ready, but public roster summaries omit identifiers and no assignment intent exists. Graphify and actual runtime/projector source were inspected. This dated task extends existing host operations; it does not change role grants or reopen production milestone M-6.

### Decisions and scope

- Add LOBBY_ASSIGN_TEAM with exact `{ memberUid, team }` payload, validated host actor, player target, valid team, and actual LOBBY state. Paused is not LOBBY.
- Moving a player clears that player's readiness; same-team assignment preserves readiness. Existing revisions, replay receipts, and all-ready/two-team or zero-player start rules remain authoritative.
- Counts are joined players (active membership where recorded), not live socket presence. Exclude host/audience. Names may duplicate: identify targets by uid, never names or list positions.
- Expose optional roster uid only to authenticated host projections. Public player/audience summaries retain their existing fields; old summaries without IDs remain readable with assignment unavailable.
- Fixture remains explicitly non-authoritative: do not invent multiplayer transitions or enable unsupported controls.
- No new database fields/migration, security-rule change, question-content change, presence tracking, production write, or deployment. Existing team/ready fields remain backward compatible.

### Sequence and affected modules

1. Extend runtime contracts and implement local intent validation/mutation in `src/features/game/runtime/contracts.ts` and `server/service.ts`. Add the missing local ready-state LOBBY guard for parity.
2. Mirror reducer/validation/projection behavior in `functions/src/game.ts`. Return a separate target-member update; `functions/src/index.ts` must atomically persist that existing member document and rebuild projected members without overwriting the host actor or event identity.
3. Add host LOBBY roster panels in `src/routes/GameRoutes.tsx` / `src/styles/global.css`, reusing existing score-panel locations, team labels/colors, readiness/start gating, pending/error handling. Restore normal scoreboard/actions after start.
4. Run targeted runtime, projection, UI, and integration checks; record evidence below. Root reviews integrated behavior and accepts.

### Visual contract

Reuse DESIGN-DNA and approved 0007 C spatial studio. Lobby scan order: team panels with joined-player counts/names, assignment/readiness, then one prominent Start Match action and blocked reason. Board remains primary gameplay content after start. Density host5; desktop existing two team slots, mobile stacked/compact with no overflow320px+. IBM Plex Arabic600 actions/headings and400–500 names; semantic crimson/emerald team treatment, opaque arena/spatial panels and paired readable ink. Existing4px spacing,12px controls, restrained panel radii. At least44px select/buttons. No new imagery, decorative metrics, constant motion, geometry changes, fake players, or public private-answer leakage. Preserve reduced motion, keyboard focus, disabled/busy/error states. Assignments only in actual LOBBY; no pause/correction controls there.

### Acceptance and required checks

- [x] Host lobby shows truthful total/per-team counts, player names/readiness, empty states, and independently addressable duplicate names.
- [x] Host assignment succeeds; target player projection updates; changed team clears ready, same team preserves it. Reject nonhost, invalid shape/team/target, host/audience/inactive targets, non-lobby/paused, stale revisions. Replay does not reapply changes.
- [x] Only host receives roster IDs; player/audience answers and identifiers remain protected.
- [x] Start works on host route with existing readiness/connection rules; pending prevents duplicate actions; explanation remains visible when blocked.
- [x] Local multiplayer assignment → readiness → start verified. Firebase transaction persistence verified in emulator integration, not only reducer tests.
- [x] Focused game/UI/Functions tests, Functions build, typecheck, scoped lint, app build, responsive visual evidence. Root reviewed final local multi-client flow and screenshots at 320, 390, and 1440px.

**Risk/rollback:** main risk is Firebase accidentally persisting the target update over the actor. Use separate update identity and transaction/projection tests. Roll back only task source edits; existing reassigned team/readiness data remains valid. Preserve all unrelated changes and existing rooms. No data reset.

**Implementation evidence (2026-09-07):** Added host-only `LOBBY_ASSIGN_TEAM` to local and Firebase runtimes. Firebase returns and transactionally persists a separate target member before rebuilding projections; host roster summaries alone contain member UIDs. Local joins now broadcast their changed projection to already-connected hosts. The host LOBBY route keeps the existing board and replaces its controls with connection-aware roster panels, assignment selects, unassigned-player handling, blocked-start copy, and `host-lobby-team-horizontal`, `host-lobby-team-vertical`, `host-lobby-member-count`, and `start-match` test hooks. The synthetic `fixture-room` remains unavailable; Vite defaults an otherwise unconfigured development server to the local authority while preserving explicit environment and build selections. Verified: `npm run test:game` (27 passing, including an existing-host websocket join-broadcast assertion), `npm --prefix functions test` (23 passing), `npx vitest run tests/app/host-lobby.test.tsx tests/app/setup-route.test.tsx` (16 passing), `npm run typecheck:app`, scoped ESLint including `vite.config.ts`, `npm --prefix functions run build`, `npm run build`, and `npm run test:e2e:firebase` (Firebase emulator E2E passing, including assignment → readiness → host-route start and projection privacy). The app build retained the existing Rollup >500 kB advisory. Root acceptance: immediate joins, duplicate-name assignment, target readiness reset, and start on the same host route passed in the normal local dev server; screenshots preserve the board and readable controls without overflow. Evidence: output/playwright/host-lobby/local-default-results.txt and local-{320,390,1440}.png. Final app build passed. Separate typecheck:game still reports pre-existing number/Timeout errors in unchanged src/features/ui/QuestionReveal.tsx:75,81; app typecheck passes. Local .env.local now selects local runtime. Forge may update only this task's checklist/evidence/blockers and mark Implementation complete — ready for review. Root marks Done after acceptance.

| ID | Work item | Owner | Dependencies | Acceptance | State |
| --- | --- | --- | --- | --- | --- |
| T-01 | Preserve history and collect design evidence | Forge / root | Immutable 0001–0007 | Receipts unchanged; C selection and expansion authority linked. | Complete |
| T-02 | Homepage/create/join representative implementation | Forge | 0006 selection | Real create/join/category controls; human live homepage review completed. | Complete |
| T-03 | Shared C public shell and recovery/auth surfaces | Forge | T-02 | Rules, setup, lobby, account/auth, missing/not-found retain real routes and recovery. | Complete |
| T-04 | C game role rollout | Forge | T-03, canonical board contract | Host/player/audience/results preserve role privacy, axes, hit geometry, and responsive layout. | Complete |
| T-05 | Guarded admin composition | Forge | Existing capability APIs | Guard remains; readable lists/details/actions; no auth bypass. | Complete |
| T-06 | Entry performance and emulator reliability | Forge | Existing router/Firebase contracts | Game/Auth/Admin lazy chunks; isolated-port E2E harness; no production default change. | Complete |
| T-07 | Independent technical/release assessment | Gate / root | T-01–T-06 | Gate technical PASS; production-only authorization checks remain. | Complete / production follow-up |

## Acceptance mapping

- `A-01` design authority: 0006 selected C and 0007 authorizes expanding the **approved homepage treatment** across the app. It does not claim a separate human route-by-route aesthetic review.
- `A-02` behavior: no fabricated data, no role/private-answer leak, canonical 25-cell geometry and team axes unchanged.
- `A-03` quality: relevant automated checks, emulator evidence, and structural scan are recorded as `QA-*` in [QA-CHECKLIST.md](QA-CHECKLIST.md).
- `A-04` release: only complete after `T-07`; production Google/App Check/signing/release verification remains outside this local source freeze.

The plan intentionally avoids production, schema, rule, and permission changes.


## T-10 — Compact host controls and consistent team cards (2026-09-08)

**Milestone:** M-9. **Owner:** Forge owns scoped implementation and checks; root owns scope, integrated review and final acceptance. **Status:** Done — root accepted 2026-09-08. **Authorization:** user requested a proper plan followed by implementation of the shown host options panel and team cards. This is a bounded refinement of approved DESIGN-DNA/0007, not a new visual direction or production release.

### Entry evidence and dependencies

- Reuse [DESIGN-DNA](../DESIGN-DNA.md) and immutable [0007 rollout authority](../design/evidence/history/0007-spatial-studio-whole-app-rollout.md); preserve all history and receipts. Later supersession in 0007 governs incompatible older flat/radius clauses.
- T-08/M-7 is accepted. T-09/M-8 supplies the existing QR dialog, host-only assignment and safe-state rules, but remains **Implementation complete — ready for review** with its independent security/Gate/LAN acceptance pending. T-10 consumes its current source without accepting, expanding or weakening those boundaries. T-10 UI acceptance cannot close T-09 or production M-6.
- Source inspection: `src/routes/GameRoutes.tsx` TeamScoreCard (around 984–1070) contains its own live-player `<select>` at 1065, while HostLobbyControls (around 1284–1311) already uses clickable draggable capsules. This divergence explains the returned dropdown; unify the presentation mechanism rather than fixing only one location again.
- Host composition (around 2212–2340) renders QR first, score pair, a phase/timer topline plus HostActions, private answer, correction trigger, connection, pause, then HostTeamManagementStatus. CELL_SELECTION repeats instruction in the topline and button. The final status after pause makes pause cease to be the last control zone. Preserve the hidden authoritative deadline reconciliation when reorganizing FIRST_ANSWER.
- `src/styles/global.css` has host rules around 4300–4505 and later additions around 4571–4575. Read the cascade before editing; consolidate the touched host component rules instead of appending another competing override block. Shared TeamScoreCard also renders stage/audience scores: scope visual changes to host variants.
- `graphify affected src/routes/GameRoutes.tsx` returned only `tsconfig.game.json:L4`, so graph coverage is insufficient for a reliable UI blast radius; actual source inspection above is authoritative. CLI reported skill 0.9.14/package 0.9.16 mismatch; no graph or tooling changes are authorized.
- Existing `QuestionReveal.tsx` currently uses `number | undefined` with `window.setTimeout/window.clearTimeout`; historical T-09 timer errors may already be repaired in the mixed working tree. Re-run checks before claiming the blocker persists. A minimal browser-timer typing repair is allowed only if reproduced, with timing/cleanup behavior unchanged.
- Initial tree contains extensive modified and untracked user/T-09/content work. Preserve it all, including prior host edits. No reset, stash, broad staging, auto-commit or wholesale file restoration.

### Decisions and scope

**D-10.1 — One capsule interaction:** share a small host-player capsule within GameRoutes (or one adjacent presentational module only if this reduces duplication). Both lobby and live cards use the same UID-based move callback and visible player name. Native drag/drop moves into a team box; tap, Enter or Space on a capsule moves to the other named team, matching the existing lobby fallback. Give the capsule an Arabic accessible name describing the destination; use visible focus and a short contextual hint. No select/dropdown, count label or directional arrow is added to live play cards. Preserve truthful lobby joined/ready counts and unassigned players. Disable moves using existing safe-state/connection/pending/member-ID guards; never infer a target from a display name or optimistically change team state.

**D-10.2 — One operational sequence:** keep QR joining in a compact top utility row of the navy host panel. Keep the two team cards as one coherent pair with rounds visually dominant and points subordinate. Put the current actionable phase in one stable control region directly below the scores. Do not repeat identical phase text as both heading and action; retain a timer/status only when it contributes distinct information. During answer judgment show the answering/winning team banner immediately above the two correct/incorrect buttons, without an axis badge. Keep existing manual team-selection fallback when legal; preserve automatic cell reveal, buzzer opening and correct-answer award sequencing. The user is changing control presentation, not runtime transitions.

**D-10.3 — Supporting operations:** private answer content uses two semantic rows: primary answer and a distinct alternatives row (existing absent-value fallback retained); wrap long real content instead of truncating it to a hard two-line height. Render correction as a red underlined text-style button, preserving its board dialog, actual team names, audit and confirm/cancel semantics. Place concise connection/error/restriction information near the lower edge, with pause/resume as the last visible operational control. Remove verbose evergreen footer instructions; show a short contextual move hint/restriction near the capsule group only when useful. Do not remove actionable errors or locked-state explanations.

**Exclusions:** actual `/admin` content tools/routes, board geometry/hit mapping/axes, backend semantics or safe-state lists, schemas/rules/permissions, QR origin/network/security logic, new dependencies, new design concepts, content-bank work, deployment/firewall changes and unrelated global CSS cleanup.

### Implementation tasks and ownership

| Task | Outcome and modules | Dependencies | Owner | State |
| --- | --- | --- | --- | --- |
| T-10.1 | Snapshot current host/lobby and inspect existing guards; establish scoped JSX/CSS consolidation map and preserve current tests/hooks. GameRoutes/global.css/focused tests, read-only runtime references. | T-08 accepted; current T-09 source; D-10.1–3 | Forge | Complete — evidence below |
| T-10.2 | Unify accessible capsules in lobby and live cards; make round wins dominant and points compact; preserve physical green-left/red-right placement and stage variants. GameRoutes/global.css. | T-10.1, D-10.1 | Forge | Complete — evidence below |
| T-10.3 | Consolidate QR utility row, stable phase actions/banner, two-row private answer, correction link, lower status and final pause; remove duplicate headings/empty gaps/verbose footer through coherent scoped rules. GameRoutes/global.css. | T-10.2, D-10.2–3 | Forge | Complete — evidence below |
| T-10.4 | Focused UI/interaction regressions, app checks and responsive real-runtime evidence; record exact outcomes/files/screenshots and any baseline failures. Existing tests/app/host-lobby.test.tsx, tests/app/game-routes.test.tsx if present, or adjacent focused host test; QuestionReveal only for reproduced timer prerequisite. | T-10.3 | Forge | Complete — evidence below |
| T-10.5 | Integrated diff and load-bearing browser check against this contract, root acceptance; report T-09/M-8 and M-6 as separate pending work. | T-10.4 | Root | Done |

Implement in order. Keep shared components local and small; do not extract an unrelated routing/state abstraction. Forge may update only this task's checklist, evidence, blockers and “Implementation complete — ready for review” state. Root alone marks Done. Reuse existing test locations after inspection; paths listed as conditional are not claims that a file exists.

### VISUAL CONTRACT — include verbatim in implementation handoff

Mood/concept: refine the approved مدار الحروف Arabic game studio; keep the current cobalt environment and navy host console with opaque, readable team/control surfaces. This is compact operational refinement within DESIGN-DNA/0007.
Focal point/scan order: board and question remain the page focus; within the host panel scan compact QR utility, paired teams with dominant round wins, one stable phase action area, private answer, correction, lower connection/status, then pause/resume last. No duplicate phase heading and action label.
Density: host 5/10 and lobby 4/10; group related content with whitespace rather than nested cards or tall empty gaps. Preserve useful explanations only where actionable.
Desktop/mobile: preserve current board/console geometry and physically green-left/red-right team order; keep team summaries paired where readable and adapt within the existing narrow layout at 320px+. Capsule names wrap; no document overflow. Mobile controls remain at least 44px and keyboard/200% zoom usable.
Typography: IBM Plex Sans Arabic; 700 round-win numerals, 600 headings/actions, 400–500 names/metadata; tabular scores/timers and isolated LTR codes. Round wins are visibly larger than answer points. Arabic has no letter spacing. Primary answer and distinct alternatives occupy two semantic rows with natural wrapping.
Palette/surfaces: reuse approved spatial/arena tokens and existing rounded outlined controls; emerald vertical and crimson horizontal identities never swap. Correction is a red underlined text-style button as explicitly requested. Keep high-contrast opaque surfaces, restrained borders and no new palette or imagery.
Spacing/shapes: use the existing 4px scale, compact 8–12px related-group gaps and 16–24px major internal separation where appropriate; preserve 12px control and 16–20px panel language. Consolidate touched host selectors; no repeated late overrides, nested card stack, fixed spacer blocks or typography shrinking to hide crowding.
Icons/imagery: retain existing labelled outline icons only where useful; player names are draggable capsules with tap/Enter/Space fallback and accessible destination labels. No player dropdown, live-player count label, directional arrow or winner axis badge. Keep the existing QR dialog.
Interaction/states: UID-based authoritative assignments retain current safe-state, pending, stale, error and offline guards. Show one answering-team banner immediately above only correct/incorrect judgment buttons. Preserve legal manual fallback and automatic reveal/buzzer/correct-award path. Correction opens the existing board dialog with actual team names; dialogs retain Escape/close/focus restoration. Pause/resume is last, connection is low, and short contextual restrictions remain available.
Motion: retain restrained existing press/state feedback and static reduced-motion behavior; controls and action slot do not move unexpectedly between phases. No new animation dependency or ambient motion.
Anti-patterns: no new visual direction, board geometry/axis changes, backend behavior changes, stage/audience restyling, duplicate phase copy, verbose evergreen footer, inaccessible drag-only interaction, fake data, private-answer leakage or `/admin` content-tool changes.

### Acceptance criteria and required verification

- [x] AC-10.1: QR trigger occupies the compact top utility row in lobby and live host panel; opens/closes the retained dialog without changing encoded URL/origin logic.
- [x] AC-10.2: host team round-win numeral is visually larger than points at desktop and mobile; names wrap, fixed green/red placement and board axes remain correct; stage/audience unchanged.
- [x] AC-10.3: both lobby and live capsules move identified players through drag/drop and tap/Enter/Space fallback; duplicate names remain independent; no live select/count/arrow. Disabled/pending/offline/locked and missing-ID cases cannot move players and have understandable feedback.
- [x] AC-10.4: ROUND_SETUP, CELL_SELECTION, QUESTION_READING, FIRST_ANSWER, OPPONENT_CHANCE, QUESTION_FAILED, ROUND_COMPLETE, PAUSED and lobby show legal controls in the intended region without repeated phase instruction. FIRST_ANSWER has team banner immediately above only correct/incorrect judgment buttons and no axis badge. Existing manual fallback and automatic transitions remain intact.
- [x] AC-10.5: private answer/alternatives remain host-only, visibly distinct and readable with long real content. Correction is red/underlined and opens the existing accessible board/audit dialog with actual team names. No workflow/data change.
- [x] AC-10.6: connection/actionable errors are lower; pause/resume is the last visible operational control when legal. No verbose footer or empty error paragraph reserves unnecessary space. Locked movement explanation survives near the affected controls.
- [x] AC-10.7: scoped CSS forms one maintainable host system; no new contradictory override tail. No clipping/overflow at 320, 390 and 1440px; verify 200% zoom, keyboard focus, reduced motion, empty/full/long-name teams and dialog scrolling.
- [x] AC-10.8: run focused host/lobby UI tests and relevant existing question-reveal/game-route tests, `npm run typecheck:app`, scoped ESLint on changed TS/TSX, `npm run build`, `npm run test:game`, and `git diff --check`. If timer typing fails, reproduce and apply only the allowed DOM timer repair, then rerun its focused test and affected checks. Record other baseline failures honestly; do not expand into unrelated fixes. Backend tests/emulator reruns are conditional on an actual newly discovered boundary change, which first requires root scope review.
- [x] AC-10.9: real local host room browser smoke: lobby join → capsule moves by both interactions → start → cell reveal/buzzer → correct/incorrect path → safe-state team move → pause/resume; QR and correction dialogs focus/close verified. Capture representative lobby, CELL_SELECTION, FIRST_ANSWER and narrow layout under `output/playwright/host-controls-t10/`; screenshots provide fidelity evidence, not automatic aesthetic approval. Do not fabricate authority in fixture mode.

### Risks, rollback and completion evidence

Main risks: shared score CSS leaks into audience, phase markup removes deadline reconciliation, capsule click accidentally fires after drag, focus is lost on reassignment, or hidden empty/status containers keep the panel tall. Scope host variants, preserve countdown callback lifecycle, suppress post-drag click when required, maintain focus by UID, and test actual rendered flow. Any discovery requiring backend/security/geometry changes pauses that expansion for root decision; T-10 itself remains frontend-scoped.

Rollback only T-10 edits using a reviewed task diff or manual inverse changes against the inspected mixed baseline; never restore whole dirty files. No data rollback or migration is needed. Capture exact edited files, commands/results, screenshot paths, known limitations and current acceptance state here. Baseline evidence is retained below; implementation evidence follows it.

**Root live baseline supplement (2026-09-08):** `output/playwright/host-lobby/t10-before.png` captures the inspected local host room. Root computed the host grid with `align-content: normal` and rows approximately 64, 260, 129, 44, 36, 22, 64, 54px. The empty `.form-message` contributes a 24px element in a 36px row; QR/pause controls are 52px in 64px rows; management status adds 54px after pause. T-10.3 must render the error container only for a nonempty error, use intentional start-aligned/content-sized rows, and remove stretching/spacer causes instead of masking with arbitrary negative margins. If a short contextual help/status line is needed, place it before pause so pause stays the last visible operational control. Baseline is observation evidence, not a target fixed panel height.

**Forge implementation evidence (2026-09-08):** T-10.1–T-10.4 are complete and ready for root review. `GameRoutes.tsx` now shares a UID-based `HostPlayerCapsule` between lobby and live host score cards. It preserves native drag/drop and tap/Enter/Space assignment, suppresses only the short pointer click produced immediately after a drag, keeps keyboard activation available after a cancelled drag, and restores focus to the same UID after an authoritative update. Missing UID, offline, busy, fixture and unsafe-state paths leave the capsule unavailable. The host panel uses a compact QR utility row, content-sized rows, paired configured team cards with larger round wins, one action area, concise two-row host-only answer content (`الإجابة` and `بدائل`), lower connection/restriction information, and pause last. Empty errors and evergreen movement text no longer reserve rows. `global.css` consolidates the shared capsule and scoped host presentation without changing stage/audience score styling. No QuestionReveal timer fault reproduced, so no timer source changed.

**Verified checks (2026-09-08):** `npx vitest run tests/app/host-lobby.test.tsx tests/app/game-board.test.tsx` passed (27 tests: UID/missing-ID/offline/busy guards, cancelled-drag keyboard activation, focus restoration, and correction-board keyboard coverage); `npm run typecheck:app` passed; `npm run lint -- --quiet` passed; `npm run build` passed with the existing Rollup >500 kB chunk advisory; `npm run test:game` passed (29 tests); `git diff --check` passed. Root's real local browser matrix passed and was refreshed after the concise-answer change in `output/playwright/host-controls-t10/results.txt`: duplicate-name lobby drag/Enter and live drag/tap assignments, locks while questioning/paused, automatic correct/incorrect flow, QR/correction dialog behavior, 320/390/1440 layouts without overflow/select/empty-error artifacts, and 200% zoom. Supplemental accessibility checks verified both dialog Escape/focus restoration, reduced-motion preference, and twelve long-name capsules at 320px without overflow. Root visually reviewed `round-setup-1440` and `first-answer-320`: readable hierarchy/spacing, green-left/red-right pairing, and pause last. This supports implementation readiness; root retains final integrated acceptance.


**Root acceptance (2026-09-08):** T-10.5 complete. Integrated capsule, host layout and private-answer source reviewed; final browser matrix in room 78DEDA passed after the concise answer adjustment. Screenshots and machine results are in [host-controls-t10](../output/playwright/host-controls-t10/results.txt); supplemental dialog focus/reduced-motion/12-capsule layout checks passed. T-10/M-9 is Done. T-09/M-8 was separately accepted by its owning task; its dated acceptance supersedes earlier pending references here. Production M-6 remains pending. Exact next action: user reviews their host screen. No production deployment or data changes in T-10.

### T-10 lobby correction after user visual rejection — 2026-09-08

The user rejected the lobby appearance after the earlier implementation handoff. The prior live-play review did not adequately assess the lobby cards; that is not evidence of human visual acceptance. Corrected HostLobbyControls and scoped global.css: navy paired cards with solid team-color headers, no AxisMark arrows or repeated headings, compact count badges, wrapping capsules, one short move hint, lower connection/readiness status, and full-width Start last. Removed the obsolete mobile single-column override; green remains physically left and red right at 320px+.

Forge verified host-lobby UI 9/9, app typecheck, lint and diff-check. Root inspected final lobby-320.png and lobby-390.png and reran the real two-player matrix in room B26D3F, including drag/keyboard assignment, ready/start, automatic question flow and responsive checks. Evidence: output/playwright/host-controls-t10/results.txt and lobby screenshots. No backend or data changes. Implementation correction is verified; user visual review remains the next step.
## T-11 — Required join names and host-created manual players (2026-09-08)

**Milestone:** M-10. **Owner:** Forge implementation/checks; root acceptance with database/security boundary review before implementation and material-risk Gate before final acceptance. **Status:** Done — root accepted with technical Gate PASS 2026-09-08. **Authorization:** user requests a required join name and host creation of named players without devices/buzzers, coexisting with joined device players. Add only; no removal or new management product.

### Source evidence and design decisions

Current source: EntryRoute sends `name.trim() || 'لاعب'` to both join paths; server/service.join also defaults to `لاعب`. Firebase joinRoom calls validateDisplayName with that fallback before checking existing membership. Its existing validator trims, bounds at 48 UTF-16 code units and rejects control characters. Firebase CanonicalMember UID is an auth identity: index.writes creates `player_<uid>` projections, and firestore.rules authenticates membership using request.auth.uid. Therefore inserting manual entries as members with fabricated UID would incorrectly mix roster and credentials. T-08–10 supply host roster, UID-safe capsules and team moves; T-10/M-9 is accepted, with the later lobby correction and outstanding user visual review preserved as dated evidence. T-09/M-8 was separately accepted by its owning task. Current allReady uses active device players and existing team/ready checks; index.joinRoom caps device players at 16, and team allocation reads members. These must consider manual participants without treating them as sessions.

**D-11.1 — Canonical separate roster:** add optional `manualParticipants?: Array<{ id: string; displayName: string; team: TeamAxis }>` to canonical room JSON in local authority and Firebase. Missing means empty. IDs are server-created UUIDs independent of auth UID; no token, capability, Auth user, member document, player projection or self identity is ever created. This additive room field is necessary to persist the explicitly requested non-device participants; no SQL table, Firestore collection, migration/backfill, indexes or rules change is needed. Existing members remain device participants by default. Retain participants across existing room lifecycle just like the current member roster.

**D-11.2 — Safe presentation and targeting:** project manual participants into the existing displayed player roster with `participation: 'manual'`, displayName/team and derived `ready: true`. Give only host projections `manualParticipantId`; never put manual IDs into `uid`. Existing/omitted participation is device. Public/player summaries may display the truthful name/team/type but no target identifier or private answers. Use discriminated capsule targets internally: device memberUid or manualParticipantId. Extend existing LOBBY_ASSIGN_TEAM payload validation to accept exactly one identifier plus valid team; reject both/neither/extra keys. Existing device payload remains compatible. Resolve IDs only in the corresponding collection. Manual moves keep derived readiness; device lobby moves still clear readiness. UID-based device authorization and self remain unchanged.

**D-11.3 — Host addition:** new host-only intent `LOBBY_ADD_MANUAL_PLAYER` with exact `{displayName, team}`. Trim/validate the name and team at authority; generate unique ID server-side only after revision/role/state/capacity validation. Firebase pure reducer accepts an authority-generated ID input for addition, or a narrowly scoped server-owned factory; clients cannot choose it. Replayed intent uses existing receipt and cannot create a second participant; transaction retries never persist duplicates. Permit addition in LOBBY only for this minimum requested feature, with visible reason elsewhere; existing safe between-question team-move states remain unchanged. User has requested creation, not late joining of manual participants. Add/remove/rename/convert-to-device flows beyond creation are excluded.

**D-11.4 — Combined roster semantics:** total active players = active device players plus manual entries. Both authorities cap this combined total at existing MAX_PLAYERS=16; use combined team sizes for new device auto-allocation. Preserve zero-player start behavior. With any participants, require at least two total and one on each valid team, every device participant ready, and every manual participant assigned (required at creation). All-manual and mixed starts work; adding manual participants does not mark devices ready. Lobby counts/ready counts reflect participants, not connections, and names may duplicate. Manual players never issue LOBBY_SET_READY or BUZZ; those operations still require the genuine authenticated/capability player actor. Existing HOST_SELECT_TEAM supplies manual answer selection, also in mixed rooms. Compute any UI `hostOnly`/device-buzzer messaging from device participants where appropriate so all-manual rooms retain the manual answer controls. Do not change buzzer eligibility, phase transitions, scoring, board, deadlines or winner history.

**D-11.5 — Mandatory device join name:** new joins require a string that trims to 1–48 UTF-16 code units and has no Unicode control characters, matching the existing Firebase bound. Reject missing, whitespace-only, non-string, overlong and control-character input at EntryRoute, local service/HTTP boundary and Firebase callable; never silently substitute `لاعب` or truncate. Allow real Arabic and duplicate names. Preserve separate host/audience defaults. Firebase checks an existing authenticated active membership before requiring a new-join name, retaining prior closed-room policy and reconnect identity/name; do not revalidate or rename legacy members on reconnect. Local reconnect already uses an issued capability rather than a new join: preserve that path, never match by display name. Join adapters must propagate useful Arabic validation feedback rather than collapsing every rejection into “room not found.”

### Ownership, sequence and dependencies

| Task | Deliverable / files | Owner | Dependencies | State |
| --- | --- | --- | --- | --- |
| T-11.1 | Read actual current source, graphify affected paths and test harness; boundary review confirms separate manual roster, authenticated membership invariants and transaction ordering. Root records review disposition here. | Root/review roles | D-11.1–5; current mixed baseline | Complete |
| T-11.2 | Add optional room/manual and projection target types; exact intent validation; local name validation, host add/move, combined readiness/capacity/allocation/projection and persistence. `src/features/game/runtime/contracts.ts`, `server/service.ts`, `server/index.ts` only as required; shared small validation helper if both toolchains support it. | Forge | T-11.1 | Implementation complete — ready for review |
| T-11.3 | Mirror authority logic and atomic persistence in `functions/src/game.ts`, `functions/src/index.ts`; manual data stays in room document, never target-member write/projection loop. Existing auth actor/member and audit identity remain intact. New join validation comes after existing-member resolution. | Forge | T-11.2 | Implementation complete — ready for review |
| T-11.4 | Required-name field/errors in `src/routes/EntryRoute.tsx`; compact host add form/disclosure name + named team + add in `src/routes/GameRoutes.tsx`, scoped CSS, capsule target discrimination, truthful manual/device readiness hints and manual answer availability. Runtime adapters only for validation/error propagation if needed. | Forge | T-11.3; visual contract | Implementation complete — ready for review |
| T-11.5 | Local/reducer/UI tests, Functions build, app checks, Firebase callable/persistence E2E and real mixed/all-manual browser evidence; update only this task's checklist/evidence/blockers and mark Implementation complete — ready for review. | Forge | T-11.4 | Implementation complete — ready for review |
| T-11.6 | Integrated diff, load-bearing mixed/manual runtime check and material-risk Gate; root marks Done after acceptance. | Root | T-11.5 | Done |

No source implementation is performed by this planning assignment. Preserve all modified/untracked work. Conditional helper/adapter files are discovered during T-11.1, not permission for broad extraction. Root owns scope decisions and final state.

### VISUAL CONTRACT — include verbatim in implementation handoff

Mood/concept: extend the approved مدار الحروف cobalt studio and current corrected navy host lobby; preserve DESIGN-DNA/0007 and T-10 composition.
Focal/scan order: join view shows room code and required player name before Join; host lobby shows its existing QR utility and a compact إضافة لاعب بدون بازر disclosure, then the paired team capsules, readiness and Start last. Board/question and established live controls retain priority.
Density: lobby 4/10, host 5/10; the add form appears on demand and contains only name, actual named team choice and one Add action with adjacent validation. Avoid a permanent administration card stack.
Desktop/mobile: preserve paired green-left/red-right team cards and current wrapping capsules; stack add fields naturally at 320px+ without whole-page overflow. Controls remain at least 44px and usable at 200% zoom.
Typography: IBM Plex Sans Arabic; 600 labels/actions, 400–500 names/help, existing 700 score hierarchy. Required-name errors are concise Arabic, associated with the field; duplicate names remain fully readable. No Arabic tracking.
Palette/surfaces: reuse existing spatial/arena tokens, opaque navy/light control surfaces, outlined rounded controls and fixed emerald/crimson identities. Manual players use a small neutral text cue such as بدون بازر, never fake connection/readiness dots or a new team color.
Spacing/shapes: existing 4px scale, compact related-field spacing and existing panel/control radii; edit canonical scoped host rules without another competing CSS tail.
Icons/imagery: no new media or dependencies. Keep QR dialog and existing capsules, with no dropdown/count/axis-arrow regression in live cards. Team choice in the creation form uses the actual named teams and accessible buttons.
Interaction/states: name is required with trimmed bounded validation, focus-visible and pending/error handling. Host-only Add creates an authoritative participant and then shows its capsule; drag/tap/keyboard move still works by typed identifier. Manual readiness is explained once near the form or roster; device readiness/buzzers stay truthful. No removal/rename/device-conversion affordance. All-manual rooms retain host team-answer selection, and Start remains last.
Motion: retain existing restrained press/state feedback and static reduced motion; no new animation, auto-scroll jump or moving controls.
Anti-patterns: no fabricated auth identity/credential/connection, no optimistic phantom player, no backend permission bypass, no name fallback, no new visual direction, no board/axis/scoring changes, no private-answer leakage or unrelated admin/content edits.

### Acceptance and required checks

- [x] AC-11.1: new device join rejects missing/blank/non-string/>48/control-character names in UI, direct local request and Firebase callable; valid trimmed Arabic persists. Duplicate names are allowed. Existing Firebase member reconnect with omitted name retains identity/name and current closed-room policy; existing local capability reconnect works without new membership.
- [x] AC-11.2: host Add creates one manual participant with selected team and trimmed name, atomically persisted and immediately projected; duplicate/replayed/stale/concurrent requests cannot double-add or exceed 16 combined players. Wrong role, malformed payload/target and non-LOBBY add fail.
- [x] AC-11.3: no manual auth user, member document, token, self state or `player_<manual-id>` projection exists. Forged manual IDs cannot authenticate, ready or buzz. Host-only move IDs remain absent from public/player payloads; existing private-answer isolation remains.
- [x] AC-11.4: all-manual two-team start succeeds; mixed start waits for every real device readiness; manual creation/moves preserve derived readiness; device lobby moves reset readiness. One-team/one-player/over-capacity cases reject and empty-room behavior remains. Combined counts and auto-allocation are correct.
- [x] AC-11.5: device and manual capsules with duplicate names move independently via drag and tap/keyboard; old device projections without participation read correctly. Named host answer selection works in mixed/all-manual questions with existing correct/incorrect flow; device buzzers still work only for genuine eligible devices.
- [x] AC-11.6: focused `tests/app/entry-route.test.tsx` and `tests/app/host-lobby.test.tsx`, `tests/game/service.test.ts`/websocket tests, `functions/src/game.test.ts` pass with additions covering validation, parity, capacity, readiness, projection and replay. Run `npm run typecheck:app`, scoped lint, `npm run test:game`, `npm --prefix functions test`, `npm --prefix functions run build`, `npm run build`, and `git diff --check`.
- [x] AC-11.7: extend existing `tests/e2e/firebase.spec.ts`/harness for actual callable create-manual → canonical room persistence → host/player projection refresh → move → device ready → mixed Start, plus all-manual Start and reconnect-name behavior. Assert authenticated member documents remain genuine and manual player projections absent; run `npm run test:e2e:firebase` and focused rules tests if available. Reducer-only evidence is insufficient.
- [x] AC-11.8: root/Forge browser evidence covers blank-name join, host form, all-manual and mixed local rooms, QR joining and keyboard creation/moves at 320/390/1440px with no overflow or T-10 hierarchy regression. Store results/screenshots in `output/playwright/manual-players-t11/`. Report actual errors and baseline failures; no fabricated multiplayer fixture success.

### Impact, rollback, unknowns and evidence

Impact is one optional bounded room JSON field and additive intent/projection variants. Existing rooms default to an empty manual roster; existing member/auth/rules behavior must remain unchanged. No migration or production write is planned. Main risks are an auth-bearing manual identity, incorrect combined counts/start logic, join reconnect accidentally rejected, or transaction retry duplicate additions. The separate canonical roster and exact target discriminant address identity risk; authority and persistence tests address the others.

Rollback preserves all room data and unrelated dirty work. Roll back only reviewed T-11 source hunks; retain optional manualParticipants data. Older code would ignore those participants and misrepresent room readiness, so do not downgrade an in-progress room using manual participants: complete/close the affected test rooms first or keep the compatible reader until they finish. No destructive data removal/backfill is authorized.

Unknowns to resolve in T-11.1: current local join validation/capacity helper details; exact adapter error mapping; test harness access for canonical Firebase document assertions; any frontend code that equates every displayed player with a device. These are bounded inspection tasks, not unresolved product decisions. Boundary review may refine implementation plumbing without changing D-11 semantics; material schema/auth alternatives return to root before implementation. Current evidence: source inspection and plan only; no T-11 tests or implementation claimed.

**Root clarification and security review (2026-09-08):** required name stays always visible in the join form with Arabic help and 48-character maximum. Host trigger is `إضافة لاعب بدون بازر`; it opens a compact name/team/Add form in the existing lobby panel, with colored actual-team buttons. Manual capsules show a subtle visible and accessible `بدون بازر` cue; no readiness checkbox or device actions. No mode switch: both participation types coexist naturally. Security review reports no blocker for the separate room array, provided the exact payload, room-scoped target resolution, combined-capacity race, server ID, replay/atomicity and no-auth-artifact criteria above are verified in runtime/emulator tests. Database/storage review disposition remains to be recorded by root before implementation.

**Storage review disposition (2026-09-08):** no blocker; optional room array requires no migration, rules change or schema-version bump. Generate the addition UUID once outside Firebase transaction retries and capture it for the reducer; existing receipt/revision writes remain atomic, so retries do not generate new durable participants. Ensure every projection rebuild, including deadline reconciliation and summary paths, reads manualParticipants from the canonical room. Preserve authenticated members lists as authenticated identities; expose a separate manual roster/count in server-owned summaries only if needed, with no admin UI expansion. Local hydration/snapshot round trips preserve the optional field and read missing as empty; add a persistence/hydration regression. Older-source rollback may retain the field inert but must observe the in-progress-manual-room downgrade warning above. Database and security T-11.1 review prerequisites are satisfied; Forge still performs its bounded source/harness inspection before T-11.2.


**T-11.1 boundary disposition (root, 2026-09-08):** Security and Database source reviews found no blocker for the optional separate room.manualParticipants array. T-11.1 complete; implementation authorized. Preserve auth-only member/projection loops including admin/deadline writers; summaries distinguish authenticated members from manual roster. Generate Firebase manual UUID once outside transaction retry and capture it, then use only after intent validation; receipt writes remain atomic. Count combined players for cap/allocation/readiness. Existing rooms default empty with no backfill/schema/rules change. Planned rollback retains compatible readers for active manual rooms rather than silently dropping participants. All required negative/concurrency/persistence checks remain acceptance requirements.

**Forge implementation evidence (2026-09-08):** T-11.2–T-11.5 are implementation complete and ready for root/Gate review. The local and Firebase authorities persist optional `manualParticipants` separately from authenticated members, apply exact host-only lobby add/move intents, keep manual readiness derived, and enforce a combined 16-player cap/readiness/start rule. Required new device names are validated at EntryRoute, local HTTP/service, and Firebase callable boundaries; Firebase resolves an existing authenticated membership before validating a rejoin name. Host projections alone receive manual target IDs; player/audience projections never receive them. The host lobby adds the required named form and typed capsules without creating a device identity. The local response path treats HTTP 200 stale/no-envelope responses as failures so the form remains populated for retry.

**Verified checks (2026-09-08):** `npx vitest run tests/app/entry-route.test.tsx tests/app/host-lobby.test.tsx` passed (17 tests); `npm run test:game -- --test-name-pattern="manual lobby participants"` passed (30 tests); `npm --prefix functions test` passed (27 tests, including the Functions build); `npm run typecheck` passed; `npm run lint` passed; `npm run build` passed with the existing Vite chunk-size advisory; and `git diff --check` passed (only existing CRLF conversion warnings). With `PLAYWRIGHT_CHANNEL=chrome`, `npm run test:e2e:firebase` passed (2 tests): its direct callable room verifies replay, a simultaneous capacity-boundary add, no manual member/projection/auth artifacts, canonical persistence, host/player projection refresh after manual move, real-device readiness and mixed `START_MATCH` to `ROUND_SETUP`, all-manual start, and omitted-name authenticated reconnect. The first Firebase run could not locate Playwright's bundled Chromium; the env-driven Chrome channel recovery is recorded and the final run passed. The emulator reported the existing Node 22-requested/Node 24-host warning only.

**Root browser evidence (2026-09-08):** local manual-only and mixed-room checks passed for required-name validation, add/retry, duplicate names, drag and keyboard moves, readiness/start, manual answer selection, and genuine device buzzers at 320/390/1440px. The stale HTTP-200 response form-retry check passed. Results and screenshots are under `output/playwright/manual-players-t11/`. A full UI suite run found 77 passing tests and one pre-existing unrelated `tests/app/setup-route.test.tsx` category-recovery expectation failure; it was not changed because category filtering is outside T-11.

**Root final acceptance (2026-09-08):** T-11.6 complete, M-10 Done. Gate PASS after independent authorization/identity/capacity/persistence checks, targeted stale-transport tests, and review of final passing callable mixed-flow evidence. Root reproduced required-name validation, manual-only and mixed local games, manual drag/keyboard movement, real-device readiness/buzz, failed/stale add preservation and successful retry. Final screenshots at 320/390/1440px and results are in [manual-players-t11](../output/playwright/manual-players-t11/results.txt), [form recovery](../output/playwright/manual-players-t11/forms-results.txt), and [final form](../output/playwright/manual-players-t11/final-form-390.png). Green-left team choices and readable join help were inspected after the final CSS corrections.

The initial Gate stale-response failure was repaired and independently rechecked. The initial Firebase browser test was device-only; the final direct callable test explicitly covers manual move, canonical/host/player projection refresh, genuine device Ready and mixed Start. This supersedes earlier incomplete mixed-Firebase claims. Final emulator status: two passing tests, no failed tests. Broader UI verification still has one category-recovery expectation failure outside the changed feature; no pre-change full-suite baseline was captured by root, so it is recorded as an out-of-scope current failure rather than a newly verified historical baseline. Build retains its existing chunk-size advisory.

Changed feature areas: EntryRoute.tsx, GameRoutes.tsx, global.css, runtime/contracts.ts, server/service.ts, functions/src/game.ts and index.ts, related Entry/Host/service/Functions tests, Firebase and smoke E2E, and optional Chrome-channel harness configuration. Canonical documents record plan and completion. No production deployment, migration, auth identity creation for manual players, or room-data deletion. Local dev was restarted with the existing SQLite store and LAN settings. Exact next action: user can join with a required name or use إضافة لاعب بدون بازر in the host lobby; both types can coexist.
### T-11 room-first name entry correction — 2026-09-08

User clarified the required sequence: room code first, then name on the room entry screen. EntryRoute now asks only for a code and navigates to /room/:roomCode/join. The new lazy NameJoinRoute shows the room code, asks for a required name, and submits the existing validated join operation; the host roster then shows the trimmed name. QR ?room=CODE links skip the code form and open this name screen. No anonymous participant is created before name submission. Manual-player support, backend validation, authorization and reconnect behavior are unchanged.

Changed: src/routes/EntryRoute.tsx, new src/routes/NameJoinRoute.tsx, src/app/App.tsx, scoped global.css, entry tests and existing Firebase/smoke UI flow selectors. Focused entry tests 7/7, typecheck, lint, build and diff-check passed. Build retains its existing chunk-size advisory. Root real local two-player check in room717122 passed code-only homepage, room-before-name routing, empty-name validation, no unnamed membership, trimmed name visible to host, QR direct-name entry and 320/390/1440 layouts. A missing spacing token was corrected to defined space-4; final390 screenshot confirms16px card padding and no overflow. Evidence: output/playwright/manual-players-t11/room-first-results.txt and room-name-390.png. Backend/emulator rerun was unnecessary for this frontend-only change; updated Firebase UI selectors were not rerun in the emulator during this correction.

**Host display-tab shortcut (2026-09-08):** added شاشة العرض at the bottom of the host panel in both lobby and live play. Uses the existing room display route with target=_blank and rel=noopener noreferrer, preserving the host tab. Changed GameRoutes.tsx and one scoped global.css rule. Scoped ESLint passed; real browser click opened a separate audience display tab for room717122 with null opener and left host URL unchanged. Mobile screenshot confirms readable white-on-navy bottom control: output/playwright/manual-players-t11/display-link-390.png. This requested navigation utility follows the previously-last operational control.

## T-12 — Truthful host player connection status (2026-09-08)

**Milestone:** M-11. **Owner:** Forge implementation/checks; root scope/acceptance, with security/database disposition before storage/boundary implementation and material-risk Gate before Done. **Status:** Done — root accepted with technical Gate PASS; broader suite timeout retained below. **Authorization:** user requests seeing which players are currently connected. This authorizes bounded additive presence storage, not production deployment, new RTDB setup or changing game readiness/scoring. T-11/M-10 is accepted; preserve its later room-before-name entry flow and bottom display-tab shortcut. Earlier T-09–10 status remains unchanged.

### Current evidence

`HostPlayerCapsule` in GameRoutes.tsx (around 1015–1060) currently renders ✓ for member.ready and بدون بازر for manual participants; readiness is not connection. `server/index.ts` stores per-room WebSocket sets containing ws/token, sends game metadata on connect/action/tick, and simply deletes the socket on close. It has no per-player reference aggregation or ping/pong liveness. `useRoom` in GameRoutes (around 733+) currently receives local game envelopes and Firebase subscribeProjection callbacks, sets connection from received data and drives automatic actions by game revision. Separate presence must not be mistaken for a game envelope or trigger those effects. Inspected current source has no explicit same-revision rejection there, but this is not a reason to embed heartbeat state in game projections.

Firebase adapter uses authenticated callable functions and onSnapshot game projections; there is no presence service. DeferredFirebaseGameAdapter must forward any new optional methods while retaining lazy loading. Firebase canonical members are auth-bearing; manual participants are a separate canonical room array. Host display is an audience projection opened under an existing identity: neither route label nor requested projection role is authority for player presence. `graphify affected server/index.ts` returned no affected nodes and warned of skill/package version mismatch, so source inspection, not graph completeness, governs this plan.

### Decisions and smallest implementation

**D-12.1 — Meaning:** a device player is connected when at least one authenticated participant transport/session for that room and canonical player UID is currently live according to the authority. This means the game client is communicating, not a claim that a person is watching or that the device is ready. Canonical active membership is only authorization/roster membership. Manual participants have no connection state: keep بدون بازر. Presence never changes ready, team eligibility, start rules, score, timer, audit or game revision.

**D-12.2 — Independent channel:** define a HostPresenceSnapshot with roomId, serverTime, observed device-player IDs and per-player connected/lastSeen/lease expiry as applicable; include a separate freshness status on the client. Keep it outside ProjectionEnvelope and independent of game revision. Local WebSocket sends tagged `{type:'presence', ...}` messages to canonical host sockets only, routed before the game-envelope handler; it retains the existing game transport instead of creating a duplicate local socket. Firebase gets optional runtime adapter methods `subscribeHostPresence` and `startPlayerPresence` (names may follow existing naming convention), forwarded through deferred adapter. Firebase subscribeHostPresence may internally poll authenticated callable reads rather than establish a Firestore listener. Fixture/unsupported channels show unknown. A small useRoomPresence hook owns state/cleanup; avoid a routing refactor.

**D-12.3 — Local authority:** authenticate upgrade once using capability verification and existing canonical member lookup; store immutable roomId/UID/actual role alongside socket. Count only canonical player members, never manual IDs, host or audience. Maintain per-room/per-UID live socket refs. On open/close/termination, send fresh host presence snapshots without game metadata writes/revision increments. Ping every 15s; record pong, terminate a socket whose last authenticated liveness exceeds 45s, and aggregate online while any socket remains. Send a server-timestamped host presence snapshot at least every 15s even if unchanged so a stalled host stream expires truthfully. Clean refs/maps/timers on close and process shutdown. Local normal close can update host immediately; abrupt network loss is detected within 45–60s, including sweep scheduling. Ping/pong transport liveness does not imply foreground visibility.

**D-12.4 — Firebase UID leases, existing infrastructure only:** use private server-owned per-device documents at `rooms/{roomId}/presence/{uid}`, separate from canonical room, members, projections, events and intent receipts. Store one lease document per actual device UID with minimal server-owned uid, updatedAt and expiresAtMs. Per-UID documents avoid shared-document transaction contention; host reads are bounded to the current active device roster (maximum16). Missing document or never-observed UID means unknown, not disconnected. Authenticated renewPresence accepts exact `{roomId}`, verifies active canonical player membership and room availability, and derives UID from request.auth and times from the server clock. Client sends no UID/session ID/role/online/timestamp. Heartbeat immediately and every15s while a genuine player client is mounted; TTL45s. Throttle persisted renewals transactionally to at most once per15s per UID, so multiple tabs share one lease and cannot amplify writes. Do not issue offline/release writes on unmount: one tab closing must never clear another tab's connection. Last-tab disappearance becomes offline after the final lease expires. Local refs still support immediate normal-close detection. Cancel timers/retries on teardown, avoid overlapping requests and refresh on foreground/online recovery. Manual/host/audience actors cannot renew as players.

**D-12.5 — Host callable read instead of new Firestore rules:** authenticated host-only getRoomPresence is polled immediately and every10s while the host view is mounted, returning derived status against server time. It rechecks canonical host membership, filters current real device players and returns no credential or session artifacts. Known unexpired lease means connected, known expired lease means disconnected, and never-seen lease means unknown. Preserve the expired observation for this distinction; read only known current roster UID document paths rather than listing arbitrary presence entries. Do not refresh lease timestamps when reading. A separate listener-readable Firestore projection adds rules/cache plumbing; do not add it or RTDB. Private documents remain denied by existing catch-all rules. No scheduled TTL job is required; missing/old data remains safe and ignored outside the current device roster. Root database/security review must approve this UID-only callable polling design before implementation.
**D-12.6 — Freshness and limits:** host initializes unknown (`حالة الاتصال غير معروفة`), never defaults to green. Successful authoritative snapshot establishes a server-time anchor against monotonic client time; detect expiry from elapsed time rather than trusting device wall clock. Host unknown overrides all device dots immediately on detected offline/transport failure/cache-only or subscription failure; also after 30s without a successful authoritative presence snapshot, on foreground resume until refresh, and when the runtime cannot supply presence. Fresh snapshots may label connected or غير متصل. Firebase status delivery adds up to10s poll delay; last-tab or abrupt player failure can remain connected for up to55s (45s lease plus poll), communicated as approximately one minute, excluding service/network delay which must result in unknown at the host. Background suspension can expire a device lease even though a tab still exists; it becomes connected again when communication resumes. Document this as recent communication, not physical network truth. Status need not equal local WebSocket's exact instant, but both runtimes implement the same user meaning and unknown behavior.

**D-12.7 — UI:** device capsules show a small dot with readable متصل / غير متصل or unknown text, using UID lookup only. Duplicate names remain independent. Readiness is distinct, lobby-only labelled جاهز / بانتظار الجاهزية; never relabel ✓ as online. Manual badges remain بدون بازر, with no dot. Preserve capsule drag/tap/keyboard assignment, compact T-10 cards, fixed axes, QR/name flow, Add manual form and bottom display shortcut. Connection state does not disable assignment/start beyond existing rules.

### File-owned sequence

| Task | Deliverable/modules | Owner | Dependencies | State |
| --- | --- | --- | --- | --- |
| T-12.1 | Confirm canonical actor lookup and callable test harness; record security/database dispositions for private lease doc, limits, auth, freshness and no rules changes. | Root/review roles; Forge baseline inspection | D-12.1–7 | Pending |
| T-12.2 | Presence types, local socket registry/liveness/tagged snapshots and focused transport tests. `server/index.ts`, small `server/presence.ts` if useful, read-only actor helper in `server/service.ts` only if needed, runtime contracts. | Forge | T-12.1 | Complete — evidence below |
| T-12.3 | Private UID lease store/pure time helpers and authenticated renew/host-read callables. `functions/src/presence.ts`, `functions/src/index.ts`, focused Functions tests. No game reducer/schema/rules behavior edit. | Forge | T-12.2 | Complete — evidence below |
| T-12.4 | Firebase/deferred presence adapter methods, isolated hook and local tagged dispatch; host capsule status and separate lobby readiness. `src/features/game/runtime/{contracts,firebase-game-adapter,deferred-firebase-adapter}.ts`, `src/routes/GameRoutes.tsx`, small presence hook/helper adjacent to runtime/UI as appropriate, scoped global.css. | Forge | T-12.3; visual contract | Complete — evidence below |
| T-12.5 | Local actual sockets and Firebase emulator callable/multi-client evidence, timers/cleanup/security/UI tests and responsive screenshots. Existing tests/game, Functions tests, tests/app/host-lobby.test.tsx and Firebase E2E harness. | Forge | T-12.4 | Complete — evidence below |
| T-12.6 | Integrated diff and load-bearing disconnect/reconnect/multitab check plus Gate; root accepts Done. | Root | T-12.5 | Done |

Forge updates only this task's checklist/evidence/blockers and Implementation complete — ready for review state. Root owns decisions/final status. No source edits in this planning assignment. No new dependency, RTDB, schema migration, production deployment, general presence dashboard or public identifier exposure.

### VISUAL CONTRACT — copy verbatim for implementation

Mood/concept: preserve approved مدار الحروف, corrected navy lobby/live host cards and existing manual capsule treatment; presence is a compact factual annotation.
Focal/scan order: player name first, device connection status second; lobby readiness remains a separate secondary status. Board/question, rounds/points hierarchy and established host action order stay unchanged.
Density: host5/lobby4; one small dot plus short readable text per device capsule, natural wrapping for long names; no new panel or status dashboard.
Desktop/mobile: preserve paired green-left/red-right cards and 320px+ no-overflow behavior, 44px capsule targets and keyboard/200% zoom operation. Keep bottom display shortcut and existing last operational actions.
Typography: IBM Plex Sans Arabic; readable compact 400–500 status text, existing name/action hierarchy; never shrink player names to make room. Arabic is untracked.
Palette/surfaces: existing token roles; connected uses existing semantic positive indication, disconnected/unknown neutral high-contrast dot/text, no new team identity color. Color never carries status alone.
Spacing/shapes: existing 4px scale and capsule radius; keep related status text close to its name with wrapping; edit scoped canonical CSS without override tails.
Icons/imagery: simple decorative status dot with textual متصل/غير متصل/unknown; manual players retain بدون بازر and no device dot. No new imagery or animated activity glyph.
Interaction/states: truthful unknown until fresh authority evidence; stale/offline host invalidates status, reconnect refreshes it. Lobby ready text is distinct from connection; assignment drag/tap/keyboard and manual creation remain unchanged. Presence cannot change gameplay readiness or legal actions.
Motion: static status changes, reduced motion static, no pulsing online dots or countdown animation.
Anti-patterns: never infer connected from ready/active, never show a manual player connected, no fake fresh green from cached data, no game-revision heartbeat, no public UID leakage, no new visual direction/board changes or status-only nested cards.

### Acceptance and required checks

- [x] AC-12.1: real local player open/close updates host independently of ready/game revision; abrupt lost pong expires within documented bound; one of two player tabs closing leaves connected until last session closes/expires. Host/display/audience/manual identities do not register as players.
- [x] AC-12.2: Firebase real emulator clients share an actor UID lease across tabs; closing one tab, last-tab disappearance, interrupted heartbeat and reconnect agree with TTL/poll limits. Known expired leases derive disconnected only from a fresh host read; missing document/never-observed UID is unknown. Host offline/stale/failed poll becomes unknown, not cached green.
- [x] AC-12.3: leases/read endpoints reject nonmembers, wrong role, manual/foreign identity attempts, client timestamps, extra/malformed fields, cross-room targets; simultaneous same-UID renewals are throttled transactionally, and closing one tab emits no offline write. Game room revision, events, audit, ready and score remain identical before/after presence-only operations.
- [x] AC-12.4: only actual host receives roster UIDs/status; player/audience cannot read presence doc or invoke host read. No Auth users/member documents/player projections added; private-answer boundaries unchanged. Existing host display tab does not impersonate a player through requested audience role.
- [x] AC-12.5: same-game-revision presence snapshots update UI independently; tagged messages never enter game-envelope processing or retrigger automatic game actions. Legacy/fixture/unsupported channel is unknown. Monotonic freshness tests include device clock skew, foreground resume, network errors and teardown/race cleanup.
- [x] AC-12.6: device capsule text/dot/readiness and manual N/A correct in lobby/live, including duplicate and long names; no existing capsule move/ready/start/add-player regression. 320/390/1440 screenshots and keyboard/reduced-motion/zoom verified.
- [ ] AC-12.7: `npm run test:e2e:firebase` executed the new real callable, browser two-tab, expiry, reconnect, and rules-denial checks, but its existing full reconnect/game/correction scenario exceeded its 90-second timeout after reaching `ROUND_SETUP`. Focused checks passed; final suite-level acceptance remains with root/Gate.

### Impact, rollback and unknowns

Private bounded lease storage is additive and ephemeral; no canonical room schema, migration or permissions expansion. Heartbeats/polls create callable/read/write traffic proportional to open game tabs; document 15s player heartbeat/10s host polling and measure emulator call counts to prevent duplicate-effect loops. Pause polling when unmounted; background resume refreshes immediately. Host read must not write game state. Deployment cost tuning/schedulers are outside this request.

Rollback removes only reviewed T-12 code hunks, preserving mixed user work and canonical rooms. Remaining private lease documents are inert/expired, not deleted; no data migration rollback required. Stop owned timers/listeners on teardown. Unknowns for T-12.1: exact existing canonical actor helper access, callable test setup, closed-room lease/read policy consistent with existing room availability, and host/client error plumbing. Root must record database/security disposition before implementation; no review or runtime success is claimed by planning.

**Root design/storage disposition (2026-09-08):** choose UID-only per-device private Firestore documents and host callable polling, superseding listener/session-map alternatives. Storage reviewed per-UID documents to avoid shared-document contention. Heartbeat15s, lease45s, transactionally accepted write at most once per15s per UID, host poll10s; approximately one-minute disconnect delay. Callables read room/member for auth/active/closed checks and write only the actor lease, never game/members/projections. Host-only polling returns sanitized current device roster data with server-time anchor. Existing catch-all rules deny the private path; no new browser rules or RTDB. Security confirmation of this final callable variant is pending root's recorded disposition; implementation remains behind that prerequisite.

**T-12.1 final boundary disposition (root, 2026-09-08):** Security confirms the final private per-UID lease plus host-only callable polling design has no blocker. Storage per-UID recommendation is incorporated. Implementation authorized; retain D-12 missing-lease unknown semantics (expired observed lease disconnected), exact payload/auth/active/open checks, transactional throttle, bounded roster reads and no game writes. Invocation/read-cost limiting is outside the timer throttle guarantee; preserve App Check. T-12.1 complete.

### Implementation evidence — 2026-09-08

Forge completed T-12.2–5. Local WebSocket transport now authenticates immutable canonical host/player actors, aggregates player sockets by UID, sends tagged host-only snapshots, pings every15s, expires liveness after45s, and excludes manual/audience actors. A signed audience capability remains read-only and receives game projections without entering the presence roster. Firebase uses private per-UID lease documents, exact payload validation, transactionally throttled player renewals, bounded host-only reads after host authorization, and the existing deny-by-default Firestore rules.

The host UI labels device connection independently from lobby readiness, keeps manual capsules `بدون بازر`, invalidates to unknown on offline/foreground/stale data, and preserves the host/player capability when a display tab acquires its separate audience capability. Firebase polling rejects disposed, foreground-invalidated, and over-30-second delayed responses using request generations and monotonic elapsed time. Local foreground refresh waits for an echoed authority snapshot rather than comparing server time to the device clock.

Verified checks: `npm run test:game` (32 passing), `npx vitest run tests/app/host-lobby.test.tsx` (12 passing), `npx vitest run tests/app/firebase-presence-adapter.test.tsx` (1 passing), `npm --prefix functions test` (27 passing), `npm --prefix functions run build`, `npm run typecheck`, `npm run lint -- --quiet`, `npm run build`, and `git diff --check`. Root real local browser evidence is in `output/playwright/player-presence-t12/`: multitab, last-close, reconnect, manual N/A, responsive widths, offline-to-unknown, and five-minute client-clock foreground refresh all passed.

`PLAYWRIGHT_CHANNEL=chrome npm run test:e2e:firebase` ran the private callable/rules test and actual same-auth browser two-tab lifecycle: one-tab close advanced the lease from the surviving tab, last close reached host `غير متصل`, and reopening reached `متصل`; it also exercised an actual wall-clock lease expiry in the prior run. The final suite process exited nonzero because the unrelated existing full reconnect/game/correction scenario exceeded its 90-second test timeout after reaching `ROUND_SETUP`; the three T-12-specific emulator tests passed. No timeout was changed to hide that result. Root/Gate acceptance remains pending.



## T-13 — Complete bundle intake to production Firestore (2026-09-08)

User explicitly authorized writing all bundle questions to production `huroof-a3ee7` and completed Firebase CLI login. This task is additive private source intake, not approval or gameplay activation. Existing T-12 work remains preserved.

- Source: `huroof_everything_available_bundle.zip`; initial inventory: `tmp/bundle-import-20260908/inventory.json`.
- Owner: root scope/status; Forge importer, parser, tests and execution evidence; Database advisory impact review; Gate production-write review.
- Destination: `questionImports/{bundleSha256}` manifest and private question/source subcollections. Existing deny-all rules apply; no rule, runtime pointer, catalog or immutable game-release changes.
- Scope: recursively inventory nested archive formats, retain all distinct question revisions, collapse identical mirrors with provenance, keep source category IDs and review status, retain media references with source archive identity. Never invent approvals, remap unsupported categories, or execute bundled code.
- Dependencies: CLI authenticated account; database disposition and integrated Gate review before production writes.
- Sequence: parse deterministic local intake artifacts; classify every candidate source file and report failures; focused parser/dedup/path tests; read-only target check; review; idempotent create-only production batches; read back all imported records and verify counts/hashes.
- Acceptance: all question-bearing files accounted for, no silently dropped parse errors, deterministic IDs, historical/source metadata preserved, exact readback hashes and counts, active release unchanged, retries cannot overwrite differing documents, no credentials in files/output.
- Risks: historical duplicates/revisions, media dependencies, unsupported categories, API costs from document count. Report exact counts before write. Limit batch sizes and document bytes.
- Rollback: imported records are isolated and inert. No destructive rollback is authorized; any future removal must be scoped to this exact bundle import with a verified manifest. Existing gameplay needs no rollback.
- Exclusions: factual/human approval, gameplay activation, new game categories or mode implementation, media publication, unrelated user edits.
- Status: Implementation complete — ready for review. Production import remains blocked on integrated Gate approval.
- [x] T-13.1 Deterministic parser/intake and reconciliation report. Evidence: `scripts/bundle-question-intake-parser.py`; `tmp/bundle-import-20260908/bundle-question-import.report.json` (replacement SHA `58c9164fde3b159d3e2a25ef9db70b22a3e9e14dc5f49866b24d426c6d10f610`, 18,693 question revisions, 60 blueprints, 60,626 occurrences, 2,269 source files, zero unresolved issues). Gate repair accounts for 2,850 PSV/charades rows and 60 formerly omitted v16/v17 sample-card occurrences.
- [x] T-13.2 Focused tests and pre-write implementation. Evidence: `scripts/bundle-question-importer.ts`, `tests/bundle-question-importer.test.ts`, `tests/bundle-question-intake-parser.test.py`; parser tests and `npx tsx --test tests/bundle-question-importer.test.ts` passed, deterministic seed-17/932 rebuild hashes match, and importer `plan` dry run passed. `npx tsc -p tsconfig.node.json --noEmit` remains blocked by unrelated existing `server/service.ts` event-map error. Integrated Gate review remains required before T-13.3.
- [ ] T-13.3 Production import and full readback verification.
- [ ] T-13.4 Root acceptance and handover evidence.


**Root T-12 final acceptance (2026-09-08):** M-11 Done; technical Gate PASS after audience capability, display-key preservation and clock-skew repairs. Root independently verified final local room D52690: actual multitab/last-close/reconnect, manual N/A, +5-minute device clock foreground refresh, host offline unknown and recovery, same-tab display socket and original capability preservation. Final320/390/1440 screenshots are readable after capsule wrapping correction; 200% zoom/reduced-motion smoke has no overflow. Evidence: output/playwright/player-presence-t12/root-final-results.txt, root-results.txt and root-*.png. Firebase actual surviving-tab lease advancement, last-tab expiry and reconnect passed; Gate independently reran callback lifecycle regression1/1. AC-12.7 full-suite green status remains explicitly unclaimed because the broader game/correction test timed out at90s; root accepts the bounded presence feature with this recorded verification limitation. No T-12 production deployment or data deletion. Active T-13 work is separate and preserved.



### T-13 production execution evidence — 2026-09-08

Partial import due to confirmed HTTP429 RESOURCE_EXHAUSTED; live billingEnabled=false. All 18,698 question-source revisions and 60 picture blueprints were successfully written and fully compared to the exact prepared plan. Also stored: 1,192 occurrence records. Total19950 verified documents;61713 planned child metadata/provenance documents remain, plus the manifest. The complete import must NOT be marked Done. No active-release pointer existed before or afterward; manifest remains absent.

Evidence: `tmp/bundle-import-20260908/production-partial-verification.json`, `quota-blocker.json`, `production-apply.log`, and `gate-preproduction-pass.md`. Final plan documents SHA950ff2d57e071279a4f2f46f32342bfacf9f4204a3c891ee9aee8848d2862c4c. Root independently ran parser3/3, uploader4/4, and Node typecheck (pass after credential adapter correction). Initial custom Admin credential attempt failed before writes; underlying Google Cloud Firestore authorized-user adapter succeeded with existing CLI auth.

Next authorized action: after billing/quota allows completion, rerun the same pinned importer apply command with the same report/Gate receipt; exact verified existing docs are skipped, missing metadata created, full readback repeated, manifest created last. Billing changes are not performed by this task. Source-question storage is fulfilled; metadata/manifest completion remains blocked by quota. No gameplay activation or approval occurred.


## T-14 — Playable content, image delivery, and deployment readiness (2026-09-08)

### Outcome and authority

User requests: make the games playable and prepare the app for deployment, following T-13 source intake. The user requested the planning phase on 2026-09-08. This plan extends the existing canonical documentation; it does not reset T-11/T-12 accepted work or T-13 immutable intake evidence.

Authorized outcome: a tested application whose offered category/mode selections are backed by real, compatible, reviewed question inventory, whose image questions display their actual media, and whose production release/deployment artifacts have reproducible verification and rollback. All source material stays preserved. A stored historical revision, blueprint, template or unreviewed draft is not evidence of a playable question. Production rollout needs the final exact release identity, review evidence, working infrastructure and technical Gate before activation. Billing/account changes remain user-controlled.

### Current evidence

- Bundle and duplicate ZIP both SHA-256 `7afd1423925b68e11f3f0cab5253879154763994272203b1a4df8007f9cd31ec`; 18 nested archives. Exact prepared intake documents SHA `950ff2d57e071279a4f2f46f32342bfacf9f4204a3c891ee9aee8848d2862c4c`.
- Production T-13: all 18,698 source-question revisions +60 blueprints +1,192 occurrences stored and exact-content verified (19,950 total); 61,713 remaining children plus manifest are quota-blocked. `tmp/bundle-import-20260908/production-partial-verification.json` and `quota-blocker.json` are the authority. Earlier 18,693 counts predate the documented five-acting-revision parser repair; preserve them as history.
- 240 actual v18 question images (60 each in source categories 011/012/014/061), 10 review sheets, one preview; rebuild supplies 60 briefs and zero replacement images. Actual binaries have not been uploaded by T-13. `image-inventory.json` is evidence.
- No production `runtime/activeRelease`; v3.3 canonical release artifact layout and genuine review/trust evidence are absent. Authoring folders exist for multiple categories and are not approved release files.
- Current v3.3 requires exactly 62 category files, each with 300–336 approved items (`src/question-bank-v3.3.ts:950`), plus selected-pack reserve validation. Source categories 063–100 are not automatically mapped to runtime IDs. Category001 has a documented natural-letter inventory blocker; never restore its invalid artificial prefixes.
- Local baseline on 2026-09-08: `npm run typecheck`, `npm run build`, `npm run test:game`, `npm --prefix functions test` passed. Build warns about a >500kB vendor chunk. `npm run test:ui`:84 passed/2 failed of86; failures are game-board round markers and setup insufficient-coverage recovery. The previously recorded Firebase full-game timeout remains unresolved release evidence, even though focused presence checks passed.
- Source audit: Hosting lacks an SPA rewrite; public/host question projections omit media; QuestionReveal receives text only. Production runtime selection, Auth/App Check and media delivery need live verification. Existing CLI release path exists; staged admin handlers do not constitute a functioning human review workflow.

### Decisions and blockers

| ID | Decision / disposition | Owner and affected tasks |
| --- | --- | --- |
| D-14.1 | First release scope: recommend reviewed available packs with explicit per-pack availability; user choice pending versus retaining full62x300 completion before launch. The fixed v3.3 scope/count contract must not be silently weakened. If available-pack launch is selected, introduce an explicitly versioned release scope policy preserving review, integrity and playback checks. | User; T-14.3/4/6 |
| D-14.2 | Preserve immutable intake and all historical versions. Curated gameplay uses a deterministic latest-effective-version/patch reconciliation ledger; exclusions and replacements remain traceable. | Root; T-14.3 |
| D-14.3 | Keep classic/image letter rules and separate charades semantics. Categories with inadequate natural coverage become combined-only or unavailable according to an explicit policy and successful selection simulation. Do not invent answers, prepend words, or relabel arbitrary trivia as charades. New gameplay modes/board rules require a separate user decision if evidence proves necessary. | Root proposes; user for rule changes; T-14.3/4 |
| D-14.4 | Genuine authenticated review, source/evidence qualification and image-rights receipts remain required. No synthetic human identities/approvals/signatures. Confirm a real reviewer/trust issuer and implement the missing narrow review path if none exists. | User/reviewer and Forge; T-14.4 |
| D-14.5 | Media delivered only for the authorized current question, using immutable content-hash identity and short-lived authorized access. Preserve default-deny raw Storage/intake. No permanent public download tokens, answer-bearing filenames/alt text, review sheets or evidence payload in game projections. | Security/Database review then root; T-14.2 |
| D-14.6 | Production target fixed to huroof-a3ee7/(default)/me-central2, emulator variables rejected. Firebase build must be explicit, not accidental fixture success. Use Node22 deployment parity. Do not commit credentials or silently change billing. | Root; T-14.1/5/6 |
| D-14.7 | Billing currently disabled. Firestore resets around midnight Pacific (next reset around2026-09-09 10:00 Kuwait), but remaining intake needs at least four free daily allowances; deploy-service eligibility is a separate prerequisite. Resume production writes only when actual quota permits. | User/infrastructure; T-14.5/6 |
| D-14.8 | Reuse existing approved visual direction and controls. No brand redesign, generated replacements or new photographs are included. Existing240 images are the media scope; missing requested photographs need their own sourcing/rights task. | Root; T-14.2 |

### Milestones and task ownership

One Forge writer owns each implementation milestone end-to-end, runs its checklist and updates only assigned evidence/checklist state. Root owns scope, decisions and Done. Read-only Security/Database reviews target changed boundaries; technical Gate runs at material production/auth/media/release boundaries and final readiness, not after every small edit. No recursive delegation.

#### M-READY-01 / T-14.1 — Runtime baseline and deployable routing

- **Owner:** Forge; root acceptance. **Dependencies:** current baseline; independent of D-14.1 and production billing. **Modules:** firebase.json, production build/preflight script and package scripts, runtime-selector/client config validation only as necessary, tests/app/game-board.test.tsx, tests/app/setup-route.test.tsx and narrowly implicated source, existing Firebase E2E harness.
- **Sequence:** reproduce each failing UI assertion against current accepted behavior; distinguish actual regressions from stale tests; fix with behavioral assertions. Add Hosting index fallback with real nested-route navigation verification. Add a fail-closed explicit production build/preflight checking intended project, Firebase runtime, emulator=false, required public config/App Check, release identity availability and Node22 requirements. Diagnose the recorded full Firebase game timeout to an actual code/test cause; do not only raise timeout.
- **Acceptance:** both failing UI scenarios pass with intended behavior; all existing UI/game/Functions tests pass; direct host/player/audience links and refresh serve app; production preflight rejects fixture/emulator/missing-config builds; a real Firebase emulator host+player+audience game covers start, buzzer, judgment, round completion/correction and reconnect without the recorded timeout.
- **Checks:** focused regressions, typecheck, UI/game/Functions tests/build, Firebase E2E, production dry-run build and Hosting emulator/preview deep-link checks. Report any environment conflicts without terminating unrelated user processes.
- **Risk/rollback:** small reviewed changes only; retain accepted T-11/T-12 behavior, match rules and fixtures. Revert only milestone-owned changes if needed; preserve user work.
- **Implementation evidence (2026-09-08):** Reproduced the two historic UI failures as stale assertions: stage score presentation intentionally has no round markers, while the host score card retains the five-marker round history; fixture setup recovery now tests the current error mapper. Added Hosting SPA fallback and `scripts/test-hosting-deep-links.ts`; the Hosting emulator served `/host/new`, lobby, host, play and display direct links with the app shell. Added `scripts/production-preflight.ts`, its unit coverage and `build:production`; it fail-closes on the fixed target `huroof-a3ee7`/`me-central2`, fixture or emulator input, absent Firebase/App Check/release identity, and non-Node22. The Firebase full-game timeout was caused by cached Firestore projections being treated as action-authoritative after reload; `ProjectionEnvelope.authoritative` now carries Firestore cache metadata, cached UI remains visible but Ready/Start remain disabled until a server-confirmed snapshot, and the E2E sends one Start then waits for `ROUND_SETUP`. No server or Functions behavior and no timeout were widened. Verified: `npm run test:ui` (14 files, 88 tests), `npm run test:game` (33 tests), `npm run functions:test` (28 tests), `npm run typecheck`, `npm run build` (existing >500 kB chunk advisory), `npm run test:hosting-deep-links`, `npx tsx --test tests/production-preflight.test.ts tests/firebase-hosting.test.ts` (3 tests), and `npm run test:e2e:firebase` (4/4 in 1.7m, including full host/player/audience game). `npm run preflight:production` intentionally rejected this workstation: Node24 instead of Node22 plus absent production public Firebase/App Check configuration and active immutable release identity/root SHA; it performed no production read/write. Exact future dependency: a Node22 environment with that public configuration and a verified immutable `FIREBASE_ACTIVE_RELEASE_ID`/matching SHA; `runtime/activeRelease` is currently absent. The system-Chrome visual run exposed the current intentionally hidden audience `.question-band` during `CELL_SELECTION`; this stale visual capture control is retained for T-15.4/6 rather than treated as a runtime regression.
- **Status:** Done — root accepted baseline 2026-09-08.

**Root acceptance:** reviewed the production-input guard, SPA rewrite, cached/server projection metadata and one-click authoritative Start wait. Reproduced Firebase adapter regression tests 2/2. Forge full UI88/88, game33/33, Functions28/28, typecheck/build and Firebase4/4 evidence supports baseline acceptance. Production build rejection is expected until actual Node22/config/release prerequisites exist; no production readiness claim. The hidden audience question-band visual assertion remains an explicitly assigned T-15.4/6 check, not a runtime baseline regression.

#### M-READY-02 / T-14.2 — Actual image assets and role-safe gameplay rendering

- **Owner:** Forge; Security/Database pre-implementation disposition; root integrated acceptance and Gate. **Dependencies:** T-14.1 baseline, D-14.5/8; local extraction/tests independent of billing.
- **Modules:** new bounded bundle-media importer/manifest under content/question-media or existing canonical media layout; Functions media service/current-question projections; shared runtime projection contract; local/fixture parity where used for tests; GameRoutes/QuestionReveal or adjacent question-media component; scoped CSS; media/rules/UI/E2E tests. Do not change global public Storage access.
- **Sequence:** safely read all240 binaries from nested v18 ZIP, reject traversal/links, preserve originals in an isolated versioned asset directory, verify checksums/decodable format/dimensions and reconcile every media reference. Build mapping from source record to immutable media identity with rights/source metadata; hold missing/invalid assets. Implement authorized current-question media grant/delivery and projection type. Render image alongside text with explicit loading/error/retry states. Provide real fixture/emulator media for end-to-end image game.
- **Acceptance:** all240 actual question images accounted for in asset manifest; review sheets/preview/briefs distinguished; every offered image question resolves the correct asset; host/audience/player visibility follows question lifecycle and the existing audience toggle; charades hidden target remains host-only; wrong member/room/question requests and expired grants rejected; no answer in filename/alt/projection. No upload is claimed based solely on local extraction.
- **Checks:** archive path/hash/decoding tests, media association tests, role/expiry/room-binding tests, Storage/rules tests, real image E2E (each of four categories), mobile and desktop screenshots including load failure/reduced-motion. Production upload/readback happens in T-14.5.
- **Risk/rollback:** image leakage, cross-room access, orphan objects, stale mapping. Stage immutable objects, activate mapping with release only; rollback to prior frontend/Functions and release pointer, retain isolated artifacts for audit.
- **Implementation evidence (2026-09-09):** `scripts/extract-v18-question-media.py` safely validates the fixed bundle SHA, nested member paths, every v18 media checksum and PNG header/dimensions; it wrote 240 originals only under `content/question-media/v18-private-240/` and a deterministic manifest (`7c4efdc47b3ba74964ac7b7324de86a23d1adea9e4b7799cf5de6b3a57c14f95`). Root independently decoded all240 PNGs and verified their hashes/dimensions (`tmp/bundle-import-20260908/root-media-decode-check.json`), while public assets were separately shown not to contain bundle hashes. The renderer mounts only for a connected authoritative host/audience projection in an allowed lifecycle, clears on room/binding/role/toggle/connection change, restores route-specific local capabilities after reload, refreshes valid grants before expiry, rejects mismatched/expired grants, retries image decode failures, and revokes late object URLs. Local delivery rechecks a ticket bearer and verifies manifest hash/PNG bounds before bytes; Firebase requires exact media ID/hash, immutable flag, fixed private object name and numeric pinned generation. The release builder emits a local-only 240-object create-only upload plan and permits immutable release media documents only from verified uploaded generations. Verified: extraction tests (3); local image byte/auth test across 011/012/014/061 (43/43 game tests); renderer grants/route authority component tests (4/4); Functions build; typecheck; Storage rules emulator; and Firebase emulator media auth fixture test plus immutable-metadata negatives. The real Firebase browser capture passed and wrote [host desktop](../output/t14.2-media/host-image-1440x900.png), [host 320px](../output/t14.2-media/host-image-320x568.png), [audience desktop](../output/t14.2-media/audience-image-1440x900.png), and [audience 320px](../output/t14.2-media/audience-image-320x568.png): all use an actual private PNG, assert natural dimensions, `object-fit: contain`, no horizontal overflow, and no audience answer text. The focused player-tabs Firebase presence lease reproduction also passed without a timeout change. Production upload/readback and activation remain T-14.5/6 and are blocked by the documented billing/Functions state.
- **Status:** Done — root accepted with scoped local Gate PASS.

**Root acceptance, 2026-09-09:** T-14.2 is Done for local media integration. Root independently decoded all240 PNGs and verified hashes/dimensions, reproduced final component4/4, and inspected all four desktop/320px host/audience captures. Independent Gate PASS: `tmp/bundle-import-20260908/t14-media-gate.md`. Original Firebase8/9 run is preserved; focused capture and presence retries each passed1/1 without changing presence source or timeout. Production upload/activation and source-pack coverage remain separate; this does not approve all historical source revisions for gameplay.

### VISUAL CONTRACT for T-14.2 — copy verbatim into the Forge handoff

Mood/concept: reuse approved مدار الحروف Arabic game studio and existing host/audience/player treatments; image questions are functional extensions of the current question surface.
Focal/scan order: current question image and prompt first, gameplay action/buzzer second, score/round context third; the host's answer remains in its existing private answer area.
Density:4–5 of10; one media panel with breathing room, no gallery or administration chrome in play.
Desktop/mobile: contained image preserving full aspect ratio beside/above the prompt on desktop and above it on320px+ mobile; never crop meaningful puzzle/lineup content; preserve touch controls and no horizontal overflow.
Typography: existing IBM Plex Sans Arabic roles and sizes; natural RTL text, no tracking, no text embedded into newly generated assets.
Palette/surfaces: current approved tokens, navy/cobalt surfaces and established semantic team colors; neutral media backdrop, no new aesthetic system.
Spacing/shapes: existing4px spacing scale, existing question-card radii and control sizing; image frame remains subordinate to the question.
Icons/imagery: use the actual verified240 assets only; neutral image labels that do not reveal answers; review sheets and redesign briefs never appear as playable images.
Interaction/states: accessible loading, loaded, failed/retry and unauthorized/hidden states; image visibility follows authoritative role/lifecycle/audience toggle; keyboard-accessible retry, polite error announcement, no automatic answer reveal.
Motion: no new image entrance animation; respect reduced motion and existing text reveal/pause behavior.
Anti-patterns: stretched/cropped puzzles, answer-bearing alt text/filenames, public permanent media tokens, fake placeholders presented as real assets, a new design direction, board/score/control redesign.

#### M-READY-03 / T-14.3 — Curated gameplay corpus and coverage report

- **Owner:** Forge; root data-policy decisions; Database review of mapping. **Dependencies:** D-14.1/2/3 and T-14.2 media manifest. Artifact analysis may start before production quota is restored.
- **Modules:** new source-preserving reconciliation/curation scripts and tests; existing question-bank schemas/policies only under explicit versioned decision; content category/media mapping and compatibility reports; setup availability projection.
- **Sequence:** inspect history/patch manifests and establish deterministic latest-effective records; never concatenate historical snapshots as unique gameplay questions. Build explicit source category index/title crosswalk, especially063–100 versus unrelated local authoring IDs. Validate required fields, mode, literal answer/Arabic normalization, accepted answers, evidence/rights presence, duplicates/conflicting revisions and media. Simulate every offered category/mode/difficulty selection through the actual runtime board/charades selector. Output eligible/held/superseded counts and exact reasons per record/category. Resolve category001 natural-coverage policy through D-14.3.
- **Acceptance:** every source-question revision maps to a traceable effective candidate or explicit hold/superseded disposition; no silent drop, fabricated fact or forced letter; numeric/nonletter answers are not injected into letter-only queues; every offered pack can start and complete required rounds with reserves without leaking/repeating excluded answers; unavailable selections have truthful actionable Arabic UI. All100 source category indexes are accounted for even when not eligible for runtime publication.
- **Checks:** golden merge/patch order, collision and duplicate tests, category crosswalk ambiguity tests, strict schemas, runtime selection simulations across all offered pack combinations/modes/difficulties and rematches; materialization deterministic across repeated runs.
- **Risk/rollback:** wrong historical version, conflated category ID, underfilled board. Keep new interpretations versioned and source immutable; rollback by discarding only unactivated generated candidate output.
- **Implementation evidence (2026-09-09):** `scripts/reconcile-bundle-question-revisions.ts` reads the pinned source JSONL (`950ff2d57e071279a4f2f46f32342bfacf9f4204a3c891ee9aee8848d2862c4c`) without mutating it, reconciles all18,698 revisions and60,636 occurrences, and writes source-preserving [JSON evidence](../tmp/bundle-import-20260908/t14.3-reconciliation.json) plus a concise [Markdown summary](../tmp/bundle-import-20260908/t14.3-reconciliation-summary.md). It uses parsed semantic question/answer/accepted-answer fields as fallbacks; records `patch-N-…` occurrence entry names as filename evidence only and holds apparent precedence pending patch-manifest confirmation; and relevant question, answer, evidence, and media fingerprints prevent corrected records from being collapsed. The crosswalk records all100 source indices separately from the existing62 runtime IDs: 4,066 source associations occur in063–100, while the 28 exact absent indices are reported as having no source question record. The regenerated artifact records 832 technical effective candidates,16,797 held,1,069 exact duplicates and zero superseded records; the 45 apparent filename-precedence relations are held rather than superseded, and every row remains `releaseApproved:false`. Actual runtime selectors ran723 category/mode/difficulty simulations: legal category-board pairs/chunks yielded205 technical passes while letter/charades and other packs record their exact reserve/availability holds. These are technical candidate results only, so no setup availability or approved pack is claimed. Verified: five focused tests covering semantic fallback, patch precedence, crosswalk/charades separation, exact collision/media/evidence preservation, strict field/mode validation and shuffled-input determinism; `tsc -p tsconfig.node.json --noEmit`; artifact count/hash/coverage assertions; `git diff --check`.
- **Status:** In progress — reconciliation artifact is complete; authentic patch-manifest confirmation, curation/review, D-14.1 mapping/availability decision, and reserve-sufficient curated packs remain external dependencies.

#### M-READY-04 / T-14.4 — Genuine review and canonical release preparation

- **Owner:** Forge for tools/integration; actual authenticated humans/rights reviewers for decisions; root acceptance. **Dependencies:** T-14.3, D-14.1/4 and real reviewer/trust identity.
- **Modules:** existing review adapter/admin review endpoints narrowly required, canonical policies/evidence/receipts/trust loader, versioned release builder/publisher compatibility, auth/security tests. Broad staged admin UI completion is excluded unless needed for this review path.
- **Sequence:** establish actual authorized reviewer identity/qualification and external signing/trust configuration; do not store private keys in corpus. Implement missing authenticated review path with immutable revision/content/evidence bindings and replay protection. Present genuine curated records/media to authorized reviewers, capture actual decisions. If user selects available packs, introduce an explicit new scope policy/version accepted by both builder and runtime while preserving receipt/trust/security guarantees. Materialize exact canonical approved files, inventory/media/report and immutable release identity; dry-run existing guarded publication path.
- **Acceptance:** no draft becomes approved through migration or AI assertion; all published rows have required real receipts and valid evidence/media; changed revisions invalidate old approvals; stale/wrong-project/trust/hash/nonce input rejected; zero required findings in the exact recomputed report; dry-run release has complete playable inventory and excluded records remain in private intake.
- **Checks:** reviewer auth/role/qualification/expiry and receipt replay tests; deterministic canonical report; release build/verify tests; exact runtime validator parity; Security/Gate for changed approval boundaries.
- **Risk/rollback:** fabricated approval or scope-policy bypass. Fail closed on missing genuine review; revoke only invalid new review configuration and retain audit history. Time to obtain human reviews is unknown and cannot be promised as agent execution time.
- **Status:** Planned; blocked for final release by missing authentic review/trust evidence and D-14.1.

#### M-READY-05 / T-14.5 — Production infrastructure and immutable upload

- **Owner:** Forge deployment preparation, user billing/account prerequisites, root reviewed production operations. **Dependencies:** T-14.1/2 artifacts; sufficient quota/billing; T-14.4 for approved-release data.
- **Modules:** existing intake resume, new reviewed media uploader, production build/preflight, Firebase config/secret references, evidence under project-local tmp/output and deployment docs.
- **Sequence:** verify live project/database/location/billing, Hosting, Storage, Functions services, Auth providers/domains, App Check app/domain configuration and least-privilege service identity. Resume exact T-13 plan without duplicating existing records; finish readback+manifest. Upload missing immutable240 media objects with checksum/mapping readback and create-only retries; no production source object overwrite. Prepare approved release through existing identity-pinned plan/prepare/verify flow. Verify Node22 and production build Firebase/emulator flags and required secret bindings without exposing values.
- **Acceptance:** T-13 manifest complete; actual required media available through reviewed delivery path; release data prepared/verified but not falsely called active; project/auth/AppCheck and budget prerequisites documented; repeat run is no-op or creates only missing identical objects. Existing rooms/intake/game history preserved.
- **Checks:** exact remote object/document hashes and counts, injected partial upload retry, deployed rules verification, real valid/invalid Auth/AppCheck probes on intended environment, known absent activeRelease preserved until T-14.6.
- **Risk/rollback:** quota, billing, mixed project, partial publication. Stop on RESOURCE_EXHAUSTED; bounded retries, truthful partial receipts, no finished marker early. Staged objects/releases are isolated; no deletion required. Do not enable billing without the user's action/explicit authorization.
- **Status:** Planned; production writes currently quota-blocked.

#### M-READY-06 / T-14.6 — Full game verification and deploy-ready handover

- **Owner:** Forge evidence, root integration/acceptance, Gate material release review. **Dependencies:** T-14.1–5; D-14.1 resolved; exact release approved/verified; infrastructure ready.
- **Sequence:** integrated UI/game/Functions/content/rules/build checks; run actual host/player/audience flows for classic,image,charades, desktop/mobile, QR/deep links, start/buzz/judge/pause/resume/reconnect, round/match completion and rematch, all offered pack coverage. Assess abuse controls for room creation with transactional limits before public rollout. Prepare rollback commands/previous identities; record exact Hosting/Functions/rules/artifact versions. Root/Gate verify release-critical evidence. Final production rollout/activation is a distinct exact-artifact operation under user authorization, followed by live smoke/audit; readiness alone is not claimed as deployment.
- **Acceptance:** no red required test or known full-game timeout; all visible categories/modes accurately reflect approved playable inventory; each of four image categories renders actual media; unauthorized actions/media and cross-room access denied; no fixture/emulator production build; rollback is runnable and does not delete history; exact active pointer and deployed artifacts checked if rollout occurs. Never label all18,698 historical revisions as distinct playable questions.
- **Checks:** full required suite once integrated, targeted bug regressions, Firebase/Hosting E2E, responsive/keyboard/reduced-motion review, rate-limit concurrency tests, artifact hash pinning, independent Gate and live release audit/smoke if activated.
- **Risk/rollback:** public broken game or answer leakage. Keep preceding verified Hosting/Functions/release identities; use existing guarded pointer restore/rollback-none only when its actual prerequisites hold. Do not activate on failed Gate or missing human review.
- **Status:** Planned.

### Execution checklist and next authorized action

- [x] T-14.0 Read-only readiness audit and baseline checks; record actual blockers.
- [x] T-14.1 Runtime/deep-link baseline and explicit production build validation — Implementation complete — ready for review; evidence above.
- [x] T-14.2 Verified240-image pipeline and role-safe rendering — Implementation complete — ready for review.
- [ ] T-14.3 Deterministic curated corpus/crosswalk/coverage and truthful availability — reconciliation artifact in progress; release availability remains held.
- [ ] T-14.4 Genuine review integration, canonical approved release and dry-run.
- [ ] T-14.5 Infrastructure checks, exact intake resume, media/release upload and readback.
- [ ] T-14.6 Integrated full-game verification, independent Gate and deploy-ready handover.

Next implementation handoff: T-14.1 only, after returning from the user-requested planning phase. Preserve all mixed work. It is independent of billing and the first-release scope decision. Later milestones must honor their entry dependencies and genuine review/product decisions; no full-release completion claim until exit evidence exists.

## T-15 — Category game, flow and whole-app polish (2026-09-08)

**Status:** Review and planning complete; T-15.1–6 implementation planned, not started. **Owner:** root owns canonical scope, decisions and acceptance; Forge owns each assigned implementation milestone. **User request:** Astra medium review and a new plan for thematic/aesthetic/animation polish, proper game flow, category-and-number cells, category replacement when unanswered, and numbered Huroof letter replacement when unsuccessful; cover admin, display, room entry and waiting room.

**Evidence:** [Current source/rendered review](APP-POLISH-REVIEW.md). Astra medium reviewed source and a local entry/setup/lobby/host/display walkthrough; root inspected lobby/display screenshots. Admin was source-reviewed. No full test suite, mobile acceptance or Firebase success is claimed by this planning phase.

**Existing authorities and dependencies:** preserve accepted T-08–12 and immutable DESIGN-DNA/0006/0007. T-14.1 remains owner of the baseline UI/Firebase-timeout/deep-link repairs; T-15 consumes its evidence and does not duplicate or mark it complete. T-14.2 owns real image delivery; later T-14 tasks own genuine content approval, production infrastructure and release. This new user-requested game kind extends the earlier T-14.3 statement that new board rules need separate scope. It does not weaken content approval or authorize billing, deployment, or live schema migration.

**Decision status:** D-15.1–7 below are explicit proposed implementation defaults, not invented human selections. The requested game kind and failure replacement are in scope; occurrence-number meaning, minimum two selected categories, failure timing and depletion behavior must be confirmed as the implementation contract in T-15.1. Review/planning completion does not claim feature completion.
## Proposed product rules and decision register

These are proposed defaults for implementation planning, not claims of user selection where wording was ambiguous. Root should record durable D-15 IDs and resolve consequential deviations before implementation.

- **D-15.1 / game kind:** add `gameKind: huroof | categories`, independent of `mode: classic | fast | custom` (pace/setup preset) and `modality: classic | image | charades` (question presentation). Missing gameKind in legacy rooms reads as huroof. Avoid overloading classic. First category delivery supports classic question presentation; image is enabled only after T-14.2 media parity and explicit supported-combination tests. Charades remains its separate existing flow; do not silently feed it to category or letter boards. Lock kind and category scope when creating a room; rematch copies settings into a new match. The existing classic/fast/custom timing truth remains unless separately changed.
- **D-15.2 / numbered category labels:** category mode keeps the exact 25-cell topology and team connection win condition. Cells show selected category + a per-category occurrence number, e.g. `كرة القدم [1]`, `الفن [2]`, `كرة القدم [2]`. Number means a unique visible occurrence for that category within the round, not points, difficulty, question ID or board coordinate. Keep immutable coordinate cellId and store categoryId/label/occurrence separately. Allocate initial occurrences deterministically in board coordinate order; later replacement gets the next unused number for its new category, never renumber other cells or reuse a label in the same round. Reset counters on a new round. Show a short “الرقم لتمييز خلايا الفئة” hint in rules/setup. Category names must actually be on cells, with line wrapping and full accessible names.
- **D-15.3 / initial category distribution:** at least two distinct playable selected categories are required to promise a genuinely different category on failure. Allocate 25 slots evenly across eligible selections (difference at most one where capacity permits), seed/shuffle positions server-side. Selected catalog presence is not playable capacity: only approved/pinned release questions and distinct unused concepts count. If a selected category cannot support its allocated cells/reserves, show exact coverage guidance before room creation, rather than borrowing an unselected category. Decide a supported maximum (reuse current maximum ten unless category-label evidence requires less).
- **D-15.4 / wrong versus terminal:** initial JUDGE_INCORRECT grants the other team its existing opportunity on the same question; do not rotate then. Opponent incorrect, opponent-window timeout, or no buzz during the initial window resolves the question as failed. FIRST_ANSWER currently stops its clock and waits for the host judgment; retain that unless a separate answer timer is explicitly designed. Pause, disconnection and reload are not question failures. Stale/replayed judgment must have no extra consequence.
- **D-15.5 / terminal replacement:** on a terminal failure, close buzzing, consume the question/concept, record one resolution event, and keep the cell unowned with no score. Retain the failed question/answer in a bounded result presentation until the host chooses Continue; at that point atomically commit replacement and return to CELL_SELECTION. “Retry” must never reopen that disclosed question. Category cells change to another eligible selected category; numbered Huroof surprise cells retain their stable 1–9 tile number and rotate to a different eligible letter. For Huroof show the number as identity and the current revealed letter as the main value; on return the number can become primary again until selection, but the letter replacement must persist server-side. Existing ordinary letter cells keep their letter and draw a fresh question on the next visit. This interpretation avoids extending the user's numbered-cell request to every visible letter.
- **D-15.6 / pool depletion:** do not repeat a consumed question or answer concept, silently widen category scope, select unavailable content or pretend replacement occurred. Require a different eligible category/letter; if none exists, put the match into an explicit content-exhausted hold, with no owner/score mutation and an explanation to the host. Do not disable the cell and continue silently, as that could make connection victory impossible. Offer a reviewed end-without-winner or new-match action; keep room state recoverable. Content exhaustion is distinct from timer/no-answer failure. Select/replacement commit must include reservation/consumption atomically so concurrency cannot oversubscribe reserves. Finite pools cannot guarantee unlimited failures; startup coverage must say what it guarantees. Preserve release pinning through rounds and reconnection; no live release swap to evade depletion.
- **D-15.7 / privacy and compatibility:** public projections may expose current category/occurrence and current revealed letter, never next hidden replacement content, accepted answers, sources, moderation or queues. Keep old schema rooms playable under their versioned policy or reject unsupported new clients explicitly; no destructive migration of live rooms. New rules require a room policy/schema discriminator. Corrections alter ownership/outcomes under existing audit rules; they do not rewind consumed questions or secretly restore previous labels.

## Compact visual contract — carry verbatim in the implementation handoff

Mood/concept: preserve human-approved مدار الحروف for استوديو الحروف, a cobalt architectural Arabic game studio with raised ice-white real board cells and calm readable task surfaces; immutable 0007 supersedes stale flat clauses in DESIGN-DNA. Focal/scan: entry current joining/creating decision then CTA; lobby code/share then roster/readiness then Start; host board/current question/timer then moderation console; player instruction then tactile buzzer; audience board/question/timer with green physically left and red physically right; admin title/status then filters, records and bounded editor actions. Density: entry/lobby4, setup/host5, audience/player3, admin6. Composition: desktop max1200px task shell, two-column setup and two-team lobby, frontal live board with fixed side scores, 16:9 audience safe stage; mobile one-column entry/lobby with reachable CTA, compact scores above board, one-thumb player, contained table overflow only for admin. Typography: IBM Plex Sans Arabic700 display/letters/scores,600 actions/headings,400–500 body; no Arabic tracking, phone body16px, tabular scores, bidi-isolated room codes/numbers. Palette/surfaces: approved --spatial-* cobalt/ultramarine, deep-blue ink, opaque ice-white zones, cyan focus/action; crimson/emerald remain team semantics; decorative background stays outside text zones. Spacing/shapes: existing4px scale,24–64px section rhythm,44px targets,12px controls and16–20px principal panels, restrained shadows and no nested-card clutter. Imagery/icons: approved spatial WebPs, authentic DOM/SVG board and rights-cleared category covers with fallbacks; one labelled outline family; category names plus occurrence numbers remain readable in actual cells. Interaction: rest/focus/pressed/busy/disabled reason/error/empty/stale/offline/reconnect/success explicit; legal server actions and truthful permissions unchanged. Motion:100ms press,200ms state, finite600–800ms entry only where useful, finite existing near-win/win feedback, short replacement transition keyed to authoritative resolution revision, static reduced-motion and no replay on reconnect. Anti-patterns: no new brand/concept, no neon/glass/rainbow/gold, no ambient loops, no moving live targets, no mirrored board geometry, no fake players/metrics/content, no screenshots baked into controls, no tiny long category labels, no auth bypass or technical publication promises.

## Proposed milestone sequence and task details

All milestones below are Planned; the present authorization is review and plan. Root assigns implementation only after this handoff, following active workflow. Suggested owner for implementation: Forge end-to-end; root owns scope/acceptance; technical Security/Database reviews before relevant boundaries and material Gate at integrated high-risk completion. Every task updates only its canonical task evidence/status, not immutable design history.

### M-POLISH-01 / T-15.1 — Lock rules, baseline and compatible domain design

Outcome: an unambiguous rules matrix and an executable known baseline before the cross-runtime feature. Scope: D-15.1–7, current state/event table, protocol/version transition, mode combination matrix, selected-category capacity and failure history design. Affected: canonical plan/DECISIONS/FEATURES, lifecycle/board/contracts/selector tests (initial failing acceptance fixtures when implementing), existing runtime and E2E harness. Dependencies: T-14.1 evidence for Firebase timeout/deep links; source review can precede its completion. Exclusions: duplicate T-14 repairs, deployment, content approval, billing work. Sequence: verify dirty baseline; resolve rule decisions; document additive persistence/compatibility; review database/security impact; refresh stale visual/join selectors under owning baseline task; record checks honestly. Acceptance: every existing and new phase has legal events, timeout behavior, role output and next action; legacy rooms are explicitly covered; startup and depletion policies are testable; no ambiguous bracket number. Checks: narrow domain/service/Functions baseline suites, app typecheck, explicit Firebase flow result linked from T-14.1. Risk/rollback: no live migration; retain old schema reader and isolate new creation behind capability/version until parity evidence.

### M-POLISH-02 / T-15.2 — Authoritative category boards and failure replacement

Outcome: both game kinds work through selection, opposing attempt, terminal failure, replacement and win in local and Firebase authority. Scope/modules: `domain/board.ts`, `domain/lifecycle.ts`, `runtime/question-selector.ts`, `runtime/contracts.ts`, `server/service.ts`, `functions/src/game.ts`, `functions/src/index.ts`, all three adapter implementations and projection serializers, narrowly related persisted schema validation. Dependencies: T-15.1 decisions/reviews; T-14.1 baseline. Sequence: typed kind and cell metadata with legacy defaults; mode-specific queue allocator; deterministic distribution/occurrence counter; atomic failure resolution and reservation; public/host projection shaping; adapters; compatibility guards. Do not use DOM text as game state. Acceptance: exact25-cell topology and original axes; example category labels persist across reconnect; first wrong does not rotate; terminal category failure rotates category exactly once, surprise failure rotates letter exactly once with tile number intact; fresh questions/concepts only; same seed/events give same normalized outcomes in both authorities; stale/replayed intents score/consume once; pool exhaustion holds truthfully; legacy Huroof and separate charades regression passes. Checks: new reducer/selector table tests; differential local/Functions event traces with fake clock; service websocket race tests; Functions tests/build; focused projection privacy tests; Firestore emulator rules if persisted paths/shapes change; typecheck game/app. Risks: queue consumption transaction bounds, drift between duplicated service code, old-client unsupported metadata. Rollback: stop new-kind creation using reviewed capability; preserve new-room readers and audit history; do not downgrade live category rooms into letter boards or undo consumed pools.

### M-POLISH-03 / T-15.3 — Setup, room entry and waiting room

Outcome: players and hosts understand the two games, enter reliably and know the next step. Modules: `setup-options.ts`, `EntryRoute.tsx`, `NameJoinRoute.tsx`, `HomeSurface.tsx`, setup/lobby portions of `GameRoutes.tsx`, relevant CSS and UI tests. Dependencies: T-15.2 protocol, approved0007 visual contract; T-12 presence retained. Sequence: separate game-kind selector from pace/modality; selected-category summary and real coverage; code/deep-link/name recovery; compact share/roster/start layout; phase-aware role navigation. Acceptance: huroof default, category choice survives URL/setup/create/reconnect; invalid combinations explained; bracket hint present; 320px and390px no document overflow; Start remains discoverable with clear readiness blocker; manual participants distinct from online players; offline/unknown presence never says connected; room-code errors preserve input and focus; one next primary action. Checks: entry/name/setup/host-lobby tests, local create/join with two separate players, keyboard/touch walkthrough, screenshots1280×720/390×844/320×568, axe. Risks: breaking accepted T-11 URL sequence or T-12 presence; rollback only scoped route/CSS delta, preserve established runtime capabilities.

### M-POLISH-04 / T-15.4 — Host, player and audience presentation with restrained motion

Outcome: new rules are legible and actionable on phones and a projected screen. Modules: `game-board.tsx`, `QuestionReveal.tsx`, `GameRoutes.tsx`, `game-state.ts`, CSS/tokens only where approved; board/question/buzzer tests and E2E. Dependencies: T-15.2–3; T-14.2 only for image combination. Sequence: category/number layout and accessible labels; active question/category/timer hierarchy; failed-question summary + explicit Continue/replacement; phase-driven host actions/player instructions; exact event-keyed animation; results/rematch/correction states. Acceptance: full long Arabic category names available, bracket IDs distinguish duplicates, category visible in cell at supported widths, board hit targets align at200% zoom, no hidden letter/answer leak, audience question toggle respected, no timer jitter, opposing chance visually distinct, reduced-motion static, reconnect does not replay celebrations, no animation delays buzzer/adjudication. Check at1280×720 and1920×1080 display plus mobile/tablet host/player; long text, image error, correction, reconnect and exhaustion states; visual/a11y tests and finite-animation assertions where behavior matters. Risks: overloaded cells and label truncation, unnecessary animation state, reduced hit area. Rollback: retain protocol and cell metadata, revert only view styling/effects; never remove category labels from an active category room.

### M-POLISH-05 / T-15.5 — Admin inventory, filters and editing polish

Outcome: Arabic operators can inspect actionable content health and complete edit/review tasks with clear capability boundaries. Modules: `features/admin/AdminRoutes.tsx`, `admin-service.ts` only if needed, admin CSS/tests; related callable query schemas only if justified and reviewed. Dependencies: T-15.1 contract; final category readiness view depends on T-15.2. Sequence: named Arabic inventory metrics with actual source meaning; per-resource columns/status/detail summary; explicit loaded-page filter and cursor controls; category picker and modality-aware editor/source/validation groups; unsaved/conflict recovery; editable review-change reason; refresh controls and bounded pending states; actual category-game readiness from pinned playable data where supported. Acceptance: no raw-key metrics as primary labels; no JSON dependency for ordinary workflow; loaded-only search unmistakable (or server filtering verified across pages); forbidden/unavailable/busy/empty/error/conflict/success work; draft preserved on conflict; disabled/restricted mutation controls reflect session capabilities, server remains authoritative; review and publication status never fabricated; category covers fall back; no unsaved draft loss on ordinary navigation. Checks: authenticated emulator admin workflow, scoped role tests for viewer/editor/reviewer, save-conflict preservation, pagination/filter behavior, a11y and responsive screenshots. Risks: expanding visual polish into new permission or indexing behavior. Rollback: keep callable contract; revert UI delta without discarding drafts or changing roles. No production auth, grants, signing or release activation in this milestone.

### M-POLISH-06 / T-15.6 — Entire game-flow acceptance and human walkthrough

Outcome: one integrated evidence set proves both modes across role/device/runtime boundaries. Dependencies: T-15.2–5 plus T-14.1; image tests additionally T-14.2. Modules: `tests/game`, `tests/app`, `tests/e2e`, Functions tests, canonical QA/route-state/handover evidence. Sequence: run shared scripted fixtures in local and Firebase emulator; run independent host/two-player/audience contexts through readiness → start → correct → wrong/opponent → no answer → rotation → reconnect → pause/resume → correction → round win → subsequent rounds → two-consecutive/three-total match win → results/rematch; inspect public payloads; repeat adverse traces for stale simultaneous buzz, replayed resolution, insufficient pools and legacy rooms; targeted full regression/build; rendered human walkthrough of four requested surfaces. Acceptance: all legal states have a way forward, public payloads remain safe, outcome/ownership/score agree, no stranded failure screen, no borrowed out-of-scope content, source-working tree matches evidence, all required tests pass or concrete blockers remain open. Required checks: typecheck/lint/scan:visual/test:content/test:ui/test:game/build, Functions build/tests, Firebase E2E/rules where applicable, visual/a11y route matrix; capture failures as evidence rather than widening timeouts to hide them. Root integrated acceptance and material technical Gate assess implementation correctness; AI aesthetic comments are advisory. Human review verifies the actual entry, waiting room, audience and admin experience. No new three-concept gate is needed for polish within approved0007; a direction change would require the repository's new-concept process. Risks/rollback: unresolved Firebase race or inaccessible category cells blocks completion; roll back scoped feature activation while preserving room readers and evidence. Production deployment remains under T-14/release authorization, not implied by this plan.

### T-15 execution authorization and contract — 2026-09-08

The user authorized implementation of the whole T-15 plan start to end and verification of proper game flow. This supersedes the preceding planning-only authorization statements. Root adopts D-15.1–7 as implementation defaults, including per-category occurrence numbers, 2–10 playable categories, opponent opportunity before terminal replacement, and truthful depletion hold. [GAME-KIND-CONTRACT.md](GAME-KIND-CONTRACT.md) records the state/event matrix, compatibility, consumption and rollback contract. No separate preference confirmation is required to begin the authorized work.

T-15.1 is in progress: current worktree and active profile verified; Graphify trace and source read; database/security persistence and projection reviews running. T-14.1 baseline implementation is assigned to Forge and remains separately owned; it supplies the prerequisite evidence rather than duplicating repair work. T-15.2 follows after baseline and review constraints are satisfied. Later T-15 milestones are authorized in sequence, subject to their technical entry requirements. Production deployment and billing remain outside T-15.

### T-15.1 acceptance / T-15.2 execution — 2026-09-08

T-15.1/M-POLISH-01 Done: rules/state contract and reference lock recorded; database/security constraints accepted in T15-RUNTIME-REVIEW.md; T-14.1 baseline accepted as above. T-15.2/M-POLISH-02 is in repair verification, assigned as one cross-runtime Forge milestone via .agent-runtime/t15-runtime-handoff.md. It adds category/Huroof policy validation, deterministic pinned category boards, reservation-safe terminal replacement, truthful exhaustion recovery, actor-bound receipts, projection privacy, and Firebase persistence parity. Evidence: [T-15.2 verification](../output/t15.2-verification.md) (historical suite evidence plus current repair checks; Firebase final result pending capture). Root owns acceptance. Remaining T-15.3–6 are authorized after their dependencies; no feature completion claim yet.

### T-15.2 acceptance / T-15.3 execution — 2026-09-09 Kuwait

T-15.2/M-POLISH-02 Done after root review/repairs: [acceptance evidence](../output/t15.2-root-review.md), [definitive Firebase6/6 exit0 log](../output/t15.2-firebase-root.log), [tracker](T15-EXECUTION.md). T-15.3/M-POLISH-03 now proceeds under the authorized full plan using `.agent-runtime/t15-entry-handoff.md` and the unchanged approved VISUAL CONTRACT. Forge owns entry/setup/waiting-room polish and its checks; root owns rendered acceptance. Later T-15.4–6 remain authorized in dependency order.

### T-15.3 acceptance / T-15.4 execution — 2026-09-09 Kuwait
T-15.3/M-POLISH-03 Done: [root rendered/live acceptance](../output/t15.3-root-review.md) and [verification](../output/t15.3-verification.md). Final lobby QR omission repaired and actual trigger/generation/dismiss checked. T-15.4/M-POLISH-04 proceeds via `.agent-runtime/t15-gameplay-handoff.md`, which carries the unchanged approved visual contract and scoped gameplay/display/motion checks. T-15.5–6 remain authorized in order.

### T-15.4 acceptance / T-15.5 execution — 2026-09-09 Kuwait
T-15.4/M-POLISH-04 Done after root readability/modal/reconnect repairs and live acceptance ([root review](../output/t15.4-root-review.md)). T-15.5/M-POLISH-05 now proceeds through `.agent-runtime/t15-admin-handoff.md` with unchanged verbatim visual contract and actual authenticated baseline/role evidence. T-15.6 remains mandatory for full-source checks, remaining lint/visual harness repairs, both-runtime complete flow and material Gate.

### T-15.5 acceptance / T-15.6 execution — 2026-09-09 Kuwait
T-15.5/M-POLISH-05 Done after root admin14/14/full typecheck, repaired-source review and authenticated history/mobile evidence inspection ([root review](../output/t15.5-root-review.md)). T-15.6/M-POLISH-06 now proceeds through `.agent-runtime/t15-acceptance-handoff.md`, including actual both-kind rematch defect, stale test/visual scan repairs, complete local/Firebase four-context matches, all required checks, root review and material technical Gate. User authorization remains full T15; production release prerequisites and human aesthetic approval are not inferred.

### T-15 final root acceptance — 2026-09-09 Kuwait
T-15.1–6/M-POLISH-01–06 Done. Root integrated review and final technical Gate PASS close the rematch identity, terminal retry, depleted-content preflight and held-control findings. The final [Gate report](../output/t15.6-gate-review.md) preserves initial failures and independent closure. Applicable post-repair verification: Functions31/31, Firebase8/8, full typecheck/scoped lint/build, focused UI21/21 and independent held-state UI4/4. Earlier unaffected verify/content69/UI111/game42/scanner4/local2/rendered2/Axe4/rules evidence remains applicable, as recorded in [execution tracker](T15-EXECUTION.md). [Walkthrough](../output/T15-WALKTHROUGH.md) presented; human visual feedback is unrecorded. No deployment or additional T-14 content/media/release scope is implied.


### T-13 daily resume — 2026-09-09

Quota reset confirmed through successful writes. Reviewed bounded importer created and exact-readback verified19,000 additional provenance records;19,950 existing records matched and were skipped. Total38,950 verified;42,713 planned children and completion manifest remain. No active-release change or gameplay activation. Evidence: tmp/bundle-import-20260908/production-resume-20260909.receipt.json and production-resume-20260909.log; scoped Gate gate-quota-resume-pass.md. Importer tests5/5 and Node typecheck passed. Today's write allowance deliberately leaves a1,000-write margin. Do not repeatedly rerun today: preflight would consume additional read quota. Future completion needs billing or a separately reviewed checkpointed audit because a complete81,663-document scan exceeds the daily free50,000-read allowance. No full-import Done claim.


### T-14 execution authorization — 2026-09-09

User said "i validate, do it now" after being told private intake was not playable. Record this as the user's explicit content publication/activation approval for supplied bundle SHA7afd1423925b68e11f3f0cab5253879154763994272203b1a4df8007f9cd31ec. This chat authorization is not a fabricated specialist qualification, per-item authenticated review receipt, or image-rights attestation. Proceed with engineering/publication preparation without asking the same approval again; only material unresolved policy or actual external blockers require user action.

Current T15 execution tracker reports all milestones accepted, preserving polished game kinds and runtime. T14.1 baseline/deep-links has been implemented by that coordinated work. Next writer assignment is T14.2/M-READY-02 image pipeline under existing visual contract. Root live read2026-09-09 verifies billingEnabled=false and Cloud FunctionsAPI403disabled; production service deployment is still blocked, independently of user approval. No backend activation can truthfully be claimed until required services and verified release exist.

### T-14.3 mapping completion — authorized 2026-09-09

User request: “map everything”, following the request to show only playable categories. This authorizes source-grounded mapping of the full supplied category inventory, including indexes 063–100. It does not turn private imported revisions into an approved active release.

- Scope/owner: Forge implements one source-preserving category crosswalk milestone; root owns acceptance. Research establishes exact bundle authority; Database reviews ID compatibility and mapping impact.
- Current evidence: app catalog has 62 identities and 8 drafting/ready entries. Existing reconciliation accounts for 100 source indexes but leaves 063–100 unmapped. Fresh read-only production evidence in `../tmp/bundle-import-20260908/category-mapping-db-check.json` confirms three source hashes and HTTP404 for `runtime/activeRelease`; prior exact verification covers all18,698 imported revisions.
- Sequence: extract authoritative category registry and provenance; resolve explicit aliases while retaining conflicts; implement versioned crosswalk and integrate reconciliation/catalog lookup where supported; regenerate deterministic mapping/coverage artifacts; run focused mapping, collision, category-filter and type checks; root review.
- Acceptance: every supplied category identity has a source-cited mapping or a precise evidenced unresolved disposition; no numeric-only alias coercion, dropped source revisions, changed imported hashes, or activation by status relabeling. Existing identities and available-only setup behavior remain compatible. Report exact added/mapped/conflicting counts and release limitations.
- Exclusions: production schema or import mutation, historical receipt rewrites, synthetic approval, and activation of unverified content. If source evidence resolves all mappings, complete them without further confirmation; only genuinely ambiguous identities require user input.
- Rollback: mappings and generated reports are additive/versioned; revert only this milestone's local changes. Private database imports and active-release state remain untouched.
- Checks: authoritative manifest extraction, collisions/ambiguity/missing-source handling, deterministic output, preserved source-count/hash accounting, existing setup/category regression tests and app/Node typecheck as affected.
- Status: Done for this source-mapping subtask — root accepted 2026-09-09. The broader T-14.3 curation/release prerequisites remain open. Forge generated the pinned 100-entry source registry and additive mapping artifacts: 18,412 of 18,698 revisions resolve to an exact source identity, including 4,066 source-only `huroof-*` revisions; 0 are ambiguous; 286 remain explicitly unresolved across 10 immutable-provenance groups; 28 registry entries are blueprint-only. No runtime/release/status activation changed.
- Implementation evidence (2026-09-09): [source registry](../content/source-catalog/huroof-everything-v1.json) pins archive `7afd1423925b68e11f3f0cab5253879154763994272203b1a4df8007f9cd31ec`, all direct source file H1/ID pairs, and 62 exact legacy runtime targets while preserving 38 source-only targets. [mapping reconciliation](../tmp/bundle-import-20260908/t14.3-mapping-reconciliation-v1.json) and [unresolved mapping evidence](../tmp/bundle-import-20260908/t14.3-unresolved-source-mappings-v1.json) are deterministic on regeneration (SHA-256 `56fdc9a1e255d15fbc2f5242b6aed4f89784b29f5a6c8b472828c131965eae22` and `60b35ee98f94b233e186c2aee064d5d8d64b767d0384ed2a750b7ab3b70bd7d8`). Verified: source/mapping tests 9/9; category/setup UI tests 21/21; scoped ESLint; app and Node typechecks. Contradictory category-shaped identifiers suppress scalar source/runtime targets and remain conflict evidence; bare numbers, titles, and runtime aliases do not map.
- Root acceptance evidence (2026-09-09): independently regenerated all100 registry entries from the pinned source bundle with byte-identical output; reran8 core registry/reconciliation tests successfully, reviewed conflict suppression and metadata-only registry boundaries. Forge additionally passed the unresolved-report test (9 mapping tests total),21 setup/category tests, scoped lint and app/Node typechecks. Source mapping is accepted with286 explicit unresolved-source holds; gameplay release is not activated.

### T-14.3 local database question connection — authorized 2026-09-09

User explicitly requests connecting database questions to the game after source mapping alone did not change setup. Implement a local, server-only connection to the existing private imported question bank for the already-explicit demo/test flow. Production approved-release semantics remain unchanged.

- Owner: Forge implementation, root acceptance, Security boundary review. This extends existing T-14.3 runtime/availability integration; no new release policy or database schema.
- Scope: server-side read-only authenticated database import adapter, deterministic source-mapped normalization/deduplication, local runtime injection/cache, metadata-only setup availability derived from actual usable inventory and game kind. UI continues excluding unusable categories. Clearly identify imported draft/test content; never relabel it approved. Existing demo fixtures remain explicit test fallback, never silent fallback on DB failure.
- Impact/rollback: reads only production huroof-a3ee7/(default) private import; no writes, pointer activation, rules changes, or credential exposure. Startup/config can disable the new source and restore prior local source. Preserve existing rooms by pinning inventory identity or failing explicitly when unavailable; don't silently swap questions mid-game.
- Sequence: inspect provider/selector and source fields; security review proposed boundary; implement bounded import reads with hash/source identity verification and fail-closed errors; select truthful categories from actual mode/coverage; test runtime integration, projections and failure paths; run local database-backed smoke and root review.
- Acceptance: local server demonstrably uses database-read source records; setup reflects imported usable categories for selected kind; real database-source question reaches a host/player-safe game flow; unrevealed answers, accepted-answer lists and raw intake never reach setup/player/audience; preserve the existing intentional terminal-failure canonical-answer reveal; non-demo approval checks unchanged; missing letters/unsupported modes/conflicting records excluded or explicitly held; no fabricated text, mode, letter or status. No production release claim.
- Checks: mocked DB normalization/hash/cache/failure tests; category/Huroof coverage; source/category identity tests; metadata/projection privacy and route scope; app/Node typecheck, focused UI/game tests, real bounded read-only DB/local smoke. If actual inventory cannot support a mode, show explicit mode-specific unavailable feedback rather than expand by guesses.
- Implementation evidence (2026-09-09): `LOCAL_DB_QUESTION_SOURCE=firestore-import` performs server-only, read-only exact Firestore `getAll` batches for the hash-pinned bundle. The bounded live inventory read 1,364 candidate records, normalized 1,184 source-grounded text records, explicitly held 180 unsupported image-mode records, exposed 29 metadata-only categories (12 source-only), and verified pooled Huroof coverage. Snapshot identity binds normalized question content, normalizer version, registry schema and bundle hash; restored/mismatched rooms fail closed. Local DB setup requires the trusted loopback source and explicit demo mode; non-demo approval behavior remains unchanged.
- Checks (2026-09-09): `npm run typecheck`; `npx vitest run tests/app/setup-route.test.tsx` (19 passing); `npx tsx --test tests/game/local-firestore-question-source.test.ts tests/game/service.test.ts` (25 passing); live read-only Firestore smoke at `127.0.0.1:8797` through Vite `5199`, including inventory, Huroof room start, host-safe question projection and audience answer exclusion. Evidence: `tmp/bundle-import-20260908/root-db-game-smoke.json`.
- Status: Done — root accepted local database connection on2026-09-09. Production release activation and unsupported-content preparation remain separate.
- Root final evidence: restarted DB authority8797 and Vite5199 with matching DB-mode flag; independent Huroof and source-only category-board (`huroof-068` + `huroof-076`) HTTP smoke reached `QUESTION_READING`, matched imported question source and excluded audience answers. Receipts: `../tmp/bundle-import-20260908/root-db-game-smoke.json` and `../tmp/bundle-import-20260908/root-db-game-smoke-categories.json`. Fresh browser displays29category cards, exact legacy covers and imported-test notice. Root provider tests2/2 and production build passed (existing vendor chunk warning). Security review found no blocker for the documented loopback launch. Forge reported19UI+25server/provider tests and app/Node/game typechecks passed; existing React act warnings remain.

## T-16 — Local master-bundle reconciliation — 2026-09-09

T16.7 follow-up accepted: the requested local game connection reads SQLite intake and preserved baseline through the existing snapshot/selector boundary.6213 explicit test questions across58 categories;600 records remain staged. Root verified live imported-source Huroof/Categories questions, terminal-failure replacement, answer secrecy and browser launch. [Runtime handoff](../output/master-import-20260909/RUNTIME-TEST-REPORT.md). No stored data/approval change.
Done — root accepted2026-09-09. User-authorized validated6540-record stream imported into the actual local preview store, with verified backup, preservation,240 media associations and zero-duplicate rerun. [T16-MASTER-IMPORT.md](T16-MASTER-IMPORT.md) records scope/rollback and [final report](../output/master-import-20260909/IMPORT-REPORT.md) contains source/category accounting and independent evidence. Stored6813, approved/playable0; existing280 demo records unchanged. No production target or historical intake modified. Forge T16.2–5 and root/Gate T16.1/4/6 complete.

## T-17 — Goal videos and image questions — 2026-09-11
User-approved implementation: [canonical task details, decisions, checks and rollback](T17-QUESTION-MEDIA.md). T17.1 media/data and T17.2 runtime/UI are Done; T17.3 implementation/deployment preparation is accepted after final Gate PASS. Online rollout remains blocked by Firebase authentication and actual reviewed release inputs. Local and Firebase-emulator image/goal flows are verified; no production activation is claimed.
