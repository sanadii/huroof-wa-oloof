# App release — 2026-09-18

## Scope and authority
User requests full verification and publishing current app updates to GitHub and Vercel. Vercel preview only. Preserve unrelated dirty files and private content. Do not publish raw data, credentials, output reports, or local environment files. Existing design and content release authorities remain unchanged. The original branch advanced remotely by 26 commits; after a rejected non-fast-forward push, this snapshot was published safely to `codex/app-release-2026-09-18` without overwriting or merging those commits.

## R1 — verification and packaging (Done)
- Owner: root coordinates; implementation agent repairs verified typecheck/UI failures.
- Evidence: initial full typecheck failed TS2790 in prompt-metadata script; UI suite 147 passed, 26 failed across six files.
- Sequence: classify failures, repair scoped regressions/tests, run typecheck/UI/Functions/build, inspect release file list and secret exposure, then independent release gate.
- Acceptance: build and relevant suites pass; remaining warnings explicitly reported; package contains public app assets and all required source dependencies, no private datasets or tokens.
- Exclusions: new content imports, schema/rules changes, production-site promotion.

## R2 — publish (Done — separate release branch and preview)
- Owner: root after independent gate.
- Commit intentional app/source/assets groups only; inspect staged paths before commit. Push current feature branch to origin.
- Use linked Vercel project `huroof-wa-oloof`, team `sanad-general`, preview environment. Verify deployment status through Vercel CLI.
- Reconcile pending same-category/paused-end Firebase changes only after verified compatible source and release review; no database content mutation.
- Acceptance: record commit SHA, push result, preview URL/status and backend deployment scope.
- Rollback: preserve prior commit/deployment identity; revert scoped commit or restore previous deployment only if needed and authorized. No force-push/reset.

## Handover
Verification evidence (2026-09-18):
- Clean index-export checkout: typecheck, production Firebase build, and all 173 UI tests pass. No local environment file was exported; exact debug-token scan returned zero matches.
- Game tests: 55 pass. Functions build/tests: 48 pass, one private-media fixture skipped.
- App-scoped lint passes. Initial broad workspace lint failed in separate content-import tooling; unrelated raw imports are excluded from this release.
- Browser smoke: Firebase home and host setup load, with the approved category catalog rendered. No full multiplayer live match was simulated.
- Production dependency audit: zero vulnerabilities; install reports 16 moderate development-tool vulnerabilities, not upgraded in this release.
- Private intake, reports, local environments and private question assets are excluded; design/tokens.css remains available to the build.
- Eight gameplay functions deployed successfully to huroof-a3ee7/me-central1. Firebase CLI then returned exit 1 solely because artifact cleanup policy is unset; no retention policy was changed.
- Local-server pause/end parity was corrected and tested.
- Content suite: 72 pass. Clean-checkout game tests require excluded private fixtures (46 pass, nine fixture-dependent failures); full workspace game suite passes all 55.
- Independent release Gate: PASS for Firebase/Vercel preview. Final clean-checkout app/server/selected-tooling lint and typecheck pass.
- Two owner-release tooling tests cannot run: missing historical private `D:/firebase-activation-20260911/approval-question-backup.json` (ENOENT), not an app/runtime assertion failure. No content publication was performed.

GitHub: pushed `d4aa33e7` (app/source/artwork/tests) and `81296e58` (root-only private data deployment exclusion) to `codex/app-release-2026-09-18`. Integration with the advanced original branch remains separate work; no force push/rebase/reset was performed.

Vercel: first build failed because the unanchored data exclusion also hid src/data. Corrected to /data/. Final deployment `dpl_FDAjpAqx92GEKhqesUajD48oeMdi` independently inspected as Ready, target preview, at 2026-09-18 16:05 Asia/Kuwait: https://huroof-wa-oloof-4t2z57wky-sanad-general.vercel.app . It uses the public Firebase production configuration with debug token blank, static preview false, emulators disabled. No live multiplayer session was exercised on the deployed preview; domain-dependent authentication/App Check remains a live smoke-check limitation. Production website promotion is outside the default preview scope.

Release exclusions and unresolved work: the original remote branch's 26 concurrent commits were not merged into this snapshot; unrelated workspace docs/import tooling remain untouched. Historical tooling fixture failures and incomplete long-running supplemental tooling checks are not claimed green. The selected app runtime/build checks above are the verified evidence. Backend container artifact retention remains unset.
