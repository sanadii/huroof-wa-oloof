# T-17 — Goal videos and image questions — 2026-09-11

Authority: user explicitly approved implementation locally and online. Root owns scope and acceptance; Forge is the single implementation writer. Preserve the mixed working tree and existing draft/release authorities. This extends the canonical implementation plan and milestone register.

## Contract and decisions

- D17.1: 99 existing single-shot goals, soft blur option 1 (`gblur=sigma=2.5:steps=2` before resizing), crop identifying captions as in preview; paired muted H.264 fast-start MP4 around 854 pixels wide. No padding/replay. Target <500 KB each, maximum 1 MB. Files/Storage objects hold bytes; DB holds opaque bindings and metadata.
- D17.2: Category `من سجل الهدف؟`, stable IDs, caption-grounded scorer answers/source timings. Arabic names reviewed without invented identities. Include verified existing image questions; hold missing/invalid images truthfully.
- D17.3: Shared host-only REVEAL_ANSWER stops timer/buzzing without scoring, persists per question, broadcasts answer/clear media to host and visible audience display. Idempotent; reset next question. Existing private host answer preference remains separate. Clear binding unavailable before reveal.
- D17.4: Local SQLite import is transactional/idempotent with backup and preservation evidence. Production uses existing reviewed immutable release process; no silent promotion of drafts. Online deployment authorized, subject to actual infrastructure/content prerequisites.
- D17.5 (integration decision, 2026-09-11): Preserve the exact-62/minimum-300 V3.3 base contract. Add a separately reviewed 99-goal media supplement and compose one immutable release with the existing single audited active-release pointer. Bind the verified base release/root, signed supplement review commitments, category catalog, goal questions and paired immutable media into the composite root. Preserve existing trusted-review authority checks; caller-supplied signatures/trust cannot invent approval. Reject collisions, missing reviews, stale hashes or absent Storage generations before publication. Goal questions are category-mode only. Preparation/verification must distinguish missing external reviewed inputs from any still-unimplemented publisher support.
- D17.5a (Gate repair clarification): Composite publication requires an already prepared and exactly verified audited base, which need not be active. Verify its immutable root, publication/provenance receipt, complete inherited review-nonce commitments and existing global claim ownership; do not reclaim inherited nonces. An offline base-plan file alone is insufficient. Older receipts lacking the required provenance fail closed for composite preparation while their existing non-composite runtime behavior remains unchanged. Bind the actual loaded review report hash to the signed supplement's report commitment.
- VISUAL CONTRACT: Reuse approved DESIGN-DNA.md and selected C Arabic game surface. Existing question area remains focal point; scan prompt, contained media, answer/action. Density 4–6. Desktop uses existing host/display layout; mobile stacks media beneath prompt without horizontal overflow. Preserve current Arabic typography roles, cobalt/ice/cyan surfaces, spacing, corner shapes, and existing icons. Media uses object-fit contain, muted inline playback once with replay/play fallback. Loading, retry, unavailable, disabled and focus states match existing controls. No new decorative motion, autoplay loop, full-screen takeover, redesigned board, or cropped image content.

## M-MEDIA-01 / T17.1 — Media package and local data

Owner Forge; prerequisites approved plan, database review before DB writes. Scope exporter/importer, private asset manifest and question records, SQLite only. Sources: resources/100-best-goals-2026-clips/single-shot/clips.json; existing photo manifests and SQLite draft rows. Existing DB baseline must be read fresh (planning read 7,253 rows; may change).

Sequence: inspect records and naming -> implement deterministic export/validation -> 198 assets -> source-name review -> backup -> dry run -> transaction -> exact readback/rerun. Metadata includes variant, checksum, content type, dimensions, duration, byte size, private path and source provenance. No schema migration unless separately justified. Existing records preserved.

Acceptance/checks: all 198 assets decode, timing pairs match, captions absent, sizes under ceiling; inspect representative all-goal contact sheets; source-to-answer audit. Integrity check, backup receipt, all prior row bytes/timestamps preserved, second apply no changes. Report all generated paths and largest/total bytes. Rollback removes only unchanged newly inserted IDs using receipt, never whole-DB restore over newer work.

Status: Done — root accepted 2026-09-11. Independent [root check](../output/t17-root/media-data-check.json) confirms all 198 hashes/size targets and exact preservation of 7,253 prior rows/timestamps. Root inspected all four contact sheets. Largest file 258,811 bytes; total 16,851,825 bytes. Full-decode and zero-write-rerun evidence reviewed.

Implementation evidence (2026-09-11): local-only package at ignored `content/question-media/goal-quiz-2026/` contains 198 opaque-name paired MP4s and 99 stable `goal-quiz-2026-001..099` draft records in category `goals-2026`. [Validation report](../output/t17-media/MEDIA-VALIDATION-REPORT.json), [import receipt](../output/t17-media/IMPORT-REPORT.json), [rerun receipt](../output/t17-media/RERUN-REPORT.json), [backup manifest](../output/t17-media/BACKUP-MANIFEST.json), and four contact sheets verify 198 full decodes, 99 timing pairs, 0 assets over 500 KB, source-caption answer aliases, a 7,253-row baseline / 7,352-row result, preservation of all prior row bytes and timestamps, and a zero-write rerun. The receipt's exact ID/data hashes permit only targeted rollback of still-unchanged new rows; no whole-DB restore is authorized.

## M-MEDIA-02 / T17.2 — Runtime, protected delivery and UI

Owner Forge; depends T17.1 accepted. Modules: shared game contracts/selection; local source/service/routes; Firebase game/media/release code; UI/media adapter/styles/tests. Security review on intended boundary before changes and integrated review after implementation.

Sequence: compatible media types/bindings -> registry resolver -> verified source/inventory selection -> authoritative reveal in local/Firebase -> phase-bound grants -> UI image/video playback and answer action -> tests/browser evidence. Support PNG/JPEG/MP4, old PNG bindings, media-aware room snapshot hashing and selection. Category mode mixes supported modalities; Huroof still requires valid letters. Avoid exposing private paths, answer filenames, clear assets or answer metadata pre-reveal. Preserve immutable release binding and role restrictions.

Acceptance: local host/display goal and image flows work, clear reveal synchronized and reset-safe, no automatic scoring, no timer/buzzer after reveal, reconnection preserves phase, cleanup on question/disconnect, retry works, legacy text/PNG flows pass. Checks: focused import/selection/service/UI/Functions tests, authorization stale/wrong-phase/wrong-role tests, typechecks/builds, live host/display browser evidence desktop/mobile. Root repeats a load-bearing check. Rollback compatible code plus targeted new imports; no deletion of existing records.

Status: Done — root accepted after repairs, independent Gate PASS and live host/display desktop/mobile verification on 2026-09-11. [Browser evidence](../output/t17-root/browser-check.json), [Gate regressions](../output/t17-gate/final-focused.log), [legacy disclosure probe](../output/t17-gate/final-legacy-probe.log). Restored failed-question prompt remains visible after refresh; prompt precedes media. Final Forge game52/Functions36 and restored UI16 checks pass. Production remains T17.3.

Implementation evidence (2026-09-11): local SQLite runtime now accepts 99 goal-video records and all 291 manifest/hash-verified V18/rebuilt image bindings, while 60 remote-only image links remain held. Category selection mixes image/video/classic questions; letter selection remains modality-specific. Local and Firebase projections bind media to an active occurrence and disclose clear answer media only after host-only `REVEAL_ANSWER`; stale occurrence, player role, hidden audience, old prompt binding, and malformed media IDs fail closed. Local byte delivery reauthorizes both ticket issue and byte read against the current projected binding. Firebase callable delivery checks immutable generation/checksum/content type and returns authenticated in-memory bytes as a blob-compatible data URL instead of Storage URLs. UI renders contained muted inline video with retry/cleanup and a separate shared answer action; CSP permits only local/blob/data media. Verified: full game suite (50), focused UI suite (5), Functions suite (34), app/node typechecks, Functions build, game lint, and production client build. An isolated SQLite-backed server displayed 88 categories with `من سجل الهدف؟` ready and selectable on the category board; direct CUA submission was rejected by the intentional local-origin guard, so root's browser review remains the final live host/display check. No production mutation or release promotion was attempted.

Repair evidence (2026-09-11): source-pinned local inventory labels now create category rooms for imported goals; host/audience shared reveal emits answer text and clear binding only after a nonempty exact occurrence match; legacy rooms with absent occurrence fields are unrevealed. All reopen paths retain a stopped buzzer after reveal; current-media authorization denies CELL_SELECTION for every role. Firebase byte delivery validates immutable PNG/JPEG/MP4 content type, object root, generation, checksum and magic; the actual rebuilt JPEG fixture passes authorization/delivery. Same-binding video grant refresh preserves its Blob source, while a clear variant starts fresh. Root live browser verified local blur-to-clear reveal, next-question reset, and an actual image. Prompt now precedes contained media on host and display. Verified repair checks: 52 game, 36 Functions, 6 focused UI; typechecks and production build pass.

## M-MEDIA-03 / T17.3 — Online release preparation and rollout

Owner Forge implementation, root integration; depends T17.2 accepted and required release Gate. Recheck Firebase billing/Storage/Functions/App Check/active release and Vercel target with read-only tooling. Historical blockers in DEPLOYMENT.md are not current evidence. Resolve immutable catalog lookup against exact release; preserve signed content review requirements.

Sequence: preflight -> verified private upload and immutable metadata -> reviewed release preparation -> compatible backend/rules -> verified activation/audit -> Firebase-mode Vercel deployment -> online role/room/media smoke. Publish only scoped source delta; preserve existing user work, no broad staging. Source/media/data/receipts excluded from public packaging as appropriate.

Acceptance: exact cloud object checksums/generations, verified release pointer, online goal/image host/display flow and new-question reset; production never represented by static fixture preview. Gate required for release/privacy. If external infrastructure/review prerequisites block, complete independent local/code/preparation work and record exact blocker and next action, not Done. Rollback previous frontend/backend and verified content pointer, keep immutable assets/releases.

Status: Implementation and deployment preparation accepted by root — final release/security Gate PASS on 2026-09-11. Online rollout is blocked by external authentication/review prerequisites and is not Done. [Final independent Gate evidence](../output/t17-gate/provenance-pass.log) verifies stale-report and inherited-nonce rejection plus valid audited-base-first publication.

Implementation evidence (2026-09-11): the generic goal exporter now reads the ignored reviewed Arabic-alias input and reproduces the accepted 99-question manifest exactly without re-encoding. Offline preparation verified actual bytes for 240 V18 PNGs, 51 rebuilt JPEGs, and 198 goal MP4s; it requires create-only upload plus exact immutable generation, SHA-256, MIME, byte size, width/height, and video duration readback before release associations. Release category labels are now copied into `releases/<releaseId>/catalogCategories/*` and Firebase room creation reads that exact pinned release, with mutable global catalog fallback restricted to old demo fixtures. Firebase Hosting and app-shell CSP permit contained `blob:`/`data:` media while retaining the approved Firebase/App Check and reCAPTCHA origins plus explicit emulator ports. Functions emulator delivery verifies the fixed private goal MP4 registry; the Firebase client decodes the authenticated callable data URL directly to a Blob for PNG/JPEG/MP4, without a Storage URL or a CSP network request. [Dry media plan](../output/t17-release-prep/PRIVATE-MEDIA-DRY-RUN.json) records 489 verified assets; [blocked preflight](../output/t17-release-prep/T17.3-PREFLIGHT-BLOCKED.json) records no cloud action, scoped private-answer scan, missing reviewed v3.3 artifacts, and next action. The initial Firebase E2E repair run had 6 failures, then one private-image failure; the repaired full suite passed 10/10 and the dedicated real goal blur-to-clear host/display reveal passed 1/1 ([root logs](../output/t17-root/firebase-e2e-final.log), [goal log](../output/t17-root/firebase-goal-final.log)). Focused release/composite/adapter/CSP checks, scoped lint, app typecheck, and production client build pass. The full node typecheck remains separately blocked by concurrent non-owned Firebase content-sync files; root's scoped exclusion check passes.

Composite contract: the retained exact-62/minimum-300 V3.3 release remains unchanged. `scripts/reviewed-media-composite-release.ts` builds a single immutable composite root for a signed reviewed-media supplement: it recomputes every base child/catalog root commitment, binds prompt versus answer asset roles and all answer text into each reviewed question hash, requires all 99 stable goal records and 198 paired MP4 bindings, and supports additional signed PNG/JPEG image records only for exact copied category labels with collision checks. Its trusted-authority and review-report loaders require canonical exact-byte SHA-256 anchors, regular external trust artifacts outside the untrusted base/supplement directories, valid trusted time/issuer schema, exact report/receipt commitments, and unique raw-nonce SHA-256 claims. `firestore-release-publisher.ts` exposes `composite-plan`, `composite-prepare`, `composite-verify`, `composite-activate`, and `composite-audit`; each publication lifecycle step requires an already prepared and exactly verified immutable V3.3 base root, its publication receipt with complete inherited nonce commitments, and global claim ownership. Older base receipts without that provenance fail closed for composite publication; existing non-composite releases are unchanged. Self-contained tests cover the tamper, stale-answer, role-swap, unrelated-key, nonce-replay, image-binding, raw-report, trust-boundary, and prepare→verify→CAS cases. No approved supplement/review report/external trust anchor exists, so no online plan, upload, publication, or activation was attempted.

## Handover

Local media/data and runtime/UI are accepted (T17.1/T17.2), as is T17.3 deployment preparation after final Gate PASS. The actual SQLite-backed app is running at http://127.0.0.1:8787; root refreshed it after the Huroof recommendation fix and confirmed 88 categories and 99 ready goal questions. [Current inventory](../output/t17-root/live-app-inventory.json) and [media failure checks](../output/t17-root/media-failure-check.json) record selector readiness and wrong-bearer/expired/missing/stale media rejection. Firestore/Storage emulator rules pass; [Firebase E2E evidence](../output/t17-root/firebase-e2e-check.json) records the repaired full 10-test pass plus the dedicated real goal test pass. The goal test verifies each screen's own blur-to-clear transition, decoded video, shared answer, removed timer and unchanged scores. App typecheck/build and scoped Node typecheck pass. The latest whole-project Node check fails in concurrent, non-owned `scripts/firebase-content-sync-production.ts:82` (`parentCollections` is undefined); [full log](../output/t17-root/typecheck-final.log), [scoped configuration](../output/t17-root/tsconfig.scoped-node.json). No unrelated source was changed to hide those failures. Preserve concurrent later database imports; the T17.1 backup is historical evidence and must not overwrite newer rows.

Next authorized action: run `firebase login --reauth`, then recheck billing, private Storage, Functions, App Check, deployment credentials and the actual active release. The last [read-only authentication retry](../output/t17-root/online-readiness.json) failed before production API reads; historical infrastructure findings are not current confirmations. Obtain the complete reviewed V3.3 base corpus and signed media supplement/review report with an externally anchored authority. Prepare and exactly verify the base, upload private media create-only, and bind actual generation/hash/MIME/size/dimensions/timing readback. Use `scripts/firestore-release-publisher.ts` operations `composite-plan`, `composite-prepare`, `composite-verify`, `composite-activate`, then `composite-audit`; the CLI requires exact base/supplement/report/trust paths and hashes, pinned project/database/location, and activation additionally requires the expected active release, audit reference and verification receipt. Deploy compatible Functions and the Firebase-mode Vercel app only against those verified inputs, then run real online host/display goal and image smoke checks. Production was not uploaded, deployed or activated in this task.

Changed-file map: `.gitignore` and `scripts/t17-goal-media.py` cover private media/import preparation; `server/service.ts`, `server/index.ts` and the local source loaders cover SQLite/runtime delivery; shared runtime contracts/adapters, `src/routes/GameRoutes.tsx` and `src/styles/global.css` cover reveal and presentation; `functions/src/game.ts`, `functions/src/question-media.ts` and `functions/src/index.ts` cover Firebase authority/delivery; the media/composite preparation scripts, release builder/publisher, V3.3 catalog contract and CSP files cover rollout preparation. Focused unit, integration, emulator and adversarial release tests accompany these changes. Original media, earlier questions and review states remain preserved. Existing mixed user work was not staged, committed or pushed. Elapsed approximately 110 minutes against the original 60–120 minute estimate. Astra coordination/security/release review and Terra implementation/database review were used; session/token telemetry unavailable.

### Initial review evidence — 2026-09-11
Database read-only review: SQLite integrity ok, 7,253 rows; JSON additions need no schema migration. Preserve stored staging/approval states; activate only verified supported media in runtime projection. Security review: current projection must not serialize private answer bindings; occurrence/phase-bound grants and authoritative byte checks are required. Root online check failed before API reads because Firebase CLI credentials expired; user asked to reauthenticate. Implementation continues locally.


## M-MEDIA-04 / T17.4 — Owner-authorized live media activation — 2026-09-11

Current authorization: user requests uploaded image questions to display images, goal-video category to appear and play, and all uploaded categories to work. This continues the existing media milestone after T-14.5 activated owner-authorized text gameplay. The text release is now active; historical missing billing/auth and absent pointer findings above are superseded by fresh evidence in output/media-live-20260911/PREFLIGHT.json. No game-media bucket exists; Firebase Storage API is disabled, billing is enabled.

D17.6: Extend the separate, honest owner-authorized publication route. Do not fabricate signed specialist reviews or alter the original signed-review path. User authority binds the exact verified local uploaded media/question manifests and source provenance into a new additive receipt. Copy and verify the current immutable text base into a new release, add only technically valid paired media/question records, and atomically compare-and-swap from the exact captured active pointer after complete readback. Preserve old rooms' pinned release and all original intake records. Do not directly edit active questions or revive completed rooms.

D17.7: Create the project's private default Firebase Storage bucket in me-central1, matching the existing Doha Functions, if fresh inspection still confirms absence. Enable only required Storage service, enforce uniform bucket access/public-access prevention, deploy existing deny-all client Storage rules, and verify effective private reads before media upload. Upload content-addressed assets create-only with exact generation/hash/MIME/size/dimension checks, no public ACLs or Firebase download tokens. No Firestore schema/rules changes are required. Runtime may explicitly recognize a verified additional photo family, with fixed private path/MIME mapping and no caller-controlled paths.

Tasks/owners and sequence:
1. T17.4.1 Root + read-only Database/Security: reconcile all question/media category sources, identify usable assets and unavailable pairs, validate runtime compatibility and rollback. Produce a category matrix including stored/usable image/video/text counts and readiness. No invented missing assets or answers. In progress.
2. T17.4.2 Forge, single implementation writer, depends on .1: implement minimal owner media release preparation/upload/publish/verify/CAS tools and any concrete runtime compatibility fix; reuse existing verified media/selector utilities. Cover immutable base/owner binding, role swaps, incomplete pairs, source drift, interrupted rerun, media subcollection audit and stale-pointer rejection. No cloud writes by Forge. Pending.
3. T17.4.3 Root, depends on .2 and Database/Security/Gate: provision verified private bucket; upload exact reviewed bytes; prepare the concrete release, perform full validation, verify every category with actual selector allocation; apply additive records and pointer-last CAS. Preserve captured rollback pointer and original immutable assets. Pending.
4. T17.4.4 Root, depends on .3: deploy only necessary Functions/client changes, run production host/display decoded image and goal-video blur-to-clear tests, confirm missing/insufficient categories are not advertised as playable, push scoped GitHub changes and publish the full category readiness MD report. Gate required before release. Pending.

Acceptance: uploaded usable image questions render decoded images; goal category appears in category mode and actual MP4 plays inline; host-only reveal supplies clear variant and answer while keeping score unchanged; no player/hidden audience/pre-reveal clear disclosure; all category rows reconciled to actual usable media/text and selector results. Missing bytes, unmatched answers and insufficient content stay explicitly unavailable rather than blank images/fake playable cards. No static-preview fallback. Root marks Done only with actual production evidence.

Checks: existing media authorization/reveal/selector tests plus narrow new publisher/CAS and new-family tests, typecheck/build of affected modules, exact create-only upload/readback and independent risk Gate. Preserve unrelated working-tree files and private media outside Git/Vercel. Rollback: CAS to the captured previous active pointer only if current pointer is exactly this release; retain all immutable evidence/assets and old-room references. Re-promote the prior compatible Vercel deployment if needed. Never broad delete/restore.

VISUAL CONTRACT: Reuse the T17 visual contract verbatim: existing question area remains focal point; prompt, contained media, then answer/action; density 4–6; existing desktop host/display layout, mobile media beneath prompt without overflow; preserve Arabic typography and cobalt/ice/cyan surfaces, spacing and corner rules; object-fit contain, muted inline playback once with replay fallback; existing loading/retry/unavailable/focus patterns; no autoplay loop, fullscreen takeover, new decorative motion, board redesign or image cropping.

### Resume evidence and D17.8 — 2026-09-12

The read-only database review is recorded in `output/media-live-20260911/DB-REVIEW.md`. Successor preparation must omit exactly 238 blank image-dependent classic projections from the copied base (tahadani-011:60, 012:60, 014:58, 061:60), retaining the original immutable release unchanged. Replace them with 291 verified image questions and add 99 goal-video questions with 198 paired clips. Expected successor: 10,838 questions, 68 categories, 489 media records. Verify these totals from actual prepared children before activation.

The initial activation scope uses the three already supported media families. Of 60 recovered photo sidecars, 36 duplicate existing rebuilt questions, 19 lack assets, and five unique photos need an additional runtime family contract; do not mix them into this activation. Report these held records explicitly rather than claiming all source rows playable.

T17.4.1 local database/security review complete; production infrastructure privacy and playback checks remain pending. T17.4.2 implementation was interrupted by a provider usage-limit error before a publisher artifact was created. User requested continuation September 12; one retry dispatched to the same configured Forge route. No Storage provisioning, new release publication, or production deployment has occurred for this milestone yet. The original 30–60 minute estimate excludes the overnight pause and must be revised once implementation availability is confirmed.

### Private infrastructure completed — 2026-09-12

Root enabled Cloud Storage for Firebase and created the default `huroof-a3ee7.firebasestorage.app` bucket in ME-CENTRAL1. Fresh metadata confirms project 245740172770, uniform bucket-level access enabled and public-access prevention enforced. Deployed bucket-specific deny-all Storage Rules source SHA-256 is `c7945b30ffec81f9b5be9cea162c4cb891e7417797004f211b564fadd2a61ca5`. Authenticated bucket/project IAM contains no public principals; authenticated project metadata has no parent. Security accepted this standalone hierarchy evidence. Runtime configuration already points at this bucket and its service account has existing project access; no new IAM grants were made.

Evidence: `output/media-live-20260911/STORAGE-CREATE.json`, `STORAGE-PRIVATE.json`, `STORAGE-PRIVACY-READBACK.json`. First reviewed object still requires generation/hash/no-token readback and unauthenticated denial on both GCS/Firebase endpoints before bulk upload. No assets uploaded yet. Fresh `RESUME-ACTIVE-POINTER.json` confirms the original owner text release remains active. Forge resume is executing normally; core M-MEDIA-04 implementation remains in progress.

## M-MEDIA-05 — Complete uploaded Category-mode inventory — 2026-09-12

Current user scope includes all uploaded categories. Read-only evidence `output/media-live-20260911/MISSING-TEXT-CATEGORIES.md` identifies 4,500 unique v19/v19.1 text questions across 15 categories missing from the older approved cloud snapshot. They are user-uploaded records covered by the current owner instruction, not specialist-reviewed material. The earlier publisher additionally rejects identity-mode text. This is an additive follow-on using the already selected owner-authorized immutable publication pattern, without changing source review states or inventing Huroof eligibility.

D17.9: After M-MEDIA-04 activation/readback, prepare a separate Category-only successor from the exact media release. Preserve all media generation bindings and existing immutable children, add only the 4,500 source-provenance-verified text questions from the audited 15-category allowlist, and retain `letterModeEligible=false` and truthful `source_transcribed_not_fact_reviewed` provenance. Category mode may present identity questions as text. No charades conversion or fabricated assets. The five unsupported charades categories remain held and absent from live choices.

Tasks and dependencies:
- T17.5.1 Database/root: missing-category source/duplicate/readiness audit completed in the linked evidence. Exact 4,500 unique questions, 15 categories, no normalized within-source duplicates.
- T17.5.2 Forge single writer, after T17.4.2 implementation complete: implement the bounded separate Category-only supplement, binding original source hashes, exact captured media base, additive owner authority, immutable full readback and pointer CAS; safe retries and reverse CAS rollback. Do not mutate or broaden M-MEDIA-04 code during its review/apply. Tests cover unsupported source modes, altered source records, false letter eligibility, duplicate/conflicting records, missing media bindings, stale base, retry and rollback. Pending.
- T17.5.3 Root/Gate, after M-MEDIA-04 live acceptance and T17.5.2 checks: review concrete supplement, apply and verify successor; repeat actual selector audit and live catalog/new-room smoke. Expected final inventory 15,338 questions, 83 runtime categories and preserved 489 media records, subject to exact prepared validation. Publish MD for all 88 source categories with stored/usable counts and five held-category reasons. Pending.

Acceptance: all 83 supported source categories appear in Category mode and successfully allocate legal boards; image/video playback survives successor copying. The 4,500 added records never enter Huroof selection. Public inventory clearly distinguishes five unsupported charades categories. Preserve original source statuses and existing rooms. Checks include focused publisher/selector tests, affected typecheck/build, independent release Gate, immutable object/document readback, and real browser smoke. Rollback compares and restores the preceding media pointer; no destructive cleanup. Root owns completion status and scoped GitHub/production handoff.

### M-MEDIA-04 review — 2026-09-12

Initial implementation reached `Implementation complete — ready for review`; independent Gate rejected cloud apply. `output/media-live-20260911/GATE.md` records concrete REST adapter issues: privacy barrier/endpoint handling, missing token-metadata rejection, rebuilt JPEG local path, partial-write retry, authority identity validation, exact final verification, and full-pointer rollback. Existing focused tests passed but did not cover these transport behaviors. Forge owns the bounded repair and concrete persistent-transport tests. No media objects or release documents have been uploaded, and the active game release is unchanged. Do not activate until this Gate repair passes. No claim of recursive descendant enumeration is authorized; audit scope is exact known direct children/collection IDs.

Clarification for held-category reporting: lower-level charades selectors and Functions branches already exist, but the current web setup has no exposed charades flow and the local import holds these source rows. Report the five categories as unavailable in the current web game, not as proof that no charades code exists anywhere. Do not convert their acting instructions to trivia or expose their secret answers through the normal audience question flow.

### M-MEDIA-04 final review and guarded upload — 2026-09-12

Final independent Gate passed the repaired publisher and 14 focused cases (`output/media-live-20260911/GATE-FINAL.md`). Node 22 typecheck, scoped lint and diff checks passed. The production multipart request now persists exact image/video metadata and content-addressed bytes without download tokens. Root began the guarded apply; the first immutable MP4 was created correctly, but authenticated readback failed with HTTP 403 before bulk upload or publication.

Fresh effective-permission evidence (`OBJECT-READ-PERMISSIONS.json`) showed that inherited basic owner/editor roles granted object creation/listing but not object reads with uniform bucket access. Security reviewed the minimum correction. Root added bucket-scoped `roles/storage.objectViewer` for the exact current deployment user and existing production runtime service account using a fresh policy etag, preserving every existing binding. `OBJECT-VIEWER-IAM.json` records the before/after policy and exactly two additions. Rollback removes only those added members against a fresh etag, never restores a stale whole policy. Bucket privacy, deny-all rules and public-access prevention remain unchanged. The retry passed the first-object private readback barrier; all 489 objects are now present. Full release readback, activation and real browser playback remain pending.

### M-MEDIA-04 live acceptance — 2026-09-12

Guarded apply and separate full verify succeeded. Active media release is `owner-media-release-548087aee7b942cb8c0a96cc95d85c17`, with 10,838 questions, 68 categories and 489 immutable media bindings. Materialized root commitment is `f65333022462c43d357b17747e30d8bdf4a776451b814655a81c94cdceed341c`. `M04-VERIFIED-BASE-SNAPSHOT.json` captures all 11,464 documents; SHA-256 `ef6f18b6600dc02d446a866b4df30e7518918565d4a895a5e7c30950d7d00b47`.

Production room `4T7F8SPR` was created from the displayed goal/video and image categories. Real Chrome host and independent in-app audience decoded the blurred goal MP4, then each loaded its own clear clip on explicit reveal; shared answer appeared, timer disappeared, scores stayed zero. Returning to the board selected a different image question and both screens decoded the 768×576 PNG. Evidence and the timeout/reveal timing detail are recorded in `output/media-live-20260911/PRODUCTION-BROWSER-CHECK.md`. Runtime private object-read access is therefore demonstrated through actual authenticated game delivery.

T17.4.1–T17.4.3 are Done. The core playback and GitHub portion of T17.4.4 is Done; the complete category report and successor retention checks remain with M-MEDIA-05. Commit `14762b78` contains only the three reviewed media publisher/test files and was pushed to `codex/app-update-2026-09-07`. No private question manifests, answer files or media bytes entered Git/Vercel. The public production alias resolves to Ready deployment `dpl_6K44t8o26vvEaxXeRB8AKic1cDxr`; this data-only publication requires no client replacement. The original mixed working tree was preserved.

### M-MEDIA-05 review checkpoint — 2026-09-12

Forge prepared concrete `owner-category-release-d6df0b1290f0cdd1a19beba32aa169c4` from the verified live M04 snapshot: 15,338 questions, 83 categories, 489 retained media, 15,994 release documents. Seven opt-in checks passed with no skips, including actual source mutation rejection and all-category selectors; portable synthetic checks cover persistent retry/authority/rollback and paginated production REST reads. Node typecheck, scoped lint and formatting passed.

Independent Gate (`output/media-live-20260911/GATE-M05.md`) found two concrete production blockers despite those passing tests: fractional video durations were serialized as integerValue, and completion root/verification receipt preceded exact child readback. The bounded Forge correction must preserve fractional values on the wire and move the verification barrier before completion artifacts, with focused regressions. No M05 cloud writes have occurred. Live media release and browser playback remain unchanged.

## M-MEDIA-06 — Larger release runtime recovery — 2026-09-12

Evidence: M05 guarded apply and separate full document verification succeeded. The live catalog smoke then returned no categories because `getApprovedReleaseCatalog` exceeded its 256 MiB instance limit, HTTP 503, after successful Auth and App Check validation. `output/media-live-20260911/M05-CATALOG-LOGS.json` records the exact OOM. Root immediately ran the reviewed full-pointer rollback, restoring working M04 without deleting any data (`M05-ROLLBACK.log`). M05 is retained and verified but not active; M04 remains the working production release.

D17.10: Fix the proven runtime capacity issue before reactivating M05. Restrict Firestore release queries to the canonical runtime fields, preserving media, answerMedia, accepted answers, identities and host source references while omitting unused owner/import provenance. Set explicit bounded memory and concurrency only on callables that read complete releases; current baseline is 256 MiB / CPU 1 / concurrency 80 / timeout 60s. Catalog max instances is 20; other current maxima are unspecified. Capture the final selected resource configuration and deployed readback. Preserve authentication, App Check, region and existing caller authorization.

Tasks:
- T17.6.1 Root: OOM diagnosis, immediate rollback, deployed resource baseline, source-reader inspection complete.
- T17.6.2 Forge, one source writer: own only clean-release `functions/src/index.ts`, focused Functions tests, `scripts/owner-category-supplement.ts` and its tests. Add actual-M05 catalog-projection/canonical-row regression and explicit canonical field-selection coverage. Add an explicit operation-referenced reactivation path after a verified rollback: exact full release/owner/old activation/rollback evidence, fresh transaction CAS from M04, new immutable reactivation receipt, idempotent exact retry and stale/conflict rejection. Preserve all existing activation and rollback records and the unchanged M05 payload. In progress.
- T17.6.3 Root/Gate: review integrated Functions/resource/reactivation change, run focused Functions and publisher checks/build, deploy only required callables. Verify their active runtime resources and health against M04 first. Then explicitly reactivate unchanged verified M05 and run actual production catalog, new-room creation, media retention, old-room pinning and Huroof smoke. Pending.

Acceptance: production catalog exposes83supported categories without OOM; new Category rooms covering all15added categories start with legalboards, image/video still decode, existingM04rooms continue working, Huroof excludes the4500Category-only additions. No increase beyond bounded resource settings and no weakening of access controls. Rollback: full-pointer CAS back to M04 on any catalog/new-game failure, retain all releases/receipts, and restore prior Functions revision only if the runtime change itself regresses compatibility. Required review is technical release Gate; no new aesthetic direction or UI redesign.

### M-MEDIA-06 review and deployment checkpoint — 2026-09-12

T17.6.2 is Implementation complete — ready for review. Final frozen changes restrict all full-release question reads to canonical runtime fields and set only getApprovedReleaseCatalog/createRoom/submitGameIntent to 512 MiB, CPU 1, concurrency 1, max 20 instances, timeout 60 seconds. Root independently reproduced all 30 compiled runtime tests, including the actual 15,338-question M05 projection. Gate independently passed the build, 30 runtime tests and 11 publisher cases, with exact source hashes recorded in `output/media-live-20260911/GATE-M06-FINAL.md`.

The narrow runtime patch was applied to the original mixed tree after `git apply --check`; the two root-owned supplement files were refreshed only after verifying their previous hashes. Unrelated work was preserved. The initial Node 22 CLI deployment stopped before mutation because function listing failed. The system Node 24 CLI successfully listed the same project with system CAs enabled; deployment retry uses that CLI while the deployed Functions runtime remains Node 22. Live acceptance is still pending.

### M-MEDIA-06 deployed; concurrent content publication detected — 2026-09-12

The three runtime Functions are ACTIVE on Node 22 in me-central1 with exact reviewed resources (`M06-DEPLOYED-RESOURCES.json`). Firebase CLI returned a nonzero result solely because no Artifact Registry cleanup policy was configured after all three successful updates; no retention policy was changed. The cold public catalog returned 68 playable categories.

The explicit M05 reactivation then failed closed before any pointer mutation: the active pointer had changed at 12:56:23 UTC to `owner-release-q4800-947e1bde42edecaa950dfa107dfd38e5`, published by the concurrently active task “Link downloaded images to questions.” This release preserves M04's 291 image questions, 99 goal-video questions and 489 media records, with 15,264 total questions and 68 categories. Do not restore M04/M05 over this newer content. That task also has a prepared Q6000 successor with 21,263 questions and 81 categories. Adding all 4,500 missing Category-only questions afterward would exceed the current 25,000 runtime bound; integration needs an explicit combined-capacity review and a single coordinated pointer publisher. Root requested user authorization to message the other task and coordinate; until answered, continue read-only reconciliation and live gameplay checks, no content-pointer writes.

Commit `d2908df9` records only the four reviewed runtime/reactivation files; commit `bf755994` records the category supplement. Neither has been pushed yet. M05 remains retained, verified and inactive. M06 runtime implementation is deployed; combined-content acceptance remains pending because the base changed.

## M-MEDIA-07 — Preserve concurrent imports and complete missing categories — 2026-09-12

D17.11: The user explicitly authorized cross-task coordination. The other task will finish its guarded Q6000 publication and source-bound verification, then stop pointer writes and notify this root. This root owns the subsequent final reconciliation only after that notification and a fresh full base capture. Do not activate retained M05 or overwrite Q4800/Q6000.

Scope: preserve every question, category, inventory/media record and authority of the final verified Q6000 base. Add only the exact 4,500 already owner-authorized Category-only questions and 15 categories from retained M05, preserving source review states and empty target letters. Expected 25,763 questions, 96 categories and 489 media bindings, subject to exact conflict/count audit. This exceeds the old 25,000 readiness ceiling; raise the explicit bound to 30,000 and use a bounded 1 GiB / CPU 1 / concurrency 1 / max 20 / 60-second configuration for the same three full-release readers, preserving canonical projections and all access controls. No schema/rules/frontend redesign.

Tasks and ordering:
- T17.7.1 Database read-only: inspect exact Q6000/M05 candidates for category, ID/content and authority conflicts; establish counts, ordering and rollback. Root owns the coordinated publication fence and fresh final-base capture. In progress.
- T17.7.2 Forge single writer in the clean release clone: implement a narrowly bounded successor publisher or extend the existing category supplement without changing deterministic legacy M05 payloads. Bind exact final base bytes/root/pointer and exact retained M05 source/authority; preserve all base media with no object uploads or replacements. Preparation is offline. Live apply must verify both retained releases and owner evidence, write immutable child documents create-only with safe retry, exact readback before completion root/verification artifacts, then transactionally CAS the exact full final-base pointer with immutable activation receipt. Refuse unexpected IDs, base drift, overlap/conflicting content and out-of-bound records. Support exact retry and rollback CAS to the captured Q6000 pointer. Only root applies. Add bounded 30,000/1 GiB runtime configuration with actual combined-payload projection/selector regression, preserving Huroof exclusion of all 4,500. Pending.
- T17.7.3 Gate/root: independently review source/frozen plan, persistent transport retry/tamper/CAS/rollback tests, actual combined selector/readiness checks, build and resource bounds. Root deploys only the three functions and verifies existing live Q6000 health before final activation. Fresh capture must still match the other task's notified final release. Pending.
- T17.7.4 Root: apply and full verify, test production catalog and new games covering all added categories, media retention and existing pinned room. Update the Markdown report for the union of all uploaded source categories with exact live/stored counts and held reasons, then scoped GitHub push and production alias check. Pending.

Acceptance: no dropped Q4800/Q6000/M04 content; exact counts and authority references independently verified; all supported categories allocate valid boards; all image/video bindings retained and real PNG/JPEG/goal playback verified; Huroof excludes Category-only additions. Five charades source categories remain hidden because the web acting-game flow is unavailable, explicitly documented. Rollback: compare-and-swap only from our successor to its fresh captured Q6000 base; retain all releases/receipts and all other task work. If another publisher changes the pointer, fail closed and coordinate rather than replacing it. Technical release review is required before cloud writes.

### M07 coordinated predecessor checkpoint — 2026-09-12

The other task's guarded Q6000 apply log now reports activation of `owner-release-q6000-37eb7fa4acc9224df8cbad6bffdf6fe1` after exact recursive readback of all 21,915 release documents. Its own final source-bound browser smoke and explicit handover are still required before this root freezes the fresh successor base. Root source review of the separate reconciler found missing pointer metadata, a Q6000/M05 pointer-shape mismatch and incomplete authority content binding; Forge is correcting these with real persistent transport tests before Gate. See `output/media-live-20260911/M07-ROOT-REVIEW.md`. No M07 cloud writes occurred.

### M07 publication fence confirmed

The user approved coordinating the two tasks. The Q6000 task explicitly handed over completed release `owner-release-q6000-37eb7fa4acc9224df8cbad6bffdf6fe1` and confirmed no further active-pointer writes. Its exact 27,976 planned-document readback passed at13:45:38.772UTC; live source-bound room `ME4M32F2` verified a Q6000 PlayStation question at13:47:17.795UTC. Root independently observed 81 categories and no server ERROR/HTTP5xx/OOM since M06 deployment. Q6000 root commitment: `41bd298412dfd23dd290e602faa5d6ac10088ce0869ffddd2e941eeae8c39cf5`. T17.7.1 conflict/ordering review is Done. Fresh final-base capture and Gate are in progress; only this root may publish the final M07 successor under the agreed fence.

### M07 final-input review — repair required

Fresh Q6000 base capture succeeded: `M07-FRESH-Q6000-BASE.json`, SHA-256 `3319857e2a8bf48051166e4cb118a4c4ad3fb369e2ad071f6058e9405d5df12c`, 21,915 release documents, 301 complete lineage authority documents, four ancestor roots. Initial M07 concrete plan is `owner-category-reconciliation-b29d90de1e33749c5173d88b9686baf4`, SHA-256 `c6527b1af82710fc01015fe605a0d8f14b4082c1328ebfb2ca91777a587912d8`, with 347 combined authority records.

Independent Gate ran reconciliation5/5, Functions build and runtime30/30 successfully but rejected publication. Its concrete omission probe removed the original owner's entries/chunk-00001 from the otherwise valid fresh capture; the builder accepted346instead of347authority records. Existing persistent tests also used a separate mock API rather than the production REST transaction adapter. Forge owns the bounded completeness and actual-transport regression repair before one re-review. `output/media-live-20260911/GATE-M07-FINAL.md` records this initialFAIL. No M07 capacity deployment or pointer write has occurred; production Q6000 remains working.

### M07 second review checkpoint

The independent re-review passed all eight reconciliation tests and confirmed complete local authority binding and actual production REST transaction coverage. It found a narrower remaining live-data barrier: the publisher must enumerate retained approval subtrees before any write, reject missing or extra documents, and never recreate retained authority. The source writer is repairing only this barrier with zero-write failure tests. See `output/media-live-20260911/GATE-M07-REREVIEW.md`. No M07 cloud writes have occurred; the working 81-category Q6000 release remains active.

### M07 final technical acceptance before deployment

The final barrier repair passed independent Gate (`GATE-M07-AUTHORITY-FINAL.md`): actual production REST3/3, including zero-write failures for missing, changed and extra authority. Root independently reproduced the authority failure test and reviewed the exact prewrite call order. Retained approvals are read-only; no authority create stage remains. Frozen reconciler `03d9b93a00b480252af941f8e9e9d951130b0788f7e08e4d5c74de84e69efde0`, tests `7af3a17cb51b3d1ef2b6f07d2c269b11921990674b40c226c61c14e1fc3bc5f2`. Concrete plan remains `c6527b1af82710fc01015fe605a0d8f14b4082c1328ebfb2ca91777a587912d8`. Full private suite9/9, Node typecheck and scoped lint pass. Root resolved the two clone-relative media fixture errors by running the actual binary fixture suite from the original workspace:4/4pass. Runtime diff is exactly1GiB and30,000bound, preserving all access controls. T17.7.2 Done; T17.7.3 local review Done, deployment/readback in progress. Operational limitation: authority enumeration is global and safely refuses unrelated approval roots; this never authorizes deleting such roots.

### M07 deployed runtime and GitHub checkpoint

The three full-release readers are ACTIVE at 1GiB/CPU1/concurrency1/max20/60seconds on Node22; exact resource readback is `M07-DEPLOYED-RESOURCES.json`. Public setup reloaded successfully with the81-category Q6000 baseline. The CLI's exit1 was only the unchanged post-deployment artifact cleanup policy warning; all three function updates succeeded. Existing M04 audience room4T7F8SPR still decoded its960x631JPEG after deployment. Implementation commit `a8beedb1cdf307b6e1b167a890adf570e1d5002e` is pushed and independently resolved on GitHub branch `codex/app-update-2026-09-07`. Only the four reviewed implementation files were committed. T17.7.3 Done; guarded M07 apply is running, activation and final live acceptance remain pending.

### M07 safe prewrite refusal and bounded scope correction

The first live apply stopped at the prewrite authority audit before any create. All four expected roots exist, but an unrelated historical Q4800 approval root also exists (`M07-AUTHORITY-SCOPE.json`). This is the operational limitation documented by Gate, not corrupted retained authority. Preserve the unrelated root. The publisher correction will enumerate only the four bound retained approval roots and their complete descendants, maintaining zero-write refusal for missing/changed/extra retained children. No release or pointer was written; Q6000 remains active. The 1GiB runtime deployment and prior live compatibility checks remain valid. Forge owns this narrow scope correction and test; Gate reviews that delta only.

### M07 scoped authority repair accepted

Gate independently passed both focused production REST cases (`GATE-M07-SCOPED-AUTHORITY.md`); root reproduced2/2 and inspected the11-line bounded delta. All347bound authority documents remain exact-readback inputs and each retained subtree is enumerated, while unrelated top-level approvals are preserved. Final source hash `144d25ecf43ef86fb3f045576c40758193f14917bf14b2b0ed8b5ba7161a6881`, tests `1289430f764440c1300582c1f71781e75c295df1b1e99933b86c7e192b3c8ff2`; deterministic plan unchanged. Full private suite10/10. Scoped live retry is running in `M07-APPLY-SCOPED.log`; no further runtime deploy is required.

### M07 final live acceptance — Done

T17.7.1–T17.7.4 are Done. Guarded scoped apply exited0 and activated `owner-category-reconciliation-b29d90de1e33749c5173d88b9686baf4` at14:30:32.402377UTC. Independent capture read all26,445release documents and deep-compared every path/content to the unchanged frozen plan:25,763questions,96categories,489media. SnapshotSHA256 `a171bf5a49fa82ed73782533a7a83e56ea4483dfad2c70076310d814a4fb5bd9`. See `M07-APPLY-SCOPED.log` and `M07-INDEPENDENT-READBACK.log`.

Actual public setup showed96of96categories. RoomsRHF3PHKF andPMWDS2MU created/started/prepared25-cellboards covering all15newcategories plusgoals/images. New Saudi-league and animalidentity questions loaded. No-answer recovery returned anenabledboard andloadedanotherquestion. Goalblur anddistinctclearclip bothplayed854x374/2.302302seconds/readyState4. New960x673ColosseumJPEG visiblyrendered. ExistingM04audience stillrendered960x631JPEG afteractivation. EarlierM06actualHuroof unanswered-second-letter checkpassed; M07actualcombined selector regression preservesHuroof exclusion of4500Category-onlyrows. Freshlogs showzeroERROR/HTTP5xx/OOM sinceactivation. Detailed evidence: `output/media-live-20260911/PRODUCTION-BROWSER-CHECK.md`.

[Final category report](../docs/CATEGORY-PLAYABILITY-2026-09-12.md) lists all101uploaded sourcecategories, exactlive/storedcounts and fiveheldactingcategories. Supportedliveinventory is96, not101; unsupportedactingflow and unmatchedphotoassets remainexplicitlyheld, neverfabricated. Ownerapproval doesnotclaimspecialistfactreview. M04media andM06runtime areDone; M05's categorypayload is incorporated intoM07 whiletheoldM05release remainsretainedinactive. This completesT17.4.4andT17.5.3 throughthesafecombinedsuccessor ratherthanoverwritingconcurrentimports.

Implementation commits `a8beedb1` and `45aab23c` arepushed to `codex/app-update-2026-09-07`; originalmixedwork preserved withnarrowpatches/hash-checkedcopies. ThreeFirebasefunctions aredeployedandresource-verified. TheVercelproductionclient uses these live backend/data changes; no additionalclientdeployment wasneeded. Finaldocumentationcommit/aliasreadback isrecordedinthehandover.
