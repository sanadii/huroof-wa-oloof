# T-17 — Goal videos and image questions — 2026-09-11

Authority: user explicitly approved implementation locally and online. Root owns scope and acceptance; Forge is the single implementation writer. Preserve the mixed working tree and existing draft/release authorities. This extends the canonical implementation plan and milestone register.

## Contract and decisions

- D17.1: 99 existing single-shot goals, soft blur option 1 (`gblur=sigma=2.5:steps=2` before resizing), crop identifying captions as in preview; paired muted H.264 fast-start MP4 around 854 pixels wide. No padding/replay. Target <500 KB each, maximum 1 MB. Files/Storage objects hold bytes; DB holds opaque bindings and metadata.
- D17.2: Category `من سجل الهدف؟`, stable IDs, caption-grounded scorer answers/source timings. Arabic names reviewed without invented identities. Include verified existing image questions; hold missing/invalid images truthfully.
- D17.3: Shared host-only REVEAL_ANSWER stops timer/buzzing without scoring, persists per question, broadcasts answer/clear media to host and visible audience display. Idempotent; reset next question. Existing private host answer preference remains separate. Clear binding unavailable before reveal.
- D17.4: Local SQLite import is transactional/idempotent with backup and preservation evidence. Production uses existing reviewed immutable release process; no silent promotion of drafts. Online deployment authorized, subject to actual infrastructure/content prerequisites.
- D17.5 (integration decision, 2026-09-11): Preserve the exact-62/minimum-300 V3.3 base contract. Add a separately reviewed 99-goal media supplement and compose one immutable release with the existing single audited active-release pointer. Bind the verified base release/root, signed supplement review commitments, category catalog, goal questions and paired immutable media into the composite root. Preserve existing trusted-review authority checks; caller-supplied signatures/trust cannot invent approval. Reject collisions, missing reviews, stale hashes or absent Storage generations before publication. Goal questions are category-mode only. Preparation/verification must distinguish missing external reviewed inputs from any still-unimplemented publisher support.
- D17.5a (Gate repair clarification): Composite publication requires an already prepared and exactly verified audited base, which need not be active. Verify its immutable root, publication/provenance receipt, complete inherited review-nonce commitments and existing global claim ownership; do not reclaim inherited nonces. An offline base-plan file alone is insufficient. Older receipts lacking the required provenance fail closed for composite preparation while their existing non-composite runtime behavior remains unchanged. Bind the actual loaded review report hash to the signed supplement's report commitment.
- VISUAL CONTRACT: Reuse approved DESIGN-DNA.md and selected C Arabic game surface. Existing question area remains focal point; scan prompt, contained media, answer/action. Density 4–6. Desktop uses existing host/display layout; mobile stacks media beneath prompt without horizontal overflow. Preserve current Arabic typography roles, cobalt/ice/cyan surfaces, spacing, corner shapes, and existing icons. Media uses object-fit contain, muted inline playback once with replay/play fallback. Loading, retry, unavailable, disabled and focus states match existing controls. No new decorative motion, autoplay loop, full-screen takeover, redesigned board, or cropped image content.

## M-MEDIA-01 / T17.1 — Media package and local data

Owner Forge; prerequisites approved plan, database review before DB writes. Scope exporter/importer, private asset manifest and question records, SQLite only. Sources: resources/100-best-goals-2026-clips/single-shot/clips.json; existing photo manifests and SQLite draft rows. Existing DB baseline must be read fresh (planning read 7,253 rows; may change).

Sequence: inspect records and naming -> implement deterministic export/validation -> 198 assets -> source-name review -> backup -> dry run -> transaction -> exact readback/rerun. Metadata includes variant, checksum, content type, dimensions, duration, byte size, private path and source provenance. No schema migration unless separately justified. Existing records preserved.

Acceptance/checks: all 198 assets decode, timing pairs match, captions absent, sizes under ceiling; inspect representative all-goal contact sheets; source-to-answer audit. Integrity check, backup receipt, all prior row bytes/timestamps preserved, second apply no changes. Report all generated paths and largest/total bytes. Rollback removes only unchanged newly inserted IDs using receipt, never whole-DB restore over newer work.

Status: Done — root accepted 2026-09-11. Independent [root check](../output/t17-root/media-data-check.json) confirms all 198 hashes/size targets and exact preservation of 7,253 prior rows/timestamps. Root inspected all four contact sheets. Largest file 258,811 bytes; total 16,851,825 bytes. Full-decode and zero-write-rerun evidence reviewed.

Implementation evidence (2026-09-11): local-only package at ignored `content/question-media/goal-quiz-2026/` contains 198 opaque-name paired MP4s and 99 stable `goal-quiz-2026-001..099` draft records in category `goals-2026`. [Validation report](../output/t17-media/MEDIA-VALIDATION-REPORT.json), [import receipt](../output/t17-media/IMPORT-REPORT.json), [rerun receipt](../output/t17-media/RERUN-REPORT.json), [backup manifest](../output/t17-media/BACKUP-MANIFEST.json), and four contact sheets verify 198 full decodes, 99 timing pairs, 0 assets over 500 KB, source-caption answer aliases, a 7,253-row baseline / 7,352-row result, preservation of all prior row bytes and timestamps, and a zero-write rerun. The receipt's exact ID/data hashes permit only targeted rollback of still-unchanged new rows; no whole-DB restore is authorized.

## M-MEDIA-02 / T17.2 — Runtime, protected delivery and UI

Owner Forge; depends T17.1 accepted. Modules: shared game contracts/selection; local source/service/routes; Firebase game/media/release code; UI/media adapter/styles/tests. Security review on intended boundary before changes and integrated review after implementation.

Sequence: compatible media types/bindings -> registry resolver -> verified source/inventory selection -> authoritative reveal in local/Firebase -> phase-bound grants -> UI image/video playback and answer action -> tests/browser evidence. Support PNG/JPEG/MP4, old PNG bindings, media-aware room snapshot hashing and selection. Category mode mixes supported modalities; Huroof still requires valid letters. Avoid exposing private paths, answer filenames, clear assets or answer metadata pre-reveal. Preserve immutable release binding and role restrictions.

Acceptance: local host/display goal and image flows work, clear reveal synchronized and reset-safe, no automatic scoring, no timer/buzzer after reveal, reconnection preserves phase, cleanup on question/disconnect, retry works, legacy text/PNG flows pass. Checks: focused import/selection/service/UI/Functions tests, authorization stale/wrong-phase/wrong-role tests, typechecks/builds, live host/display browser evidence desktop/mobile. Root repeats a load-bearing check. Rollback compatible code plus targeted new imports; no deletion of existing records.

Status: Done — root accepted after repairs, independent Gate PASS and live host/display desktop/mobile verification on 2026-09-11. [Browser evidence](../output/t17-root/browser-check.json), [Gate regressions](../output/t17-gate/final-focused.log), [legacy disclosure probe](../output/t17-gate/final-legacy-probe.log). Restored failed-question prompt remains visible after refresh; prompt precedes media. Final Forge game52/Functions36 and restored UI16 checks pass. Production remains T17.3.

Implementation evidence (2026-09-11): local SQLite runtime now accepts 99 goal-video records and all 291 manifest/hash-verified V18/rebuilt image bindings, while 60 remote-only image links remain held. Category selection mixes image/video/classic questions; letter selection remains modality-specific. Local and Firebase projections bind media to an active occurrence and disclose clear answer media only after host-only `REVEAL_ANSWER`; stale occurrence, player role, hidden audience, old prompt binding, and malformed media IDs fail closed. Local byte delivery reauthorizes both ticket issue and byte read against the current projected binding. Firebase callable delivery checks immutable generation/checksum/content type and returns authenticated in-memory bytes as a blob-compatible data URL instead of Storage URLs. UI renders contained muted inline video with retry/cleanup and a separate shared answer action; CSP permits only local/blob/data media. Verified: full game suite (50), focused UI suite (5), Functions suite (34), app/node typechecks, Functions build, game lint, and production client build. An isolated SQLite-backed server displayed 88 categories with `من سجل الهدف؟` ready and selectable on the category board; direct CUA submission was rejected by the intentional local-origin guard, so root's browser review remains the final live host/display check. No production mutation or release promotion was attempted.

Repair evidence (2026-09-11): source-pinned local inventory labels now create category rooms for imported goals; host/audience shared reveal emits answer text and clear binding only after a nonempty exact occurrence match; legacy rooms with absent occurrence fields are unrevealed. All reopen paths retain a stopped buzzer after reveal; current-media authorization denies CELL_SELECTION for every role. Firebase byte delivery validates immutable PNG/JPEG/MP4 content type, object root, generation, checksum and magic; the actual rebuilt JPEG fixture passes authorization/delivery. Same-binding video grant refresh preserves its Blob source, while a clear variant starts fresh. Root live browser verified local blur-to-clear reveal, next-question reset, and an actual image. Prompt now precedes contained media on host and display. Verified repair checks: 52 game, 36 Functions, 6 focused UI; typechecks and production build pass.

## M-MEDIA-03 / T17.3 — Online release preparation and rollout

Owner Forge implementation, root integration; depends T17.2 accepted and required release Gate. Recheck Firebase billing/Storage/Functions/App Check/active release and Vercel target with read-only tooling. Historical blockers in DEPLOYMENT.md are not current evidence. Resolve immutable catalog lookup against exact release; preserve signed content review requirements.

Sequence: preflight -> verified private upload and immutable metadata -> reviewed release preparation -> compatible backend/rules -> verified activation/audit -> Firebase-mode Vercel deployment -> online role/room/media smoke. Publish only scoped source delta; preserve existing user work, no broad staging. Source/media/data/receipts excluded from public packaging as appropriate.

Acceptance: exact cloud object checksums/generations, verified release pointer, online goal/image host/display flow and new-question reset; production never represented by static fixture preview. Gate required for release/privacy. If external infrastructure/review prerequisites block, complete independent local/code/preparation work and record exact blocker and next action, not Done. Rollback previous frontend/backend and verified content pointer, keep immutable assets/releases.

Status: Implementation and deployment preparation accepted by root — final release/security Gate PASS on 2026-09-11. Online rollout is blocked by external authentication/review prerequisites and is not Done. [Final independent Gate evidence](../output/t17-gate/provenance-pass.log) verifies stale-report and inherited-nonce rejection plus valid audited-base-first publication.

Implementation evidence (2026-09-11): the generic goal exporter now reads the ignored reviewed Arabic-alias input and reproduces the accepted 99-question manifest exactly without re-encoding. Offline preparation verified actual bytes for 240 V18 PNGs, 51 rebuilt JPEGs, and 198 goal MP4s; it requires create-only upload plus exact immutable generation, SHA-256, MIME, byte size, width/height, and video duration readback before release associations. Release category labels are now copied into `releases/<releaseId>/catalogCategories/*` and Firebase room creation reads that exact pinned release, with mutable global catalog fallback restricted to old demo fixtures. Firebase Hosting and app-shell CSP permit contained `blob:`/`data:` media while retaining the approved Firebase/App Check and reCAPTCHA origins plus explicit emulator ports. Functions emulator delivery verifies the fixed private goal MP4 registry; the Firebase client decodes the authenticated callable data URL directly to a Blob for PNG/JPEG/MP4, without a Storage URL or a CSP network request. [Dry media plan](../output/t17-release-prep/PRIVATE-MEDIA-DRY-RUN.json) records 489 verified assets; [blocked preflight](../output/t17-release-prep/T17.3-PREFLIGHT-BLOCKED.json) records no cloud action, scoped private-answer scan, missing reviewed v3.3 artifacts, and next action. The initial Firebase E2E repair run had 6 failures, then one private-image failure; the repaired full suite passed 10/10 and the dedicated real goal blur-to-clear host/display reveal passed 1/1 ([root logs](../output/t17-root/firebase-e2e-final.log), [goal log](../output/t17-root/firebase-goal-final.log)). Focused release/composite/adapter/CSP checks, scoped lint, app typecheck, and production client build pass. The full node typecheck remains separately blocked by concurrent non-owned Firebase content-sync files; root's scoped exclusion check passes.

Composite contract: the retained exact-62/minimum-300 V3.3 release remains unchanged. `scripts/reviewed-media-composite-release.ts` builds a single immutable composite root for a signed reviewed-media supplement: it recomputes every base child/catalog root commitment, binds prompt versus answer asset roles and all answer text into each reviewed question hash, requires all 99 stable goal records and 198 paired MP4 bindings, and supports additional signed PNG/JPEG image records only for exact copied category labels with collision checks. Its trusted-authority and review-report loaders require canonical exact-byte SHA-256 anchors, regular external trust artifacts outside the untrusted base/supplement directories, valid trusted time/issuer schema, exact report/receipt commitments, and unique raw-nonce SHA-256 claims. `firestore-release-publisher.ts` exposes `composite-plan`, `composite-prepare`, `composite-verify`, `composite-activate`, and `composite-audit`; each publication lifecycle step requires an already prepared and exactly verified immutable V3.3 base root, its publication receipt with complete inherited nonce commitments, and global claim ownership. Older base receipts without that provenance fail closed for composite publication; existing non-composite releases are unchanged. Self-contained tests cover the tamper, stale-answer, role-swap, unrelated-key, nonce-replay, image-binding, raw-report, trust-boundary, and prepare→verify→CAS cases. No approved supplement/review report/external trust anchor exists, so no online plan, upload, publication, or activation was attempted.

## Handover

Local media/data and runtime/UI are accepted (T17.1/T17.2), as is T17.3 deployment preparation after final Gate PASS. The actual SQLite-backed app is running at http://127.0.0.1:8787; root refreshed it after the Huroof recommendation fix and confirmed 88 categories and 99 ready goal questions. [Current inventory](../output/t17-root/live-app-inventory.json) and [media failure checks](../output/t17-root/media-failure-check.json) record selector readiness and wrong-bearer/expired/missing/stale media rejection. Firestore/Storage emulator rules pass; [Firebase E2E evidence](../output/t17-root/firebase-e2e-check.json) records the repaired full 10-test pass plus the dedicated real goal test pass. The goal test verifies each screen's own blur-to-clear transition, decoded video, shared answer, removed timer and unchanged scores. App typecheck/build and scoped Node typecheck pass. The latest whole-project Node check fails in concurrent, non-owned `scripts/firebase-content-sync-production.ts:82` (`parentCollections` is undefined); [full log](../output/t17-root/typecheck-final.log), [scoped configuration](../output/t17-root/tsconfig.scoped-node.json). No unrelated source was changed to hide those failures. Preserve concurrent later database imports; the T17.1 backup is historical evidence and must not overwrite newer rows.

Next authorized action: run `firebase login --reauth`, then recheck billing, private Storage, Functions, App Check, deployment credentials and the actual active release. The last [read-only authentication retry](../output/t17-root/online-readiness.json) failed before production API reads; historical infrastructure findings are not current confirmations. Obtain the complete reviewed V3.3 base corpus and signed media supplement/review report with an externally anchored authority. Prepare and exactly verify the base, upload private media create-only, and bind actual generation/hash/MIME/size/dimensions/timing readback. Use `scripts/firestore-release-publisher.ts` operations `composite-plan`, `composite-prepare`, `composite-verify`, `composite-activate`, then `composite-audit`; the CLI requires exact base/supplement/report/trust paths and hashes, pinned project/database/location, and activation additionally requires the expected active release, audit reference and verification receipt. Deploy compatible Functions and the Firebase-mode Vercel app only against those verified inputs, then run real online host/display goal and image smoke checks. Production was not uploaded, deployed or activated in this task.

Changed-file map: `.gitignore` and `scripts/t17-goal-media.py` cover private media/import preparation; `server/service.ts`, `server/index.ts` and the local source loaders cover SQLite/runtime delivery; shared runtime contracts/adapters, `src/routes/GameRoutes.tsx` and `src/styles/global.css` cover reveal and presentation; `functions/src/game.ts`, `functions/src/question-media.ts` and `functions/src/index.ts` cover Firebase authority/delivery; the media/composite preparation scripts, release builder/publisher, V3.3 catalog contract and CSP files cover rollout preparation. Focused unit, integration, emulator and adversarial release tests accompany these changes. Original media, earlier questions and review states remain preserved. Existing mixed user work was not staged, committed or pushed. Elapsed approximately 110 minutes against the original 60–120 minute estimate. Astra coordination/security/release review and Terra implementation/database review were used; session/token telemetry unavailable.

### Initial review evidence — 2026-09-11
Database read-only review: SQLite integrity ok, 7,253 rows; JSON additions need no schema migration. Preserve stored staging/approval states; activate only verified supported media in runtime projection. Security review: current projection must not serialize private answer bindings; occurrence/phase-bound grants and authoritative byte checks are required. Root online check failed before API reads because Firebase CLI credentials expired; user asked to reauthenticate. Implementation continues locally.

