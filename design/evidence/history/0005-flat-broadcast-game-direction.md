# 0005 — Owner-approved flat midnight broadcast gameplay direction

Date: 2026-09-04  
Status: append-only implementation authorization; the human product owner remains the
only selection authority.

The selected Studio of Letters concept continues. For gameplay surfaces only, this record
supersedes the incompatible material-depth clauses in `0004-premium-material-evolution.md`
and the prior selection receipt's gameplay-material interpretation. The board remains a
440×440, 25-cell, six-neighbour board with the same q/r topology, centres, hit overlays,
and four fitted rails. It is now a midnight Arabic broadcast tabletop: flat dark-navy faces,
crisp inset keylines, and one continuous path per rail. Crimson is physical left/right for
the horizontal team; emerald is physical top/bottom for the vertical team.

The audience composition is board first, then a white outlined timer/round module above the
board and a white question tray below it. Compact score cards sit physically left (green) and
right (red), expose exactly the labels `الأخضر` and `الأحمر`, and show real round state only.
The approved evidence images are reference/provenance material, never semantic product UI.

Forbidden for gameplay: SVG/CSS gradients, shell/face depth, rail-depth faces, highlights,
shades, filters, shadows, glows, glass, decorative rounding, tall colour slabs, fake
scores/timers, duplicate audience countdown state, gold/yellow/violet/indigo, and any
coordinate or topology change.

C1: “The deterministic validator may verify only evidence identity, structure, dimensions,
provenance, and threshold consistency; it must never certify aesthetic quality or emit an
aesthetic PASS.”

C2: “Every AI/model review is optional and advisory; it cannot authorize or block exact
human selection or live-review gates. Distinct-family, distinct-session, read-only,
provenance-backed review may be provenance-separated but remains advisory, and same-family
or same-session review is never independent.”

C3: “History bindings must remain valid when later directions or outcomes are appended; no
implementation may revalidate an old receipt against a different mutable whole-file history
hash.”

C4: “Preserve all existing uncommitted and untracked work; do not reset, stash,
broad-stage, auto-commit, or import stale runtime policy over canonical source.”
